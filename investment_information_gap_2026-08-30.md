# 投資判断に足りない情報の棚卸し・第2巡(2026-08-30)

前回の棚卸し(`investment_decision_gap_2026-08-29.md`、J-1〜J-10)は
**「判断の段階(A〜H)に穴が空いていないか」**を見た。会社概要・財務推移・
バリュエーションの現在地・分位点・チェックリスト・需給・カレンダーが入り、
段階の枠としては埋まった。

本文書は視点を変える。埋めるのは枠ではなく**読み方**である。

> **実際に発注ボタンを押す直前、プロが手元に置いていて、いまの画面に無いもの。**

前回との違いは3つ。

1. 前回は**縦方向**(1銘柄について何を出すか)を埋めた。今回は**横方向**
   (誰と比べるのか)と**時間方向**(実際にどれだけ動いた銘柄なのか)を埋める。
2. 前回は「会社の姿(静的な事実)」を出した。今回は**「経営陣の行動(動的な事実)」**
   ——予想を守るか、自社株を持っているか、誰が買い増しているか——を出す。
3. 原則4(既にDBにあって画面に出ていないものを先に直す)を、**今日DBを直接数えて**
   再適用した(→ §1)。前回より深刻な取りこぼしが見つかっている。

---

## 0. 前提:引き継ぐモデルが最初に読むもの

**判断の原則は 30.1.1 をそのまま踏襲する。**

- **原則1**:機械が測れるものだけ機械に測らせる。定性判断は代行せず、記録の器と外部への導線を用意する。
- **原則2**:利用者が書くデータはファイル(git)、機械が導くデータはDB。
- **原則3**:EDGAR 由来・需給由来のシグナルを `evaluate_gates` とスコアリングに入れない。
- **原則4**:既にDBにある情報を画面に出していない箇所は、新規データ収集より先に直す。

**本文書はここに原則5を足す。**

- **原則5**:**実測できるリスクを、モデルの仮定で代用しない。**

  根拠は画面自身が書いている。`TickerDetailPage.tsx:312-319` は実現倍率の幅について
  「モデルの仮定によるもので、実測で較正されていない」「σ の縮小(0.85)により銘柄差は
  15% しか残らないため、幅はほぼ全銘柄で似た形になる」「銘柄ごとにリスクを測れている
  わけではありません」と**自分で断っている**。
  にもかかわらず、`price_snapshots` の **3,594,456 行**から実現ボラティリティも
  最大ドローダウンも一度も計算していない。**測れるものを測らずに、測れないものを
  仮定で埋めて見せている**——これが今回いちばん大きな構造的な穴である。

**実装の順序・規約は 30.11 と 30.0.2 に従う。**
本計画には**マイグレーションが1件も無い**(§4 で確認)。したがって 30.11 の
「マイグレーションを持つ項目を並行着手しない」制約は今回かからない。

---

## 1. 実測:DBにあって画面に出ていないもの(2026-08-30 時点)

すべて `docker exec autoscreener-db-1 psql -U autoscreener -d autoscreener` で数えた実数である。

| データ | 所在 | 行数/カバレッジ | 画面表示 | 対応 |
|---|---|---:|---|---|
| 日次OHLCV | `price_snapshots` | **3,594,456 行**(直近400日で252営業日以上ある銘柄 **4,866**) | 52週レンジのみ | **L-1 / L-6 / L-8** |
| ベンチマーク価格 | 同上(`tickers.is_benchmark`) | IWM/IJR 2000年〜, SPY 1993年〜, IWC 2005年〜 | **無し** | **L-9** |
| 決算サプライズ履歴 | `raw_snapshots.payload.earnings_dates` | **4,324 行**(date / eps_estimate / reported_eps / surprise_pct) | **無し** | **L-3** |
| アナリスト予想の改訂 | `raw_snapshots.payload.eps_revisions` | **4,324 行** | **無し** | **L-3** |
| インサイダー保有比率 | `payload.info.heldPercentInsiders` | **16,309 / 16,534(98.6%)** | **無し** | **L-4** |
| 機関投資家保有比率 | `payload.info.heldPercentInstitutions` | **16,309(98.6%)** | **無し** | **L-4** |
| 経営陣(氏名・役職・年齢・報酬) | `payload.info.companyOfficers` | **16,418(99.3%)** | **無し** | **L-4** |
| 浮動株数 | `payload.info.floatShares` | **15,181(91.8%)** | 需給欄に金額のみ | **L-4** |
| 実測ベータ | `payload.info.beta` | **13,592(82.2%)** | **無し** | **L-1** |
| 提出書類の時系列 | `filings` | **163,659 行**(8-K **31,451**, DEF 14A **2,851**, SC 13G **3,149**, SC 13D **408**) | **無し**(API も client 関数もあるのに未使用) | **L-5** |
| 顧客集中 | `customer_concentration` | **0 行**(収集バッチ未実行・API未配線) | 無し | **L-10** |
| ガイダンス | `guidance` | **0 行**(同上) | 無し | **L-10** |
| 訴訟 | `litigation_events` | **0 行**(同上) | 無し | **L-10** |
| 循環性割引の実額 | `scores.factors.growth_cyclicality_adjustment` | 全スコア行 | バッジのみ、**値は非表示** | **L-7** |

### 1.1 とくに指摘しておくこと

**`GET /api/v1/filings/{ticker}`(`routes.py:2200`)も、それを呼ぶ
`fetchFilings()`(`frontend/src/api/client.ts:176`)も既に存在するが、
フロントエンドのどのページからも呼ばれていない。**
API・クライアント・型定義まで揃っていて、`<FilingsSection>` が無いだけである。
163,659 行の提出書類が、実装済みの経路の先で1件も表示されていない。

**`scores` の `score_date` は 4 日分しかない(2026-08-23 〜 2026-08-28)。**
順位の安定性(L-12)は、いま実装しても意味のある表示にならない。ただし
**順位は後から遡って復元できない**ので、保存だけは今のうちに始める価値がある。

---

## 2. 結論(先に3つ)

### ① サイジングに必要な「実測されたリスク」が画面に1つも無い

`config/portfolio.yaml` は `per_position_cap: 0.04` を全銘柄一律で適用している。
これは「全銘柄のリスクが同じ」という仮定である。実際には、この母集団には
年率ボラティリティ 30% の銘柄と 150% の銘柄が同居している。

さらに重要なのは**ドローダウンの実績**である。10バガーになった銘柄は、
その道中でほぼ例外なく −50% 以上の下落を複数回経験する。この事実を**買う前に**
見せておかないと、投資家は底で降りる。TENX は「7年で10倍」という保有前提を
要求しておきながら、**その7年間に何が起きるかを一度も見せていない。**

`moic_quantiles`(J-4)の帯は幅を見せているように**見える**が、画面自身が
「ほぼ全銘柄で似た形になる」と断っているとおり、銘柄の識別には使えない。
一方で実測に必要なデータは 359万行そろっている(→ **L-1**)。

### ② 「なぜこの銘柄で、隣の同業ではないのか」に答える材料が無い

J-3 でセクター内分位は入った。しかし分位は「何番目か」しか出さず、
**誰と比べたのかが見えない**。売上成長 45% が「業界の追い風」なのか
「その会社の実力」なのかは、同業を横に並べない限り判別できない。
プロの銘柄選択は絶対値ではなく**同業内の相対的地位**で行われる(→ **L-2**)。

### ③ 経営陣の行動を測るデータが集まっているのに、1つも出ていない

7年後への外挿は、突き詰めれば**経営陣の実行力に賭けている**。
その最も安価で最も一貫した代理変数が3つあり、**3つとも既にDBにある**。

- **自分の予想を守るか**:`earnings_dates` に過去のサプライズ履歴(4,324行)
- **自分の会社を持っているか**:`heldPercentInsiders`(16,309行、98.6%)
- **誰が買い増しているか**:`filings` の SC 13D(408件)/ SC 13G(3,149件)

いずれも表示されていない(→ **L-3 / L-4 / L-5**)。

---

## 3. 不足情報の全体マップ

前回の段階A〜Hに続く形で、段階I〜Mとして列挙する。
状態は ✅=実装済み / △=部分的 / ❌=無い。

### 段階I. リスクの実測(どれだけ揺れる銘柄か)

| # | 必要な情報 | なぜ必要か | 状態 | ギャップ |
|---|---|---|---|---|
| I1 | 実現ボラティリティ(年率) | ポジションサイズの分母。4%固定は「全銘柄同リスク」の仮定 | ❌ | **L-1** |
| I2 | 過去の最大ドローダウン(1年/3年/上場来) | 「7年持てるか」の唯一の実測。10倍株は道中で−50%を複数回経験する | ❌ | **L-1** |
| I3 | ドローダウンからの回復日数 | 含み損に耐える期間の実績 | ❌ | **L-1** |
| I4 | ベンチマークに対するβ・下落時捕捉率 | 市場全体が下げたときこの銘柄がどれだけ下げたか | ❌ | **L-1**(`info.beta` は 82% 取得済み) |
| I5 | モデルσとの突き合わせ | 仮定と実測がどれだけ違うかを利用者に見せる | ❌ | **L-1** |

### 段階J. 相対比較(この銘柄か、隣の同業か)

| # | 必要な情報 | なぜ必要か | 状態 | ギャップ |
|---|---|---|---|---|
| J1 | 同業ピアの横並び表 | 成長が業界の追い風か会社の実力かの判別 | ❌ | **L-2** |
| J2 | ピア選定の根拠の明示 | 誰と比べたか分からない比較は誤読を生む | ❌ | **L-2** |
| J3 | セクター内分位 | 相対的な割高割安 | ✅ | J-3 済み |
| J4 | ベンチマーク(IWM等)の実績分布 | 10倍狙いの機会費用はインデックス | ❌ | **L-9** |

### 段階K. 経営とガバナンス(誰が経営し、誰が持っているか)

| # | 必要な情報 | なぜ必要か | 状態 | ギャップ |
|---|---|---|---|---|
| K1 | 決算サプライズの履歴 | 経営陣の予測能力と誠実さの代理変数 | ❌ | **L-3** |
| K2 | アナリスト予想の改訂方向 | サプライズ単独は「予想を下げてbeat」で操作可能 | ❌ | **L-3** |
| K3 | アナリストカバー人数(0人か) | 0人であること自体が10バガー探索では重要な情報 | ❌ | **L-3** |
| K4 | インサイダー保有比率 | オーナー経営は最も一貫した定性変数 | ❌ | **L-4** |
| K5 | 機関投資家保有比率・浮動株比率 | 需給の厚みと、まだ誰も見ていないかの判定 | △ | 金額のみ。**L-4** |
| K6 | 経営陣の顔ぶれ(創業者が残っているか) | 同上 | ❌ | **L-4** |
| K7 | 大量保有報告(SC 13D/G)の時系列 | アクティビスト参入・機関の建玉変化 | ❌ | **L-5** |
| K8 | 直近の 8-K(役員辞任・重要契約・監査人交代) | 定量指標が動く前に出る一次情報 | ❌ | **L-5** |
| K9 | インサイダー売買(Form 4) | 実際の売買行動 | △ | 278銘柄のみ。**L-10** |

### 段階L. 執行の現実(建てられるか、降りられるか)

| # | 必要な情報 | なぜ必要か | 状態 | ギャップ |
|---|---|---|---|---|
| L1 | ADV・投入上限 | 執行可能性 | ✅ | 30.2.2 |
| L2 | **建てる/降りるのに何日かかるか** | 金額上限より投資家が実際に知りたい量 | ❌ | **L-6** |
| L3 | ADVの中央値と平均の乖離・ゼロ出来高日 | 出来高が数日に集中している銘柄の識別 | ❌ | **L-6** |
| L4 | ストレス時の出来高 | テールイベント時に降りられるか | ❌ | **L-6** |
| L5 | 往復コスト | 執行コスト | ✅ | 30.2.3 |

### 段階M. 保有中の規律(すでに持っている銘柄について)

| # | 必要な情報 | なぜ必要か | 状態 | ギャップ |
|---|---|---|---|---|
| M1 | 達成倍率・利食い計画・テーゼ点灯 | 売却規律 | ✅ | J-8 |
| M2 | **取得単価基準の残余必要倍率・残余必要CAGR** | 目標は今日の株価基準で毎日引き直される。買値からの残りが見えない | ❌ | **L-11** |
| M3 | 保有銘柄どうしの実測相関 | `asset_correlation` は全体一律の1つの値。鉱山株3つは実質1銘柄 | ❌ | **L-8** |
| M4 | 顧客集中の推移(テーゼ崩壊の先行指標) | `monitoring_metrics` が参照しているのにテーブルが空 | ❌ | **L-10** |
| M5 | 推定の足場(データ品質) | 上位30がクランプ銘柄に偏る既知問題の可視化 | △ | 警告バッジのみ。**L-7** |
| M6 | 順位の安定性・観測日数 | 「今日初めて上位に出た」と「30日連続上位」の区別 | ❌ | **L-12**(データが4日分しかない) |
| M7 | 税・為替・コスト控除前である旨 | 円建て投資家の実感値との乖離 | ❌ | **L-13** |

---

## 4. 優先順位とスコープ

| ID | 内容 | 種別 | 追加収集 | マイグレーション | 規模 |
|---|---|---|---|---|---|
| **L-0** | J-0(生存バイアス修正)の運用実行 —— **まだ実行されていない** | 運用 | EDGAR(実装済) | 不要 | 半日+実行時間 |
| **L-5** | 提出書類の時系列(8-K / SC 13D/G / DEF 14A) | フロントのみ | 不要 | 不要 | 半日 |
| **L-1** | 実測リスク(ボラ・最大DD・回復日数・β・下落時捕捉率) | 算出+表示 | 不要 | 不要 | 1.5日 |
| **L-3** | 決算サプライズとアナリスト予想改訂の履歴 | 算出+表示 | 不要 | 不要 | 1日 |
| **L-4** | 経営陣と株主構成(インサイダー保有・機関保有・役員) | 表示 | 不要 | 不要 | 半日 |
| **L-2** | ピア比較(同業内の横並び) | 算出+表示 | 不要 | 不要 | 1日 |
| **L-7** | 推定の足場(データ品質の集約表示) | 算出+表示 | 不要 | 不要 | 半日 |
| **L-6** | 執行の現実(建玉日数・ADV中央値・ストレス時出来高) | 算出+表示 | 不要 | 不要 | 半日 |
| **L-11** | 取得単価基準の残余必要倍率・残余必要CAGR | 算出+表示 | 不要 | 不要 | 半日 |
| **L-8** | 保有銘柄どうしの実測相関 | 算出+表示 | 不要 | 不要 | 1日 |
| **L-10** | 顧客集中・ガイダンス・訴訟の3テーブル(実行+配線) | 運用+配線 | EDGAR(実装済) | 不要 | 1.5日 |
| **L-9** | ベンチマークの実績分布(参照線) | 算出+表示 | 不要 | 不要 | 半日 |
| **L-12** | 順位の保存(表示は後日) | 保存のみ | 不要 | 不要 | 半日 |
| **L-13** | 期待倍率が税・為替・コストを含まない旨の明示 | 文言のみ | 不要 | 不要 | 1時間 |

**実装順序**:L-0 → L-5 → L-1 → L-3 → L-4 → L-2 → L-7 → L-6 → L-11 → L-8 → L-10 → L-9 → L-12 → L-13。

**本計画にマイグレーションは1件も無い。** 新規テーブルも新規カラムも作らない。
すべて「既にあるデータを計算して出す」か「既にあるバッチを走らせて配線する」である。
これは意図的な設計判断であり、理由は §7.3 に書いた。

### 4.1 全項目に共通する絶対の制約

> **どの項目も `probability`(順位)を1ビットも動かしてはならない。**

- `evaluate_gates` / `scoring/moic.py` / `scoring/engine.py` の**出力を変えない**。
- 新しい値を `scores.factors` に**書かない**(§7.3)。
- 受け入れ確認は全項目共通で次の1行:
  ```
  同一 score_date の run-scoring を変更前後で実行し、全銘柄の probability が完全一致すること
  ```
- 実測リスク(L-1)を σ の代わりに使いたくなるが、**やってはならない**。
  v4 の σ を触ると較正写像とバックテストKPIが全部無効になる。
  実測は**並べて見せるだけ**で、モデルには入れない(原則3の精神をリスク指標にも適用する)。

---

## 5. 実装計画

各項目は 30.11 の手順(設定 → 純粋関数 → バッチ → API → フロント → README → 受け入れ基準)に従う。
純関数は必ず `src/autoscreener/screening/` か `scoring/` に切り出し、
`tests/unit/` にテストを置く(フロントには vitest 基盤が無いため、型とビルドで担保する)。

---

### L-0. J-0 の運用実行(実装ではなく運用・最優先)

**状態**:`README.md` が 2026-08-29 時点で
「**モデル検証の回復(J-0)はまだ実行していません**」と書いており、今日も変わっていない。

これは前回の文書がそのまま有効である。繰り返す:

1. `uv run python -m autoscreener.cli recover-quarantine`
2. `.env` の `EDGAR_USER_AGENT` を設定して `uv run python -m autoscreener.cli collect-delistings`
3. `register-benchmarks` → `backfill-history`(**ベンチマークは既に4本入っている**ので確認のみでよい)
4. `run-backtest` を実行し、`delisted_settlement_rate > 0` になったか、
   v4 が `momentum_12m` / `revenue_growth` ベースラインを信頼区間つきで上回るかを記録する
5. 結果を `README.md` 冒頭の警告ブロックに反映する

**L-1〜L-13 はこの結果がどうであっても価値がある**(いずれもモデルの順位ではなく
一次的な事実の提示であるため)。ただし**順位そのものの信頼性はここでしか決まらない**。

---

### L-5. 提出書類の時系列(最初に着手する・バックエンド変更ゼロ)

**なぜ最初か。** API(`routes.py:2200`)もクライアント関数(`client.ts:176`)も
型定義(`types.ts:289-302` の `FilingListItem` / `FilingListResponse`)も**既にある**。
足りないのはコンポーネント1つだけで、163,659 行が表示されるようになる。
コスト最小・効果最大であり、原則4の純粋な適用である。

**なぜ投資判断に必要か。**
「今なぜこの株価なのか」は、直近の 8-K を3件読めば大半が分かる。とくに:

| Item | 内容 | 判断への効き方 |
|---|---|---|
| 1.01 / 1.02 | 重要契約の締結・終了 | 売上の非連続な変化。定量指標に出る2〜4四半期前に出る |
| 2.02 | 決算発表 | 決算日の確定(カレンダーの推定値の裏取り) |
| 3.01 | 上場基準の不適合通知 | 即死要因。`red_flags` が拾っていない形式もある |
| 4.01 / 4.02 | 監査人の交代・過年度決算の訂正 | 財務数値そのものの信頼性 |
| 5.02 | 役員の辞任・選任 | CFO の突然の辞任は最も強い定性シグナルの1つ |
| 8.01 | その他重要事象 | 訴訟・買収提案・規制対応 |

`SC 13D`(408件)は**アクティビストの参入**、`SC 13G`(3,149件)は
**機関投資家の5%超取得**を意味する。どちらも「誰が今この会社を買っているか」の一次情報である。

**実装**

1. 新規 `frontend/src/filingForms.ts`
   - `FORM_LABELS: Record<string, string>` —— `"8-K": "臨時報告(8-K)"`, `"SC 13D": "大量保有報告(アクティビスト)"`,
     `"SC 13G": "大量保有報告(受動的)"`, `"DEF 14A": "委任状(役員報酬・議決権)"` など
   - `ITEM_LABELS: Record<string, string>` —— 上の表の Item コード辞書
   - **未知のフォーム・Itemは原文コードのまま出す。空文字や「不明」に潰さない。**
2. 新規 `frontend/src/components/FilingsTimelineSection.tsx`
   - `fetchFilings(ticker, 30)` を呼ぶ。読み込み中・エラー・0件を区別して描画する
     (0件のときは「EDGAR未追跡の銘柄です」——`filings_checked_on == null` と同じ扱い)
   - 新しい順に、`filed_date` / フォーム日本語ラベル / Item ラベル / 原本リンク
   - **8-K / SC 13D / SC 13G / 4.01 / 5.02 は視覚的に強調する。**ただし
     `red_flags` のような警告色は使わない(事実の提示であって判定ではない)
   - 「Form 4(インサイダー)」は件数が多い(108,150行)ので**既定で折り畳む**
3. `TickerDetailPage.tsx` の「提出書類とレッドフラグ」セクション(現 353-380行)の**直後**に挿入する
   —— レッドフラグは「機械が判定した結果」、こちらは「判定していない生の時系列」であり、
   この順に並べると読み手が両者の性質の違いを取り違えない

**やってはいけないこと**

- 8-K の本文を要約して出すこと(原則1。要約は `LlmAnalysisSection` の担当で、最下部に置くと決まっている)
- Item コードから「良い/悪い」を判定して色を付けること

**受け入れ基準**

- [ ] `filings` に行がある銘柄で、直近30件が新しい順に出る
- [ ] `filings_checked_on == null` の銘柄でエラーにならず「未追跡」と出る
- [ ] 原本リンクが SEC で開く
- [ ] `npm run build` と `npm run lint` が通る

---

### L-1. 実測リスク(本計画の本丸)

**なぜ必要か。** §2① のとおり。加えて具体的に:

- **サイジング**:`per_position_cap: 0.04` を全銘柄に当てるのは、
  年率ボラ 35% の銘柄と 140% の銘柄を同じリスクとみなすということである。
  この文書では規律そのものは変えない(§5 の「やってはいけないこと」)が、
  **利用者が自分で判断できるように実測値を並べて出す。**
- **保有継続**:10バガーの道中は −50〜−70% の下落を複数回含む。
  「この銘柄は過去3年で最大 −68% を経験し、回復に 412 日かかった」を
  **買う前に**読ませることは、TENX が要求している7年保有と表裏一体である。
- **モデルとの突き合わせ**:`log_moic_sigma` が仮定でほぼ一律であることは
  画面が自白している。実測ボラを並べれば、その乖離が利用者に見える。

**データ源**:`price_snapshots`(3,594,456行、追加収集不要)。
ベンチマークは `tickers.is_benchmark` の4本(IWM/IWC/IJR/SPY、2000年〜)。

**実装**

1. 新規純関数 `src/autoscreener/screening/price_risk.py`

   ```python
   @dataclass(frozen=True)
   class PriceRisk:
       observation_days: int
       realized_vol_1y: float | None        # 日次対数リターンの標準偏差 × sqrt(252)
       max_drawdown_1y: float | None        # 負の値(-0.68 = -68%)
       max_drawdown_3y: float | None
       max_drawdown_days_3y: int | None     # 最大DDの谷までの日数
       recovery_days_3y: int | None         # 谷から前高値回復まで。未回復なら None
       currently_in_drawdown: float | None  # 現在値の前高値からの下落率
       beta_1y: float | None                # ベンチマークに対する回帰係数
       downside_capture_1y: float | None    # ベンチマークが下げた日だけで測った捕捉率
       benchmark_symbol: str | None

   def compute_price_risk(
       closes: Sequence[tuple[datetime.date, float]],
       benchmark_closes: Sequence[tuple[datetime.date, float]] | None,
       *, min_observations: int = 60,
   ) -> PriceRisk | None: ...
   ```

   **実装上の注意(すべて過去に踏んだ落とし穴に対応する)**

   - **日付で内部結合してから**リターンを取る。位置揃えでベータを計算すると、
     片方だけ取引停止した日で系列がずれる
   - 観測が `min_observations` 未満なら該当フィールドを `None` にする。
     **0 や 1.0 で埋めない**(27.20:「測れなかった」と「測った結果がこうだった」を分ける)
   - 分割・配当は `price_snapshots` の格納方針に合わせる。
     `dividend` 列があるので、**総リターンで計算する**(D-11 と同じ理由。
     価格リターンだけだと配当を出す銘柄のボラが系統的に低く出る)
   - `benchmark_closes` が無い/短い場合は `beta_1y` と `downside_capture_1y` のみ `None` にし、
     ボラと最大DDは返す(部分的な結果を捨てない)
   - ベンチマークは**時価総額で選ぶ**:マイクロキャップは IWC、それ以外は IWM。
     選んだ銘柄を `benchmark_symbol` で返し、UIに必ず出す(何と比べたか分からない β は無意味)

2. API
   - `schemas.py` に `PriceRiskView`(上の dataclass と同型)を追加し、
     `CandidateDetail` に `price_risk: PriceRiskView | None = None` を足す
   - `routes.py` の `get_candidate_detail` は **既に `price_snapshots` を読んでいる**
     (`compute_price_range` の呼び出しが `routes.py:1058`)。同じ結果を使い回し、
     **クエリを増やさない**。ベンチマーク系列だけ1本追加で引く
   - `CandidateSummary` に `realized_vol_1y` と `max_drawdown_3y` を追加。
     一覧の50行分は `ticker_id IN (...)` の**1クエリ**で取り、N+1にしない
   - **ソート対象にはしない**(順位のキーは `probability` のまま。§4.1)

3. フロント
   - `TickerDetailPage.tsx` の「実現倍率の幅」セクション(現 285-321行)の**直後**に
     「実測された値動き」セクションを置く。**モデルの幅の直後に置くのが要点**で、
     仮定(P10–P90)と実測(ボラ・最大DD)を並べて読ませる
   - 表示例:

     ```
     実測された値動き(直近1年 / 3年)          観測 251 日
       年率ボラティリティ   62%     ← モデルσ 0.41 との比較を併記
       最大ドローダウン     -68%(3年)/ -41%(1年)
       谷までの日数         184日 ・ 回復まで 412日
       現在の位置           前高値から -23%
       β(IWM 基準)        1.84 ・ 下落時捕捉率 1.31
     ```

   - **必ず出す注記**(文言はこの趣旨で):
     > これは過去の実測値であり、将来の値動きの予測ではありません。ただし
     > 上の「実現倍率の幅」がモデルの仮定であるのに対し、**こちらは実際に起きたことです。**
     > 目標年数まで保有するとは、この振れ幅に耐えるということです。
   - ランキング一覧に「年率ボラ」「最大DD(3年)」の2列を追加する
     (列が多い表なので、`table-scroll` の既存の横スクロールに収める)

**テスト**(`tests/unit/test_price_risk.py` 新規)

- [ ] 単調増加の系列で `max_drawdown == 0`、`recovery_days is None`
- [ ] 既知のV字系列(100→50→100)で `max_drawdown == -0.5`、回復日数が正しい
- [ ] 未回復の系列で `recovery_days is None`、`currently_in_drawdown` が入る
- [ ] 観測 59 日で `realized_vol_1y is None`(0.0 ではない)
- [ ] ベンチマークが `None` でも β 以外が返る
- [ ] 銘柄とベンチマークで欠測日がずれている系列でも、日付結合により正しい β が出る
- [ ] 配当のある系列で総リターンベースのボラになっている

**受け入れ基準**

- [ ] 上位20銘柄すべてで実測ボラと最大DDが出る
- [ ] 上場直後の銘柄(観測60日未満)で「観測不足」と出てエラーにならない
- [ ] 同一 score_date の `run-scoring` の `probability` が変更前後で全銘柄一致

**やってはいけないこと**

- 実測ボラを `log_moic_sigma` に代入する/混ぜること。σ を触ると 28.4・28.8 の
  較正写像とKPIが無効になる。**次の巡回で正式に測って提案すべき論点であって、この巡回の作業ではない**
- 実測ボラから `per_position_cap` を自動調整すること。サイジング規律の変更は
  KPI で測らずに入れてはならない(`config/portfolio.yaml` 冒頭の「この値はモデルが決めるものではない」)

---

### L-3. 決算サプライズとアナリスト予想改訂の履歴

**なぜ必要か。** 7年の外挿は経営陣の実行力への賭けである。その実行力を測る、
今日ただちに使える代理変数が `earnings_dates` に入っている。

**同時に、これは単独で読んではならない指標でもある。**
サプライズは「予想を下げてから beat する」ことで操作できる。したがって
**`eps_revisions`(予想が上に動いているか下に動いているか)と必ず並べて出す。**
予想が下がりながら beat が続いている会社は、beat が続いていること自体が悪い報せである。

**カバレッジは 4,324 / 16,534(26%)にとどまる。これ自体が情報である。**
残り74%は**アナリストが誰も見ていない銘柄**であり、10バガー探索においては
「まだ誰も見ていない」という肯定的な読みと「情報が薄い」という否定的な読みの
両方が成り立つ。**「未取得」ではなく「アナリストのカバーなし」と明示的に書き分ける。**

**データ源**:`raw_snapshots.payload.earnings_dates` / `.eps_revisions`(追加収集不要)。
実際の格納形式(実測済み):

```json
[{"date": "2026-11-04", "eps_estimate": 0.38, "reported_eps": null,  "surprise_pct": null},
 {"date": "2026-08-04", "eps_estimate": 0.35, "reported_eps": 0.4,   "surprise_pct": 13.76},
 {"date": "2026-05-05", "eps_estimate": 0.30, "reported_eps": 0.35,  "surprise_pct": 15.77}]
```

`reported_eps == null` の行は**将来の予定**であり、実績と混ぜてはならない。

**実装**

1. 新規純関数 `src/autoscreener/screening/earnings_history.py`

   ```python
   @dataclass(frozen=True)
   class EarningsHistory:
       covered: bool                       # アナリスト予想が存在するか
       analyst_count: int | None           # info.numberOfAnalystOpinions
       periods: list[EarningsPeriod]       # 実績のある回のみ、新しい順に最大8件
       beat_count_8q: int | None
       miss_count_8q: int | None
       mean_surprise_pct_8q: float | None
       median_surprise_pct_8q: float | None
       consecutive_beats: int | None       # 直近から連続で beat した回数
       estimate_revision_30d: float | None # eps_revisions の上方修正比率
       estimate_revision_7d: float | None
       next_estimate: float | None         # reported_eps が null の直近1件
   ```

   - `surprise_pct` は yfinance の値をそのまま使わず、
     **`(reported - estimate) / abs(estimate)` で再計算する**。
     `estimate` が 0 近傍のときは `None`(ゼロ除算と、±数千%の外れ値の両方を防ぐ)
   - 赤字企業(estimate が負)ではサプライズの符号が直感と逆転する。
     **estimate < 0 の回は `surprise_pct` を `None` にする**か、UIで「赤字予想」と明示する
   - `eps_revisions` の実際のキー名は銘柄ごとに揺れる可能性があるので、
     `point_in_time.py` の `_series` と同じ**表記ゆれ吸収の方針**を踏襲する

2. API
   - `GET /api/v1/candidates/{ticker}/financials`(`routes.py:1355`)の
     `FinancialHistoryResponse` に `earnings: EarningsHistoryView | None` を足す。
     **新しいエンドポイントは作らない**(同じ `raw_snapshots` の payload を読むため、
     クエリが増えない)

3. フロント
   - `FinancialHistorySection.tsx` の末尾に「予想と実績」サブセクションを追加
   - 直近8四半期を、予想 / 実績 / 差(%)の表 + 連続 beat 数
   - **予想改訂を必ず横に並べる**:「直近30日で予想が +4.2% 上方修正」
   - 注記(この趣旨で):
     > サプライズは「予想を下げてから上回る」ことで作れます。予想の改訂方向と
     > 必ず一緒に読んでください。予想が下がり続けながら beat が続いているのは
     > 良い状態ではありません。
   - カバーなしの表示:
     > この銘柄をカバーしているアナリストはいません(予想が存在しないため、
     > 予想対比は測れません)。これは欠損ではなく事実です。

**テスト**(`tests/unit/test_earnings_history.py` 新規)

- [ ] `reported_eps == null` の行が実績に混ざらない
- [ ] `eps_estimate == 0` でゼロ除算せず `None` になる
- [ ] `eps_estimate < 0`(赤字予想)の回が `None` になる
- [ ] 連続 beat の数え方が直近から途切れるまでで正しい
- [ ] `earnings_dates` キーが存在しない payload で `covered=False` が返り例外にならない

**受け入れ基準**

- [ ] カバーのある銘柄で直近8四半期の予想対比が出る
- [ ] カバーの無い銘柄で「アナリストのカバーなし」と出て、未取得と区別できる

---

### L-4. 経営陣と株主構成(スキン・イン・ザ・ゲーム)

**なぜ必要か。** インサイダー保有 22% の会社と 0.4% の会社では、
増資・買収提案への対応・役員報酬・資本配分の意思決定が構造的に違う。
7年間の外挿を信じるかどうかは、**その7年を経営する人間が同じ船に乗っているか**に依存する。
これは定量モデルが構造上まったく扱っていない次元であり、しかもデータは 98.6% そろっている。

**データ源**:`payload.info` の
`heldPercentInsiders`(16,309)/ `heldPercentInstitutions`(16,309)/
`companyOfficers`(16,418)/ `floatShares`(15,181)。追加収集不要。

**実装**

1. `schemas.py` の `CompanyProfile`(J-1 で追加済み)に追記:

   ```python
   held_percent_insiders: float | None = None
   held_percent_institutions: float | None = None
   float_ratio: float | None = None            # floatShares / sharesOutstanding
   officers: list[OfficerView] = []            # 上位5名(name / title / age / total_pay)
   ```

2. `routes.py` の `get_candidate_detail` で `payload["info"]` から詰める。
   J-1 の実装のすぐ隣で、**追加のクエリは不要**。
   `companyOfficers` は配列なので、`totalPay` 降順で上位5名に切る。

3. フロント:`TickerDetailPage.tsx` の会社概要セクション(現 127-186行)の中に追加。

   ```
   保有構成   インサイダー 18.4% ・ 機関投資家 31.2% ・ 浮動株比率 62%
   経営陣     Jane Doe(CEO, 51) / John Roe(CFO, 47) / ...
   ```

**やってはいけないこと**

- `heldPercentInsiders` を SEC の受益所有比率と同一視して断定すること。
  yfinance の定義は曖昧で、DEF 14A の受益所有表とは一致しない。
  **必ず DEF 14A へのリンクを併記し、「一次情報で確認してください」と書く**
  (`filings` に DEF 14A が 2,851 件あるので、L-5 の時系列から辿れる)
- 保有比率から良し悪しを判定して色を付けること(原則1)

**テスト**

- [ ] `heldPercentInsiders` が無い payload で `None` が返り 200 になる
- [ ] `companyOfficers` が空配列/欠損でも例外にならない
- [ ] `floatShares > sharesOutstanding` の異常値で `float_ratio` が 1.0 を超えても
      クランプせずそのまま返す(データ異常を隠さない。UI側で注記する)

**受け入れ基準**

- [ ] 上位20銘柄すべてでインサイダー保有比率と経営陣が出る
- [ ] DEF 14A へのリンクが出る(CIK 未解決の銘柄では L-5 と同じく「CIK 未解決」と出す)

---

### L-2. ピア比較(同業内の横並び)

**なぜ必要か。** §2② のとおり。分位(J-3)は「何番目か」を出すが、
**誰と比べたかを出さない**。売上成長 45% が業界全体の追い風なのか
その会社固有の実力なのかは、同業を並べないと判別できない。
また「同じテーマで、もっと安く、もっと成長している会社が隣にある」という発見は、
ランキング上位だけを縦に見ていると絶対に起きない。

**データ源**:`scores` + `tickers.industry` / `sector` / `market_cap`。追加収集不要。

**実装**

1. 新規純関数 `src/autoscreener/screening/peers.py`

   ```python
   def select_peers(
       target: PeerCandidate,
       universe: Sequence[PeerCandidate],
       *, max_peers: int = 10, cap_ratio_range: tuple[float, float] = (0.2, 5.0),
   ) -> PeerSelection: ...
   ```

   - 第1段:同一 `industry` かつ時価総額が対象の 0.2〜5.0 倍
   - 第2段:第1段が3社未満なら同一 `sector` に緩める。**緩めたことを返り値で示す**
   - 第3段:それでも3社未満なら**空を返す**(無理に並べない)
   - 並び順は時価総額降順。対象銘柄自身を必ず含め、UI でハイライトする

2. API:`GET /api/v1/candidates/{ticker}/peers`
   - 同じ `score_date` の断面からのみ選ぶ。**日付をまたいでプールしない**(D-4 の再発防止)
   - 各行の列:`ticker` / `company_name` / `market_cap` / `probability` / `rank` /
     `expected_moic` / `revenue_growth` / `gross_margin` / `ev_to_gross_profit` /
     `net_debt_to_gross_profit` / `share_growth_rate`
   - `peer_basis: "industry" | "sector" | "none"` と `peer_count` を必ず返す

3. フロント:詳細画面の「バリュエーションの現在地」セクション(現 453-534行)の直後
   - 対象銘柄の行を太字+背景色でハイライト
   - 各列の**最良値にだけ**控えめな印を付ける(全部に色を付けると比較が読めなくなる)
   - **選定根拠を表の上に必ず1行で書く**:
     > 比較対象:同業種(Software - Application)で時価総額が 0.2〜5倍の 8 社。
     > 同じ日の断面から選んでいます。
   - `peer_basis == "sector"` のときは:
     > 同業種の銘柄が3社未満だったため、セクター単位まで広げています。比較の精度は落ちます。
   - `peer_basis == "none"` のときは表を出さず「比較対象が見つかりません」と出す

**やってはいけないこと**

- ピア中央値からの乖離を新しいスコアにすること(§4.1)
- ピアを `probability` 順に並べること。順位で並べると「TENX の順位が正しい」という
  前提の上でしか読めない表になる。**時価総額順**にして、順位は1列として見せる

**テスト**(`tests/unit/test_peers.py` 新規)

- [ ] 同 industry が 10 社超のとき、時価総額レンジ内から 10 社に切られる
- [ ] 同 industry が 2 社のとき sector にフォールバックし `peer_basis == "sector"`
- [ ] sector でも 2 社のとき空 + `peer_basis == "none"`
- [ ] 対象銘柄自身が必ず結果に含まれる
- [ ] `industry` が `None` の銘柄で例外にならない

**受け入れ基準**

- [ ] 上位20銘柄のうち15銘柄以上で industry ベースのピアが3社以上出る
      (下回るなら `tickers.industry` の充足率を先に調べる)
- [ ] 選定根拠が必ず画面に出る

---

### L-7. 推定の足場(この銘柄の数字はどれだけデータに支えられているか)

**なぜ必要か。** `model_audit_v4_2026-08-26.md` の既知問題
「**上位30銘柄はモデルの外挿限界(クランプ)に当たった銘柄に偏っている**」は、
今も解消していない。`warnings` はコード列として出ているが、
**一覧では右端の小さなバッジ**であり、順位と並べて読める形になっていない。
結果として、利用者は「1位だから良い」と読み、「1位なのは成長率が上限に張り付いて
外挿されたからだ」とは読まない。

さらに `growth_cyclicality_adjustment`(理論監査 30.1 で入れた循環性割引の実額)は
`scores.factors` に入っているのに、`TickerDetailPage.tsx` の `DIAGNOSTICS`(17-47行)に
載っていないため**値が一度も表示されていない**。バッジで「循環性あり」とは出るが、
「どれだけ割り引いたか」は出ない。

**実装**

1. 新規純関数 `src/autoscreener/screening/evidence_grade.py`

   ```python
   def compute_evidence_grade(
       *, warnings: Sequence[str], reconciliation: Sequence[ReconciliationItem],
       annual_period_count: int, quarterly_period_count: int,
       data_age_days: int | None,
   ) -> EvidenceGrade: ...
   ```

   返すもの:`grade: "A" | "B" | "C"`、`clamp_count`、`missing_count`、
   `reconciliation_mismatch_count`、`period_count`、`reasons: list[str]`。

   - 入力は**すべて既に detail が持っている値**である。新しい収集も新しい計算も不要
   - 減点方式ではなく**理由の列挙**を主にする。等級は要約であって、
     読むべきは `reasons`(「初期成長率が上限クランプ」「SEC突合で1件不一致」等)

2. API:`CandidateDetail` と `CandidateSummary` に `evidence_grade` を追加

3. フロント
   - ランキング一覧の「警告」列を「足場」列に置き換える(A/B/C バッジ + ツールチップに理由)
   - 詳細の `DIAGNOSTICS` 配列に **`growth_cyclicality_adjustment` を追加**:
     ラベル「循環性による成長率の割引」、書式 `${(v*100).toFixed(1)}pt`

**やってはいけないこと**

- 等級を `probability` に掛けること。順位を動かすとバックテストの検証資産が無効になる
- 等級 C を「悪い銘柄」と読ませる文言にすること。**C は「この銘柄の推定は
  データに薄く支えられている」であって「この会社が悪い」ではない**。UIの文言で必ず分ける

**受け入れ基準**

- [ ] 上位30銘柄の等級分布が出て、クランプ偏在が一覧から読み取れる
- [ ] `probability` が変更前後で全銘柄一致

---

### L-6. 執行の現実(建てるのに何日、降りるのに何日)

**なぜ必要か。** `max_position_usd` は金額を出すが、投資家が本当に知りたいのは
**「降りるのに何日かかるか」**である。ADV の 10% で建てられる金額を提示されても、
その金額を売り切るのに何日かかるかは書かれていない。

さらに `ADV は平均`である。出来高が決算日など数日に集中している小型株では、
**中央値が平均の 1/3 以下**になることがあり、平常時の板は ADV から想像するより
はるかに薄い。テールイベント時(まさに降りたいとき)の出来高はさらに落ちる。

**データ源**:`price_snapshots.volume`。追加収集不要。

**実装**

1. `src/autoscreener/screening/liquidity.py` に**追加**する(既存の
   `compute_liquidity_profile` の**戻り値と既存フィールドは一切変えない**
   —— ゲートと `positions` のサイジングが依存している):

   ```python
   adv_median_20d: float | None
   adv_stress: float | None        # 直近1年の出来高下位10%日の平均売買代金
   zero_volume_days_60d: int
   days_to_build: float | None     # max_position_usd / (adv_usd * participation_cap)
   days_to_exit_stressed: float | None  # 同上を adv_stress で割ったもの
   ```

2. フロント:詳細画面「取扱可否・流動性」(現 324-350行)に併記。

   ```
   投入上限  $3,200(板が制約)
     建てるのに   1.2 日  ・  ストレス時に降りるのに  4.8 日
     ADV 平均 $32.0K / 中央値 $11.4K(平均の 36%)・ 直近60日で出来高ゼロの日 3 日
   ```

   - 平均と中央値の乖離が 2 倍を超えるとき、または `zero_volume_days_60d > 0` のときは
     注記を出す:
     > 出来高が一部の日に集中しています。平常時の板は ADV から想像するより薄く、
     > 表示している投入上限は平均的な日を前提にした値です。

**やってはいけないこと**

- 既存の `adv_usd` / `max_position_usd` の**計算式を変えること**。
  `evaluate_gates` の流動性下限と `config/portfolio.yaml` のサイジングが依存しており、
  変えるとゲート通過銘柄が動く(=順位が動く)。**追加表示のみ**

**テスト**

- [ ] 出来高が全日同じ系列で 平均 == 中央値
- [ ] 出来高が1日に集中している系列で 中央値 << 平均
- [ ] `volume` が全て `None` の銘柄で例外にならず全フィールド `None`
- [ ] `adv_usd == 0` で `days_to_build` がゼロ除算せず `None`

---

### L-11. 取得単価基準の残余必要倍率(保有中)

**なぜ必要か。** 「7年で10倍」は**今日の株価**基準で毎日引き直される。
取得単価 $10・現在 $25 の銘柄について、画面は `achieved_moic 2.50x` と
`P(目標達成)` を並べて出すが、この `P` は**今日の $25 から10倍**の確率である。
投資家にとっての残りは 4倍であり、10倍ではない。

逆に −60% の銘柄は、当初の目標まで残り 25 倍を要求されている。
これは事実上テーゼが壊れている状態だが、画面上は `P(目標達成)` が
他の銘柄と同じ体裁で並ぶ。**「買値から見た残り」を出さないと、
含み損の銘柄ほど判断が甘くなる。**

**実装**

1. `PositionView` に追加(計算のみ、新テーブル不要):

   ```python
   remaining_moic_to_target: float | None   # target_moic / achieved_moic
   remaining_years: float | None            # opened_on + horizon_years - today
   required_cagr_from_here: float | None    # remaining_moic ** (1/remaining_years) - 1
   required_cagr_at_entry: float | None     # 取得時点の必要CAGR(target の required_cagr)
   ```

2. フロント:`PositionsPage.tsx` の「達成倍率」列の隣に
   「残り」列(`4.0x / あと 5.2年 / 年率 33%`)を追加。
   `required_cagr_from_here` が `required_cagr_at_entry` の **1.5 倍を超えた**ときに
   注記を出す(バッジは付けるが**警告色にしない**)。

3. **必ず画面に出す一文**(`monitoring.yaml` 冒頭・`PositionsPage.tsx:44-47` と同じ立場):
   > これは売却シグナルではありません。当初の必要年率より大幅に高くなっているのは、
   > 「当初のテーゼを、いま同じ強さで信じられるか」を確認する合図です。

**テスト**

- [ ] `remaining_years <= 0`(期限到来)で `required_cagr_from_here` が `None`
- [ ] `achieved_moic >= target_moic`(目標達成済)で残余が 1.0 以下になり、負の年率を返さない
- [ ] `current_price` が `None` の銘柄で全フィールド `None`

---

### L-8. 保有銘柄どうしの実測相関

**なぜ必要か。** `PortfolioOutlook.asset_correlation` はバックテストから推定した
**全体一律の1つの値**である。「N銘柄買ってもN回の独立な試行にはならない」ことは
`RankingPage.tsx:185-194` が正しく説明している。しかし**どのペアが重複しているか**は
出ない。鉱山会社を3社持っていれば実質1銘柄だが、画面上は3銘柄として集計される。

これは机上の懸念ではない。`investment_theory_review_2026-08-30.md` は
**「素材・エネルギーが母集団の 11.6% しかないのに上位50の 40% を占めていた」**と
実測しており、しかも**この集中は解消していない**と明記している。
上位から素直に選ぶと、利用者は同じ賭けを重ねる。

**データ源**:`price_snapshots`。保有は最大30銘柄なので相関行列は一瞬で出る。

**実装**

1. `src/autoscreener/scoring/portfolio.py` に追加:

   ```python
   def pairwise_return_correlation(
       series_by_ticker: Mapping[str, Sequence[tuple[datetime.date, float]]],
       *, min_overlap: int = 120,
   ) -> dict[tuple[str, str], float]: ...
   ```

   - **日付で内部結合**してから相関を取る(L-1 と同じ理由)
   - 重なりが `min_overlap` 未満のペアは結果に含めない(0.0 を入れない)

2. API:`PositionsResponse` に
   `correlations: list[{a, b, correlation, overlap_days}]`(相関降順、上位10ペア)を追加。
   同じものをランキング画面の上位N銘柄にも出す
   (`CandidateListResponse.portfolio` の隣。**表示している50件ではなく上位20件**に限る)

3. フロント
   - `PositionsPage.tsx` の「ポートフォリオ集計」内、`asset_correlation` の説明の直後
   - 相関 0.6 以上のペアを列挙:
     > **実質的に同じ賭けになっている可能性のある組み合わせ**
     > AAA — BBB 相関 0.78(重なり 251 日)
   - 注記:
     > 相関は共通因子の代理でしかありません。同じ商品市況・同じ顧客・同じ規制という
     > 「なぜ連動するのか」までは分かりません。一次情報で確認してください。

**やってはいけないこと**

- 相関から `asset_correlation` を置き換えること。`portfolio_outcome` は
  バックテスト由来の ρ を使う設計で、そこを実測相関に差し替えると
  `probability_at_least_one` の意味が変わる。**並べて出すだけ**

**テスト**

- [ ] 完全に同じ系列2本で相関 1.0
- [ ] 重なり 119 日のペアが結果に含まれない
- [ ] 1銘柄のみのポートフォリオで空リストが返り例外にならない

---

### L-10. 顧客集中・ガイダンス・訴訟(実行 + 配線)

**なぜ必要か。** これは L-0 と同型の**沈黙の失敗**である。

`customer_concentration` テーブルは **0 行**。しかし
`screening/monitoring_metrics.py` はこのテーブルを参照して
`customer_concentration_disclosed_drop` を評価しており、
`research/TEMPLATE.md` はこの指標を **premortem の先行指標**として名指ししている。

つまり **「主要顧客の10%超開示が消える」というテーゼ崩壊条件は、
データが1行も無いために、永久に点灯しない。**
利用者の画面には「テーゼ点灯:なし」と出る。これは
「確認した結果、問題なし」ではなく「そもそも確認していない」である。
**この2つが画面上で区別できていない。**

`guidance`(0行)と `litigation_events`(0行)も同様で、
収集バッチ(`batch/collect_concentration.py` / `collect_guidance.py` /
`collect_litigation.py`)は実装済み、テーブルも作成済み、
**API にもUIにも配線が無い。**

**手順**

1. **運用**:`.env` に `EDGAR_USER_AGENT` を設定したうえで(L-0 と同じ前提)
   ```
   uv run python -m autoscreener.cli collect-concentration
   uv run python -m autoscreener.cli collect-guidance
   uv run python -m autoscreener.cli collect-litigation
   ```
   実行後に各テーブルの行数と、カバーできた銘柄数を記録する。
   **`collect-concentration` は 10-K 本文のパースなので取りこぼしが多い**
   (`screening/customer_concentration.py` の docstring がそう書いている)。
   カバー率を測り、README に実測値を書く。
2. **API**:`CandidateDetail` に
   `customer_concentration` / `guidance` / `litigation` を追加。
   いずれも **`None` = 未取得、空リスト = 取得したが該当なし** として厳密に区別する。
3. **フロント**:詳細画面に表示。とくに
   - 顧客集中:期末ごとの「10%超顧客の数と比率」の推移
   - 訴訟:証券集団訴訟の提訴日・原告・状態(`red_flags` とは別枠。判定ではなく事実)
4. **最重要**:`PositionsPage.tsx` の「テーゼ点灯」列を**3状態にする**。

   | 表示 | 意味 |
   |---|---|
   | 「なし」 | 指標を評価したうえで、点灯していない |
   | **「未評価」** | 指標の元データが無く、そもそも評価できていない |
   | 「点灯」 | 点灯している |

   現状の実装は前2者を「なし」に潰している。**これは投資判断における最も危険な種類のバグである**
   ——監視していると信じている項目が、実際には監視されていない。

**受け入れ基準**

- [ ] 3バッチが完走し、各テーブルの行数とカバー銘柄数が記録されている
- [ ] 「未評価」と「なし」が保有画面で視覚的に区別できる
- [ ] データが 0 行のままの指標について、画面が「未評価」と表示する
      (バッチが失敗しても、嘘の安心を出さない)

---

### L-9. ベンチマークの実績分布(参照線)

**なぜ必要か。** 「期待倍率 2.4x」は絶対値である。同じ7年間に IWM を持っていたら
何倍だったのかが無いと、**2.4x が上手いのか下手なのか判断できない。**
10倍株探索の機会費用はインデックスであり、これを画面に置かないと、
利用者は「インデックスに負ける期待値の銘柄」を上位から選び得る。

**データ源**:`tickers.is_benchmark` の4本(IWM 2000年〜、IWC 2005年〜、
IJR 2000年〜、SPY 1993年〜)。**既に入っている。**

**実装**

1. `src/autoscreener/backtest/` に、ベンチマークのローリング N 年 MOIC の
   分位点(P10/P25/P50/P75/P90)を計算する純関数を追加する
2. `GET /api/v1/benchmark/reference?horizon_years=7` として返す。
   **銘柄ごとの計算は不要**(全銘柄共通の参照線)。フロントで1回だけ取得してキャッシュする
3. フロント:「実現倍率の幅」(現 285-321行)の帯の**下に、同じ横軸で**
   ベンチマークの帯を重ねて描く

   ```
   この銘柄   P10 0.00x ──── P50 1.8x ──── P90 12.4x        │ 目標 10x
   IWM(過去の7年実績)  P10 0.9x ── P50 2.1x ── P90 3.4x
   ```

4. **必ず出す注記**:
   > 下の帯は IWM の**過去の実績分布**(2000年以降のローリング7年)であり、予測ではありません。
   > 上の帯はモデルの仮定です。**性質の違うものを並べています。**

**やってはいけないこと**

- 超過リターンを計算して1つの数字にすること。実測分布とモデル仮定の差を引き算すると、
  引き算の結果が「実測された超過リターン」だと読まれる

---

### L-12. 順位の保存(表示は後日)

**なぜ必要か。** `score_date` は現在 **4 日分しかない**(2026-08-23〜28)。
順位の安定性を表示しても、いま意味のある画面にはならない。

**しかし順位は後から遡って復元できない。**
過去の `scores` 行から今日の設定で順位を再計算しても、
それは「当時の順位」ではなく「今の設定で当時のデータを見た順位」である。

**したがって、いま着手すべきは表示ではなく保存だけである。**

**実装**

- `run-scoring` の最後に、既定目標(7年/10倍)での順位を
  各 `scores` 行の `factors` に `rank_default_target` として書く
  (**JSONB なのでマイグレーション不要**)
- 併せて `first_ranked_on`(その銘柄が初めて候補として順位付けされた日)を
  導出できるようにしておく
- 表示(順位の推移スパークライン・観測日数バッジ)は
  **30営業日分たまってから**次の巡回で実装する

**受け入れ基準**

- [ ] `run-scoring` 実行後、`scores.factors` に `rank_default_target` が入る
- [ ] `probability` が変更前後で全銘柄一致

---

### L-13. 期待倍率が何を含んでいないかの明示(文言のみ)

30.1.3 が税務・為替計算をスコープ外と決めているので、**計算は実装しない**。
ただし、**含んでいないものを明示していないこと自体は欠陥である。**

J-10 で円換算表示は入ったが、**期待倍率は USD のまま**である。
円建ての投資家にとって「7年で10倍(USD)」は、為替次第で円ベース 7倍にも 13倍にもなる。

**実装**:`TickerDetailPage.tsx` のスコアサマリ(現 224-281行)と
`RankingPage.tsx` の `model-notice` に1文を足す。

> 表示している倍率・確率はすべて**米ドル建て・税引前・取引コスト控除前**です。
> 円建ての手取りは、為替(現在 ○○円/ドル)と譲渡益課税、為替手数料の分だけ変わります。
> 往復コストの目安は詳細画面に出しています(税と為替はこのアプリでは計算しません)。

---

## 6. 検討したが、この巡回では実装しないもの

| 項目 | 理由 |
|---|---|
| **実測ボラでポジションサイズを自動調整** | サイジング規律の変更は KPI で測らずに入れてはならない。実測を見せるところまでが今回のスコープ(L-1) |
| **実測ボラを `log_moic_sigma` に反映** | σ を触ると 28.4 の縮小推定・28.8 の較正写像・バックテストKPIがすべて無効になる。**理論的には正しい方向なので、次の巡回で `compare-configs` を通して正式に測るべき論点として残す** |
| **入口バリュエーションの順位への反映** | `investment_theory_review_2026-08-30.md` §5 で実装・測定済み、1年バックテストで3KPIとも負のため**不採用**。蒸し返さない |
| **TAM・モート・ユニットエコノミクスの自動推定** | 原則1。定性判断は代行しない。ノートの自由記述欄と一次情報への導線が正しい設計 |
| **税務・為替の損益計算** | 30.1.3 でスコープ外。L-13 で「含んでいない」ことだけ明示する |
| **アナリスト目標株価の表示** | `targetMeanPrice` は 11,810 行あるが、**12か月の目標株価は7年で10倍という時間軸と無関係**で、並べると短期の値動きへ注意を逸らす。カバー人数(L-3 K3)だけを出す |
| **ニュース記事の取得・表示** | ライセンスとノイズ。8-K の時系列(L-5)が同じ役割をより信頼できる形で果たす |
| **順位推移の表示** | データが4日分しかない。保存だけ先に始める(L-12) |

---

## 7. 引き継ぎのための約束事

### 7.1 着手前に必ず読むもの

1. `outside_tenx_implementation_plan_2026-08-28.md` の **30.0.2**(コーディング規約)と **30.11**(進め方)
2. `investment_decision_gap_2026-08-29.md` の §4(J-1〜J-10 の実装方針。同じ流儀で書く)
3. `investment_theory_review_2026-08-30.md` の §0 と §5(**やってはいけない差し戻し**が書いてある)
4. 本文書の **§4.1**(全項目に共通する絶対の制約)

### 7.2 各項目の完了条件(共通)

- [ ] 純関数が `screening/` か `scoring/` にあり、`tests/unit/` にテストがある
- [ ] 欠損を 0 や 1.0 で埋めていない(27.20:「測れなかった」と「測った結果」を分ける)
- [ ] 通貨をまたぐ値は取引通貨に統一している(2026-08-26 の欠陥の再発防止)
- [ ] 日付をまたいだ断面のプールをしていない(D-4 の再発防止)
- [ ] **同一 score_date の `run-scoring` の `probability` が変更前後で全銘柄一致**
- [ ] `npm run build` と `npm run lint` が通る
- [ ] `README.md` に追加分を1段落で書いた

### 7.3 なぜマイグレーションが1件も無いのか(設計判断)

意図的である。理由は3つ。

1. **今回の不足はデータの不足ではなく、提示の不足だから。**
   §1 のとおり、必要なデータはすべて既にDBにある。新しいテーブルを作るのは、
   既にあるデータを出し切ってからでよい(原則4)。
2. **スキーマを増やすと、次の巡回の担当者が読む対象が増えるから。**
   `models.py` は既に 23 テーブルあり、うち 3 テーブル(`customer_concentration` /
   `guidance` / `litigation_events`)は **0 行のまま画面にも出ていない**。
   同じ状態を増やさない。
3. **新しい値を `scores.factors` に書かないため。**
   J-3 が分位を `factors` に入れた前例があるが、`factors` は
   **`run-scoring` が書き、モデルが読む器**である。実測リスク・ピア・
   ボラのような**モデルが読んではならない値**を同じ器に入れると、
   いずれ誰かが特徴量として使う。表示専用の値は API 計算に置く。
   **例外は L-12 の `rank_default_target` のみ**で、これは
   `run-scoring` の出力そのもの(順位)であり、器として正しい。

### 7.4 この文書自体の限界

- **L-1 の実測リスクは、順位付けの問題を何ひとつ解決しない。**
  L-0 のバックテスト再実行で v4 がベースラインを上回れないと判明した場合、
  「よく見える誤り」に実測ボラという正確な装飾が付くだけである。
  **L-0 より先に来るものは無い。**
- 本文書は 2026-08-30 の DB 実測に基づく。行数・カバレッジは
  日次収集で変わるので、着手時に §1 の数え直しから始めること
  (数え直しのSQLは §1 の各行から復元できる)。
- 段階I〜Mの列挙は網羅を主張しない。とくに**空売り筋の主張の中身**、
  **サプライチェーンの集中**、**規制当局の審査状況**は、
  一次情報を人間が読む以外に方法がなく、器(ノートの自由記述)と
  導線(L-5 の提出書類、DDチェックリストの外部リンク)しか用意していない。
  これは原則1に照らして意図どおりである。
