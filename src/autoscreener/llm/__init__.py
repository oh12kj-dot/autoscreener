"""Claude API を使う定性分析レイヤー(K-9)。

**このパッケージの出力はゲートにもスコアにも入らない。** 根拠は
`outside_tenx_implementation_plan_2026-08-28.md` 第618行の原則1——再現性が無く
検証もできない判定をブロッキング条件にしてはならない。LLMは同じ入力でも
毎回同じ答えを返さないため、除外や順位づけの根拠に使った瞬間、バックテストが
再現できなくなる(過去のランキングを再計算しても当時と同じ値にならない)。

したがってここが生むものは、すべて `llm_analyses` 表に隔離し、用途を
**表示・投資ノートの下書き・人間の下読み**に限る。`tests/unit/
test_llm_advisory_isolation.py` が、`screening/` と `scoring/` がこの
パッケージを import していないことをテストとして固定している。
"""
