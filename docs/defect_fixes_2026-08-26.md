# 欠陥調査と修正の記録(2026-08-26)

既存の要件定義書(`10bagger_app_requirements.md`)・監査記録
(`model_audit_v4_2026-08-26.md`)の主張を**いったん脇に置き、コードそのものを
読んで**欠陥を洗い出した記録である。したがって本書の指摘の多くは、
「文書には直したと書いてあるが、コードでは効いていない」「文書がそもそも
触れていない」という種類のものになっている。

対象コミット時点の全テスト(288件)は**すべて緑だった**。以下の10件はいずれも
例外を投げず、ログにも出ず、静かに順位と表示を壊していた。

| # | 欠陥 | 深刻度 | 実データでの影響 |
|---|---|---|---|
| [D-1](#d-1) | 報告通貨と取引通貨を混ぜて EV を計算していた | **重大** | 262銘柄(5.0%)。AFYA が総合17位 → 83位、STNE が30位 → 118位 |
| [D-2](#d-2) | 粗利率フロアの「加点装置」化が、片方の経路にだけ残っていた | 高 | 前期粗利率が取れない薄利銘柄に無償の `margin_multiple` 加点 |
| [D-3](#d-3) | 「何年で何倍」の再計算で σ の縮小中心が7年のまま固定 | 高 | 「3年で3倍」の確率が中央値で **1.11倍**(最大2.24倍)過大 |
| [D-4](#d-4) | デシルを評価日をまたいでプールしていた | 高 | 主要KPIのリフト倍率が 1.32 → 1.46 に是正 |
| [D-5](#d-5) | S-7(観測1つの銘柄の成長率上限)が設定・コードの両方で無効 | 高 | BRUN が総合9位 → 18位 |
| [D-6](#d-6) | S-7 / S-8 のテストが構造的に失敗しえない書き方だった | 中 | 上の D-5 を2日間見逃していた原因 |
| [D-7](#d-7) | `apply_gates --date` が過去日を今日のデータで書き直していた | 中 | 2026-08-25 の再判定に 1,503銘柄分の先読みが混入しうる状態 |
| [D-8](#d-8) | 14.10の前日比急変検知がどこからも呼ばれていなかった | 低 | 単位変更・yfinance側不具合の早期検知が丸ごと不在 |
| [D-9](#d-9) | 分割調整済みの日次株式数と、未調整の年次株式数を混ぜていた | **重大** | 134銘柄(2.5%)。RKT が希薄化 184%/年 → 15%/年 で誤除外から復帰 |
| [D-10](#d-10) | 株式分割の遡及調整が、一度取りこぼすと永久に適用されない | 高 | FLOC の株式数系列が 22M → 93M → 42M と単位混在のまま残存 |

修正後の全テスト:**309件すべて緑**(回帰テスト21件を追加)。

---

## 10件のうち4件に共通する構造

D-5・D-6・D-7・D-8 は同じ形をしている。**「実装した」「設定した」「テストした」の
いずれかが欠けているのに、残りが揃っているせいで完成しているように見える**。

- D-5:コードは書かれ、docstringも書かれ、テストもある。**設定ファイルに値が
  無い**ので既定値(=上限として機能しない値)で走っていた
- D-7:`raw_snapshots.available_from` という列が「14.3 先読みバイアス対策」という
  コメント付きで存在し、収集時に正しく書き込まれている。**読む側のクエリが
  1つも無かった**
- D-8:関数もテストも揃っている。**呼び出し側が無かった**

このプロジェクトは同じ形の欠陥を過去に3回発見している(`min_listed_quarters`
が未参照だった件・`is_valid` が未参照だった件・`Ticker.listed_date` が未使用
だった件、いずれも24.6)。**「宣言されているが配線されていない」を機械的に
検出する手段が無い**ことが根本原因であり、その対策は末尾の[残課題](#残課題)に
書いた。

### もう1つの系統:単位の違う数を足している

D-1・D-9・D-10 は別の形で共通している。**同じ名前の量が、経路によって違う単位で
入ってくる**。

| 量 | 経路A | 経路B |
|---|---|---|
| 金額 | 株価・時価総額 = 取引通貨 | 決算数値 = 報告通貨(D-1) |
| 株式数 | `price_snapshots` = 分割調整済み | 貸借対照表 = 報告値のまま(D-9) |
| 株価 | 取得しなおした値 = 分割調整済み | 保存済みの行 = 当時の単位(D-10) |

いずれも `float` なので型では止まらず、値も桁違いにはならないことが多いので
目視でも気づけない。**yfinance から来る量には「単位」というもう1つの次元があり、
それを持ち回っていない**ことが根本にある。ゲート側(`apply_gates`)は D-1 の
換算を持っていたのにモデル側が持っていなかった、という非対称も同じ原因である。

---

<a id="d-1"></a>
## D-1【重大】報告通貨と取引通貨を混ぜて EV を計算していた

### 何が起きていたか

13.5 に記録があるとおり、yfinance は ADR 等で **株価・時価総額は取引通貨
(`currency`)、決算数値は報告通貨(`financialCurrency`)** という混在した単位で
値を返す。除外ゲート側(`batch/apply_gates.py`)はこれを
`normalize_financial_currency_value` で換算していたが、**MOICモデルの入力を
組み立てる `scoring/point_in_time.py` には同じ処理が無かった**。

その結果、次の式が単位の違う数を素で足し引きしていた。

```
enterprise_value = market_cap(取引通貨) + net_debt(報告通貨)
current_multiple = enterprise_value / gross_profit(報告通貨)
terminal_equity  = terminal_ev(混在) − net_debt(報告通貨)
leverage_effect  = equity_moic / ev_moic     ← 上の帰結として全部歪む
```

`min_gross_profit_usd`(USD建ての足切り)も報告通貨の値と比較されていた。

### 実データでの規模

```
最新 raw_snapshot 5,287銘柄中、currency ≠ financialCurrency … 262銘柄(5.0%)
うち換算レート(_fx_rate_financial_to_trading)が取得できているもの … 262銘柄(100%)
```

2026-08-25 のランキングでは **AFYA(BRL報告、fx=0.194)が総合17位、STNE が30位**に
入っていた。BRL建てのネットデットが USD 時価総額に素で足されており、AFYA の
ネットデットは実質 **5.2倍** に膨らんでいた。

### 修正

`scoring/point_in_time.py` に `financial_to_trading_rate()` を追加し、
`build_moic_inputs` で **金額そのものを持つ4項目だけ**を取引通貨へ換算する。

- 換算する:`revenue_latest` / `gross_profit_latest` / `net_debt` / `lease_liability`
- 換算しない:`market_cap`(もとから取引通貨)
- 換算不要:成長率・粗利率・希薄化・FCFマージン・自己資本比率・ランウェイ
  (すべて同一通貨どうしの比なので通貨に依存しない)

**換算レートが取れない場合は `None` を返して「測れない」扱いにする。**
ゲート側の「換算不能は判定不能」方針と揃えた。誤った単位で計算した順位を出す
より、Tier 2(監視リスト)へ回すほうが正しい。

同じ関数をバックテストも通るため、`_passes_point_in_time_gate` の売上高上限
判定も同時に正される。

### 効果(2026-08-25 の実データ)

| 銘柄 | 修正前 順位 | 修正後 順位 | 期待倍率 | レバレッジ倍率 |
|---|---|---|---|---|
| AFYA | 17 | **83** | 2.487 → 1.823 | 1.495 → 1.096 |
| STNE | 30 | **118** | 2.352 → 1.746 | 1.469 → 1.091 |
| PDS  | — | 155 | 1.651 → 1.597 | 1.132 → 1.096 |

擬似バックテストの集計KPIはほぼ動かない(観測 3,080 → 3,109、リフト
1.47 → 1.46、rank_IC +0.154 → +0.152)。**5%の銘柄の誤りは、母集団平均には
現れない**。これは「KPIが良ければモデルは正しい」という読み方への反例であり、
個別銘柄の逆解析を続けるべき理由でもある。

### 限界(正直に記録する)

`_fx_rate_financial_to_trading` は**収集時点のスポットレート**であり、過去に
遡れない。バックテストでは全評価日に現在のレートを当てることになる。
リステートメントの先読み(`point_in_time.py` の limitation 1)と同種の近似で
あり、為替が大きく動いた通貨では過去時点の EV がずれる。

---

<a id="d-2"></a>
## D-2【高】粗利率フロアの「加点装置」化が、片方の経路にだけ残っていた

`model_audit_v4_2026-08-26.md` の S-1 は、`margin.floor`(5%)が
「外挿の暴走を防ぐ下限」のはずが、粗利率が floor を下回る銘柄では**外挿結果を
上から押し上げる加点装置**になっていた問題を修正したと記録している
(AMR:粗利率 11.2% → 1.2% の崩壊が `margin_multiple` 4.29倍の「改善」として
計上されていた)。

しかしコードを読むと、**前期の粗利率が取れない銘柄が通る early return には
修正が入っていなかった**。

```python
if inputs.gross_margin_prior is not None:
    annual_trend = ...
else:
    return _clamp(current, margin.floor, margin.ceiling)   # ← ここが未修理
...
lower_bound = min(current, margin.floor)                    # ← S-1 の修正はこちらだけ
terminal = _clamp(current + total_change, lower_bound, margin.ceiling)
```

粗利率1.2%・前期データなしの銘柄は、トレンドが測れないというだけで終端粗利率
5.0%(=floor)へ持ち上げられ、`margin_multiple` 4.3倍を無償で受け取っていた。
S-1 が塞いだはずの穴が、もう一方の経路にそのまま残っていた。

**修正**:`lower_bound` を分岐の前で決め、両方の経路が同じ下限を使うようにした。
回帰テスト `test_margin_floor_does_not_lift_a_margin_without_a_prior_period` を
追加。

---

<a id="d-3"></a>
## D-3【高】「何年で何倍」の再計算で σ の縮小中心が7年のまま固定されていた

### 何が起きていたか

27.24 の目標変更機能(「3年で3倍」など)は、保存済みの入力から**その年数で
計算し直す**。READMEも「保存済みの入力からその年数で計算し直して並べ替えます」
「厳密に再計算」と書いている。

ところが σ の縮小推定(28.4)は、σ を**断面の幾何平均へ85%寄せる**。その中心
`median_log_sigma` は**スコアリング時のホライズン(既定7年)で測った値**であり、
`scores.inputs.cross_section` にそのまま保存されている。σ はホライズンとともに
伸びるのに、中心だけが7年のまま据え置かれていたため、**短いホライズンでは σ が
中心へ引き上げられていた**。

対数正規の閾値超過確率は σ に極めて敏感なので、これはそのまま表示確率の誤差になる。

### 実データでの規模(2026-08-25、367銘柄を「3年で3倍」で再計算)

```
σ の中央値            修正前 0.705  →  修正後 0.669
P(3年で3倍) の修正前/修正後の比   中央値 1.106   平均 1.165   最大 2.243
```

つまり「3年で3倍」の確率は**中央値で11%、最悪の銘柄では2.2倍に過大表示**されて
いた。合成ユニバースでの検算では、断面ごと組み直した厳密解 4.76% に対し
修正前は 5.70%、修正後は 4.62% になる。

### 修正

`CrossSection` に **測定時のホライズン(`horizon_years`)** を持たせて保存し、
`compute_moic` が現在のホライズンと違う場合に中心を引き直す
(`sigma_center_horizon_scale`)。

厳密には断面全体をリクエストごとに組み直すのが正しいが、詳細画面は1銘柄しか
読まない。代わりに「中心はこの銘柄と同じ比率でホライズンに反応する」と仮定して
倍率を掛ける。σ のホライズン依存は成長率不確実性の伝播係数
`Σ fade^t/(1+g_t)` にほぼ集約されており、レバレッジ・脆弱性・マルチプル等の
定数項は銘柄内で共通に効くため、近似の誤差は小さい(検算で σ 0.667 の厳密解に
対し本手法は 0.666)。

`horizon_years` を持たない古い保存行(0=不明)は**引き直さない**。分からない
ものを推測して動かすより、従来どおりの値を返すほうが安全である。

---

<a id="d-4"></a>
## D-4【高】デシルを評価日をまたいでプールしていた

### 何が起きていたか

`backtest/metrics.py` の `compute_metrics` は、**全評価日の観測を1つのプールに
してから**確率の降順に並べ、10等分していた。

モデルが出す確率の**水準**は評価日ごとに動く(σ の縮小中心もナウキャストの
基準線も、その日のユニバースから作られるため)。したがってプールしたまま切ると、
上位デシルが「モデルが有望と見た銘柄」ではなく「**たまたま確率の水準が高かった
評価日の銘柄**」で占められる。

実データの評価日ごとのユニバース・オンペース率は **16.0% 〜 35.1%** と2倍以上
ばらついており、この混入は無視できない。14.2 が定めるデシル単調性・リフト倍率は
「**同じ日に並んだ銘柄の中で**上位ほどリターンが高いか」を測る指標であり、
プール方式はその定義から外れている。

### 修正

評価日ごとにデシルへ切ってから、同じデシル番号どうしを束ねる
(`_cross_sectional_buckets`)。断面資産価格の標準的な手順でもある。

### 効果(同一の観測群でKPIだけ組み替えた比較)

| 指標 | プール方式(旧) | 評価日ごと(新) |
|---|---|---|
| デシル単調性 | +0.842 | **+0.806** |
| リフト倍率 | 1.32 | **1.46** |
| 最悪評価日のリフト | 1.03 | 1.03 |
| rank_IC | +0.152 | +0.152 |
| 右裾5%のリフト | 2.18 | 2.18 |

**単調性は下がり、リフトは上がる。どちらも是正であって改悪ではない。**
プール方式は日をまたいだ構成の偏りでリフトを押し下げ、単調性を押し上げていた。
なお評価日ごとのリフトの平均は 1.48 であり、新しいプール値 1.46 はこれと整合する
(旧値 1.32 は整合しなかった)。

---

<a id="d-5"></a>
## D-5【高】S-7(観測1つの銘柄の成長率上限)が設定・コードの両方で無効だった

### 何が起きていたか

`model_audit_v4_2026-08-26.md` の S-7 は「CAGR・YoY のどちらか一方しか無い銘柄には
より保守的な上限 `max_initial_rate_single_observation` を適用する」と記録し、
`moic.py` の docstring も同じ主張をしている。**どちらも事実ではなかった。**

1. `config/scoring.yaml` に `max_initial_rate_single_observation` が**書かれて
   いない**。`config.py` の既定は `1.0` なので
   `min(max_initial_rate=0.60, 1.0) = 0.60` となり、通常の上限と同じになる
2. 仮に設定しても、**価格ナウキャスト(`nowcast_initial_growth`)は素の
   `max_initial_rate` で丸めていた**ので、補正を経由すれば保守的な上限を
   超えて戻れた(片側からの迂回路)

S-7 が動機として挙げていた **BRUN(上場直後で3年CAGRが無い型)は、2026-08-25 の
ランキングで総合9位に付いていた**。安全装置は一度も作動していなかった。

### 修正

- `initial_growth_ceiling()` を公開関数に切り出し、`base_initial_growth` /
  `nowcast_initial_growth` / `is_initial_growth_clamped` の**3箇所すべてが同じ
  上限を参照する**ようにした(迂回路を塞ぐ)
- `config/scoring.yaml` に `max_initial_rate_single_observation: 0.45` を明記

### 値の選定(擬似バックテスト・1年ホライズン・8評価日)

| 値 | 単調性 | リフト | 最悪日リフト | 右裾5%の最悪日 | 上位デシル破綻率 |
|---|---|---|---|---|---|
| 1.00(無効) | +0.806 | 1.46 | 1.03 | 1.22 | 10.5% |
| **0.45** | +0.806 | **1.51** | 1.03 | 1.22 | **10.2%** |
| 0.35 | +0.879 | 1.51 | **0.83** | **0.82** | 9.3% |
| 0.25 | +0.915 | 1.45 | **0.83** | 0.97 | 7.7% |

0.35 以下は単調性と引き換えに **最悪評価日のリフトが1を割る**(28.16 で解消済み
だった弱点の再発)。**どのKPIも悪化させない 0.45 を採る。**

`config/scoring.yaml` 冒頭の分類では「(c) 擬似バックテストで選んだ値」に当たる。
独立な観測期間が実質3つしかない標本で選んでいるため、弱くしか特定されていない。

### 効果(2026-08-25 の実データ)

**BRUN:総合9位 → 18位**(初期成長率 0.600 → 0.450、期待倍率 3.11 → 2.53)。

---

<a id="d-6"></a>
## D-6【中】S-7 / S-8 のテストが構造的に失敗しえない書き方だった

D-5 を2日間見逃していた直接の原因。

```python
# S-7 のテスト(修正前)
assert single <= config.growth.max_initial_rate_single_observation + 1e-9
# → 設定値が既定 1.0 のままなら、上限が機能していなくても常に成立する

# S-8 のテスト(修正前)
assert delta <= config.growth.nowcast_cap_sign_flip + 1e-9
# → 本番設定は 1.0(意図的に無効)なので、機構の有無に関わらず常に成立する
```

どちらも「**設定値そのものを閾値にしたアサーション**」であり、設定が機能しない
値になっている限り、機構が存在しなくても緑になる。

### 修正

- 挙動を検証するテストは、**設定値を明示的に差し替えて**「上限が効いていること」
  「効いていない場合との差」を比較する
- 本番設定が S-7 を有効にしていることは、
  `test_shipped_config_actually_enables_the_single_observation_ceiling`
  という**別のテスト**で見る(挙動の検証と設定の検証を分ける)
- S-8 は本番設定で意図的に無効(実測でKPIを改善しなかったため)。テストは
  `nowcast_cap_sign_flip=0.05` を明示して**機構そのもの**を検証する形に変え、
  無効であることは yaml のコメントで明示する

---

<a id="d-7"></a>
## D-7【中】`apply_gates --date` が過去日を今日のデータで書き直していた

CLI は `apply-gates --date` / `run-scoring --date` を「過去日を再計算できる」と
案内している。しかし `apply_gates` は日付に関係なく

- **最新の** `raw_snapshot`
- `price_snapshots` の**全行**の末尾90行

を読んでいた。つまり過去日の `universe_snapshots` を**今日のデータで**書き直して
いた。スコアリング側は `build_moic_inputs(as_of=...)` で厳密に切っているため、
同じ日付の「ゲート判定」と「スコア」が別の時点の情報で作られる不整合にもなる。

`raw_snapshots.available_from` は **`# 14.3: 先読みバイアス対策`** というコメント
付きで存在し、収集時に正しく書き込まれているのに、**読むクエリが1つも無かった**。

### 実データでの規模

2026-08-25 の時点で、`available_from > 2026-08-25` の raw_snapshot を持つ銘柄が
**1,503件**あった。旧コードで `apply-gates --date 2026-08-25` を再実行すると、
この1,503件分の先読みが混入する。

### 修正

- `apply_gates._gather_gate_input(session, ticker, snapshot_date)` が
  `available_from <= snapshot_date` と `trade_date <= snapshot_date` で絞る
- `scoring/engine._load_scoring_universe` も同様に絞る(価格は
  `build_moic_inputs` が二重に切るが、SQL段で落としたほうが速く、意図も明確)

DB を使う回帰テスト `tests/unit/test_apply_gates_point_in_time.py` を追加。

---

<a id="d-8"></a>
## D-8【低】14.10 の前日比急変検知がどこからも呼ばれていなかった

`validation/rules.detect_day_over_day_spike` は実装もテストもされているが、
**本番コードからの呼び出しが1つも無い**。14.10 が「単位変更・yfinance側の不具合の
早期検知」として定めた仕組みが、丸ごと存在しないのと同じ状態だった。

### 修正

`collectors/snapshot_collector.collect_one` から呼び、直前のスナップショットと
`totalRevenue` / `totalCash` / `sharesOutstanding` / `marketCap` を比較する。

**検知しても `is_valid` は動かさない。** 急変は「異常の疑い」であって「値が
間違っている証明」ではなく、決算期をまたいだ正常な急変(赤字転換・大型買収)も
ある。Altman Z'' をハードゲートにしなかった教訓(`exclusion_gates.py` の
モジュールdocstring)と同じ失敗を繰り返さないため、**ログと
`collection_logs.detail.day_over_day_spikes` に残して運用者の目に触れさせる
ところまで**を仕様とする。

---

<a id="d-9"></a>
## D-9【重大】分割調整済みの日次株式数と、未調整の年次株式数を混ぜていた

### 何が起きていたか

`price_snapshots.shares_outstanding` は 13.4 の分割調整を通って**現在の株式単位**に
揃えてある。一方、貸借対照表の `Ordinary Shares Number` は yfinance が返す
**報告値そのまま**であり、分割・併合を挟むと単位が変わる。

`exclusion_gates.dilution_cagr` と `point_in_time.build_moic_inputs` は、この2系列を
**無条件に混ぜていた**。

- `dilution_cagr` は「実測窓が長いほうを採る」(27.9)。年次系列のほうが長いので、
  **単位が壊れているほうが優先的に選ばれる**
- `build_moic_inputs` の `combined_shares` は両系列を日付順に並べ、最後の観測を
  時価総額に使う。バックテストの過去時点では年次側が最後になりうる

### 実データでの規模

同一銘柄内で「日次 ÷ 年次」の比が期をまたいで**2倍以上**動く銘柄が
**134件(2.5%)**。実際の増資は両系列に同じように効くので比は動かない——
比が動くのは片方だけが調整されているときに限られる。

希薄化CAGRの値が実際に変わったのは **57銘柄**。

| 変化 | 件数 | 例 |
|---|---|---|
| `dilution_ceiling`(+25%)で誤除外されていた → 復帰 | 11 | **RKT** 184%/年 → 15%/年、DCX 1536%/年 → 測定不能 |
| 逆に、自社株買い扱いの加点を受けていた → 正しく除外 | 9 | FLYE −30%/年 → +133%/年、LGCL −67%/年 → +244%/年 |
| 「測れない」に変わった(A-1の断面中央値で中立補完) | 30 | — |

株式併合を挟んだ銘柄では年次系列が「株式数が99%減った」ように見え、
希薄化CAGRが**自社株買い扱い(下限 −5%)の無償加点**になる。逆方向の分割では
「年率+700%の希薄化」となり、**正常な銘柄が除外される**。RKT(Rocket Companies)が
後者の実例だった。

### 修正

`annual_share_series_is_comparable()` を `exclusion_gates.py` に追加し、
**年次の各期末に最も近い日次観測(±20日)との比**を取る。比が期をまたいで
2倍以上動いたら単位が揃っていないと判定し、**年次系列を使わない**
(`dilution_cagr` からも `combined_shares` からも外す)。

分割倍率そのものは payload に無いため、ここで復元することはできない。
**直すのではなく、使わない。** 年次を外した結果、日次の窓が2年に満たなければ
希薄化は「測れない」となり、A-1 の断面中央値による中立補完と
`希薄化データ欠損` バッジに回る。誤った希薄化率を使うより、測れないと認める
ほうが正しい(27.1の欠損方針と同じ)。

### 閾値の較正で1度失敗している(記録しておく)

最初は許容幅を **1.25倍** にした。その結果、急速に希薄化している FLOC
(2025-01の22.0M株 → 2026-08の44.0M株)が「期末と観測日のあいだの実際の増資」
だけで比が1.38まで動き、**年率+9.8%の希薄化が消えて総合4位まで浮上した**——
検知装置が、まさに検知したかったものを消していた。

実データの分布(5,287銘柄、±20日窓)は 1.1超が547件、1.5超が210件、2.0超が
134件、3.0超が95件と連続的で、1.5以下の領域には実際の増資が混ざる。
**分割・併合の倍率は実務上どんなに小さくても2倍**(1:2併合 / 2:1分割)であり、
それより細かい比率は存在しない。一方、期末から20日以内の観測との比が銘柄内で
2倍動くほどの増資は、同じ20日窓の中で2回、しかも逆向きに起きる必要があり
現実的でない。**2.0 を採る。**

---

<a id="d-10"></a>
## D-10【高】株式分割の遡及調整が、一度取りこぼすと永久に適用されない

`snapshot_collector._reconcile_splits` は「**分割日以降の行がまだ1件も無いこと**」を
条件に遡及調整していた。日次収集が分割当日に、Yahoo が分割を反映する**前**に
走って分割前価格の行を書いた場合、翌日以降は
`already_have_post_split_rows` が真になり、**分割前の行は永久に調整されない**。

D-9 の調査中に FLOC がこの状態であることが分かった。発行済株式数が
22M(2025-01) → 93M(2025-12) → 42M(2026-05) と単位の混ざった系列で残っていた。

### 修正

行の有無ではなく**値そのもの**で判定する。`history()` が返す終値は常に現在単位
に分割調整済みなので、**同じ取引日の保存値と取得値を比べれば**保存側が調整済み
かどうかが分かる。

- 比が 1.0 ± 2% → 調整済み。何もしない
- 比が分割倍率 ± 2% → 未調整。遡って適用する
- 突き合わせる取引日が無い → 従来どおり「分割日以降の行の有無」で判断

あわせて `fetch_latest_price` の取得窓を `5d` → `1mo` に広げた。5日窓では分割から
数日以内に収集が走らないと突き合わせる日が無くなる。APIコール数は変わらない。

---

## 修正後の擬似バックテスト(2026-08-26、1年ホライズン・8評価日・3,107観測)

```
デシル単調性(主指標)  +0.806      ← D-4 の是正により旧表示 +0.830〜+0.842 から変更
リフト倍率              1.51       最悪の評価日 1.03
順位IC                 +0.152      t = +3.48(評価日を独立とみなした上限値)
破綻回避               上位デシル 10.4% vs ユニバース 10.7%
較正誤差               予測 20.3% vs 実測 24.8%(−4.5pt)
ナウキャスト上限張り付き率  22.1%

右裾リフト(28.11)   閾値リターン   上位10%の的中   リフト   最悪の評価日
  断面上位 25%          +39.4%          38.1%      1.52        0.90
  断面上位 10%          +90.2%          18.7%      1.87        0.81
  断面上位  5%         +127.4%          11.0%      2.19        1.22
  断面上位  2%         +217.7%           6.1%      3.03        0.81
```

**14.2 の目標(右裾リフト 2.0 以上)は上位5%・上位2%の事象で達成している。**
一方で上位10%・上位2%は最悪の評価日でリフトが1を割る。「常に効くモデルでは
ない」という README の但し書きは修正後も有効である。

上場廃止として決済された観測は **0.00%**。`tickers` は NASDAQ Trader の
**現在の**銘柄一覧を出典とするため、期間中に廃止された銘柄はマスタに存在せず、
バックテストの母集団から丸ごと欠落している(27.15)。実測のリターン・
オンペース率は実態より良い方へ偏っている。**この生存バイアスは今回の修正では
解消していない**(下記[残課題](#残課題))。

---

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `src/autoscreener/scoring/point_in_time.py` | D-1:`financial_to_trading_rate` 追加、金額4項目を取引通貨へ換算 / D-9:単位が揃わない年次株式数を混ぜない |
| `src/autoscreener/scoring/moic.py` | D-2:粗利率フロアの下限を分岐前に統一 / D-3:`CrossSection.horizon_years` と `sigma_center_horizon_scale` / D-5:`initial_growth_ceiling` を3箇所で共有 |
| `src/autoscreener/backtest/metrics.py` | D-4:`_cross_sectional_buckets`(評価日ごとのデシル) |
| `src/autoscreener/batch/apply_gates.py` | D-7:`available_from` / `trade_date` で判定日以前に絞る |
| `src/autoscreener/scoring/engine.py` | D-7:同上 |
| `src/autoscreener/screening/exclusion_gates.py` | D-9:`annual_share_series_is_comparable`(年次株式数の単位検証) |
| `src/autoscreener/collectors/snapshot_collector.py` | D-8:`_detect_spikes` を配線 / D-10:分割の遡及調整を値ベース判定に |
| `src/autoscreener/collectors/yfinance_client.py` | D-10:取得窓を `5d` → `1mo`、`recent_closes` を返す |
| `config/scoring.yaml` | D-5:`max_initial_rate_single_observation: 0.45`(選定根拠つき) |
| `tests/unit/test_moic.py` | D-2/D-3/D-5/D-6 の回帰テスト、空虚なアサーションの置き換え |
| `tests/unit/test_point_in_time.py` | D-1 の回帰テスト |
| `tests/unit/test_backtest_metrics.py` | D-4 の回帰テスト |
| `tests/unit/test_snapshot_collector.py` | D-8 / D-10 の回帰テスト |
| `tests/unit/test_exclusion_gates.py` | D-9 の回帰テスト |
| `tests/unit/test_apply_gates_point_in_time.py` | D-7 の回帰テスト(新規、DB必要) |

`config/scoring.yaml` を変えたため `config_hash` が
`e2f7ff9ef0b4bdf5` → `7a3c3ed29a1d2073` に変わった。較正写像は設定の完全一致を
要求するので(`engine.load_calibration_map`)、**`run-backtest` を実行するまで
スコアは未較正のまま保存される**。本修正では実行済みで、2026-08-25 のスコアも
再生成してある。

### 適用手順(別環境で追随する場合)

```bash
uv run pytest                                            # 309件緑を確認
uv run python -m autoscreener.cli run-backtest           # 較正写像を新しい config_hash で作り直す
uv run python -m autoscreener.cli apply-gates  --date <最新のスコア日>
uv run python -m autoscreener.cli run-scoring   --date <最新のスコア日>
```

D-10 は**今後の取りこぼしを防ぐ**修正なので、既に単位が混ざっている
`price_snapshots` の行は直らない。該当銘柄が分かっている場合は個別に
バックフィルすると収束する(`_backfill_one` は既存行を上書きする)。

```bash
uv run python -m autoscreener.cli backfill-history --symbols FLOC,XXXX
```

---

## 残課題

今回**手を付けなかった**もの。いずれも「気づいたが直していない」ことを明示する。

### R-1【重大】バックテストの生存バイアス

`tickers` は現在の銘柄一覧を出典とするため、評価期間中に上場廃止された銘柄が
母集団に存在しない。`_realized_return` は保有中に廃止された銘柄を最終観測値で
清算する仕組みを持つが、**そもそも標本に入っていない銘柄は救えない**。
実測の `delisted_settlement_rate` が 0.00% であることがその証拠であり、
CLI もこれを警告として出す。全KPIが実態より良い方へ偏っている。

対処には過去時点の銘柄一覧(NASDAQ Trader の日次アーカイブ等)が要る。

### R-2【中】較正写像が同一標本で学習・評価されている

`fit_calibration_from_observations` は、KPIを測ったのと**同じ観測群**で較正写像を
学習している。ホールドアウトも交差検証も無い。ビン数10・観測3,107 なので過学習は
軽微と見込めるが、`calibration_error` は楽観側に偏る。

### R-3【中】デュアルクラス銘柄では株式数の2系列が定常的に食い違う

D-9 の単位検証は「日次(合算)÷年次(1クラス)」の比が期をまたいで動くことを
根拠にする。デュアルクラス銘柄では、上場によって比が 1.0 → 3.2 のように
**一度だけ**動くため、分割と区別がつかない。

実データで FLOC(Flowco Holdings、2025年1月上場)がこれに当たる。
`Ordinary Shares Number` が Class A のみの29.1M株、`get_shares_full` が
合算の93.1M株を返しており、比が3.2まで動く。D-9 の検証はこれを単位不一致と
判定し、希薄化を「測れない」に落とす(A-1 の断面中央値で中立補完され、
UIには `希薄化データ欠損` のバッジが出る)。

**これは誤検知ではない**——2系列が実際に別のものを数えている以上、混ぜて
CAGRを取るのは間違いであり、「測れない」は正しい。ただし年次系列だけを見れば
年率+9.8%の希薄化が読み取れるので、**情報を捨てている**のも事実である。
クラス別の株式数を取れる経路があれば解消できる。

### R-4【中】既に壊れている `price_snapshots` の行は自動では直らない

D-10 は**今後の取りこぼしを防ぐ**修正であり、過去に取りこぼした結果として
既に単位が混ざっている行は直さない。`backfill-history --symbols <銘柄>` を
実行すれば取得しなおした値で上書きされ収束する(`_backfill_one` は既存行を
上書きする実装になっている)。**どの銘柄が壊れているかを検出する仕組みは
まだ無い。** `annual_share_series_is_comparable` が False を返す銘柄の一覧を
定期的に出すのが最短の入口になる。

### R-5【低】配当を無視している

`history(auto_adjust=False)` の終値は分割調整済み・**配当未調整**である。
実現リターンは価格リターンであり、配当を出す銘柄では過小評価になる。
小型グロース中心のユニバースでは影響は小さいが、KPIの定義としては明示すべき。

### R-6【低】「宣言されているが配線されていない」の自動検出

本書の D-5・D-7・D-8 と、過去の24.6の3件は同じ形の欠陥である。
設定キー・DB列・純粋関数のうち「書き込まれるだけで読まれないもの」を
CIで検出する仕掛けがあれば、この系統は構造的に止められる。
