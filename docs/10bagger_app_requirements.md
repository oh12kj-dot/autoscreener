# 10バガー候補発掘アプリ 要件定義書

version 2.1(新規アプリ版) / 2026-08-24

> **v1.1 改訂メモ**:yfinance 1.4.1 の実データ検証にもとづき、13〜17章を追記。
> 特に **13章はv1.0の前提を一部否定する実測結果**を含むため、7章の重み調整より先に読むこと。
>
> **v1.2 改訂メモ**:「商用利用可能なレベルのクオリティ」の要求を受け、想定スコープを明確化した上で(11章)、例外処理・数値ロジックの堅牢化を18章として追加。**「商用品質(エンジニアリングの堅牢性)」と「商用利用(データライセンス・金融規制上の許諾)」は別軸**であり、本バージョンは前者のみを対象とする(後者は11章参照)。
>
> **v1.3 改訂メモ**:フェーズ1(データ収集基盤)実装後、NASDAQ/NYSE候補5,312銘柄からの無作為抽出370銘柄の実データを用いて4章・15.2の閾値を検証し、19章として追記。1件のセクター除外漏れ(REIT)と1件のデータ単位の罠(`debtToEquity`)を発見し修正済み。
>
> **v1.4 改訂メモ**:フェーズ2(除外ゲート)実装・実データ適用の過程で、15.2のAltman Z''ハードゲートが赤字グロース株(バイオテック等)を系統的に誤除外することが判明し、設計を変更した(19.8)。ゲート適用対象は34銘柄→48銘柄(+41%)に増加。
>
> **v1.5 改訂メモ**:フェーズ3(スコアリングエンジン)を実装し、NASDAQ/NYSE候補5,312銘柄全件収集・全ゲート適用・全スコアリングを実施した(20章)。ゲート通過768銘柄は4章の目標レンジ(米国800〜1,500銘柄)に整合。実装過程でローカルタイムゾーンとUTCの不整合バグを発見・修正(20.3)。
>
> **v1.6 改訂メモ**:日次自動実行(Windows Task Scheduler)とFastAPI(6.5の4エンドポイント)を実装し、実データで動作確認した(21章)。
>
> **v1.7 改訂メモ**:前方検証ジョブ(14.3)、運用移行チェックリストの一部(DBバックアップ・APIの読み取り専用ロール分離・18.7監視閾値のログ実装)を実装した(22章)。
>
> **v1.8 改訂メモ**:Reactフロントエンド(3章、6.4の3画面)を実装し、Playwrightで実際にブラウザ操作して動作確認した(23章)。これで17章の実装順序8までが完了。
>
> **v2.1 改訂メモ**:銘柄評価の**手法そのもの**が目的に対して妥当かを実データのスコア分布から検証し、**加重幾何平均が「弱点のない銘柄」を選ぶ結果、成長軸トップ20の総合順位が中央値182位/546銘柄になる**(=10バガーの入口である『突出した高成長・初期は割高』という profile が構造的にランキングに出てこない)ことを確認した。幾何平均は破綻回避として機能しているため変更せず、15.5が設計していた二層構成のTier 2(監視リスト)を実装して拾い直す方式を採った(26章)。
>
> **v2.0 改訂メモ**:計算ロジック・銘柄評価方法の妥当性をアプリ全体で通しレビューし、**評価結果を実際に歪めていた指標の誤りを5件・データ整合性の欠陥を3件・パイプライン/API間の不整合を3件**修正した(25章)。さらにSEPNの実データレビューから、サブスコア内の指標カバレッジが総合スコアに反映されていなかった設計上の穴と、TTM売上成長率の基数効果を追加で修正し(25.7)、上位10銘柄のサブスコア内訳を1指標ずつ検証して希薄化指標の欠測・アナリストカバレッジ欠損の誤解釈・EV倍率の分母の古さを修正した(25.8)。特に12-1モメンタムが定義どおりになっておらず短期反転の回避が逆に働いていた点、「安定して減収している銘柄」が成長の安定性で最上位になっていた点は、v1.9までのスコアの解釈に影響する。
>
> **v1.9 改訂メモ**:スコアリングエンジンをv2へ拡張し、20.1で見送っていた予想修正モメンタム・決算サプライズ連続性・インサイダー買い越し・12-1モメンタムを実装した(24章)。`earnings_dates`取得に`lxml`パッケージが必要だったこと、`insider_transactions`の`Transaction`列が常に空で`Text`列からの判定が必要だったことが実データで判明。

---

## 1. 目的・概要

yfinanceで取得可能な定量データのみを用いて、将来10倍株(10バガー)になる可能性が相対的に高い銘柄を定期的にスクリーニング・スコアリングし、ランキング形式で閲覧できる**独立したWebアプリケーション**を新規開発する。

**非目的**:個別銘柄の売買判断を断定すること、将来リターンを保証・予測すること。一次スクリーニングツールであり、最終判断は利用者が定性分析(事業内容、経営者、モート)を行った上で下す前提とする。

---

## 2. スコープ

### 2.1 対象範囲(このアプリで作るもの)
- データ収集バッチ(yfinanceからの定期取得・保存)
- スコアリングエンジン(生データ→サブスコア→総合スコア)
- ランキング表示・銘柄詳細表示のWeb UI
- スコア推移・順位変動の履歴管理
- 独自のバックエンドAPI・データベース・フロントエンド一式

### 2.2 対象外(将来検討・別プロジェクト)
- ニュース・IRテキストのLLM分析
- インサイダー取引・空売り比率の外部データ連携(EDGAR等)
- 自動売買・発注機能
- 他アプリ(既存の監視・通知アプリ等)との連携・データ共有
- プッシュ通知・アラート機能(初期リリースはWeb上での閲覧のみ)

---

## 3. システム構成

| レイヤー | 技術選定(案) | 備考 |
|---|---|---|
| データ収集 | Python + yfinance | 定期実行バッチ(cron / スケジューラ) |
| データベース | PostgreSQL または SQLite(初期は軽量に) | 生データ・スコア履歴を保存 |
| バックエンドAPI | FastAPI | スコアリングロジックとREST APIを実装。既存アプリとは完全に別プロセス・別リポジトリとして新規構築 |
| フロントエンド | React | ランキング一覧・詳細ページ・履歴チャートを表示。既存アプリのUIコンポーネントは流用せず、新規に設計 |
| バッチスケジューラ | cron / APScheduler等 | 日次・週次のデータ更新ジョブを管理 |

**明記事項**:本アプリは既存の市場監視・シグナル通知アプリとはコードベース・データベース・デプロイ単位を分離した完全に独立したプロダクトとする。将来的な統合(データ共有・通知連携)は本要件のスコープ外とし、必要になった時点で別途検討する。

---

## 4. 対象銘柄(ユニバース)

| 項目 | 内容 |
|---|---|
| 対象市場 | 米国株(NYSE/NASDAQ)、東証(プライム・スタンダード・グロース) |
| 時価総額フィルタ | 小型〜中型株中心。上限は設定ファイルで可変(例:米国 $3.5B未満、日本 1,300億円未満) |
| 除外対象 | ETF、REIT、ADR、金融セクター(指標の非対称性を避けるため) |
| 上場期間条件 | 上場後最低4四半期分の決算データがあること |
| ユニバースサイズ想定 | 米国800〜1,500銘柄、日本500〜1,000銘柄 |

**要調達**:yfinance自体は銘柄一覧APIを持たないため、S&P1500/Russell2000構成銘柄リストや東証上場銘柄リストを初期データとして別途用意する(CSV等での初期投入、または定期更新の仕組みが必要)。

> **v1.1修正**:市場ごとの取引通貨建てで閾値を定義すること(米国株はドル建て、日本株は円建て)。v1.0では米国株の上限を円建て(5,000億円)で表記しており、為替換算の手間・レート変動による閾値ブレを招くため修正した。この時価総額ゲートは**大型株除外の一次フィルタ**であり、15.2の売上高ゲート($3B超で除外)とは独立したAND条件として両方適用する(理由は15.6)。

---

## 5. データ要件

### 5.1 使用するyfinance項目

| カテゴリ | 取得元 | 主要フィールド |
|---|---|---|
| 基本情報 | `Ticker.info` | `marketCap`, `sharesOutstanding`, `floatShares`, `sector`, `industry`, `beta` |
| 成長性 | `Ticker.quarterly_financials` / `.info` | `Total Revenue`(四半期推移), `revenueGrowth`, `earningsGrowth` |
| 収益性 | `Ticker.quarterly_financials` | `Gross Profit`, `Operating Income`, `Net Income`(推移) |
| 収益性指標 | `Ticker.info` | `returnOnEquity`, `returnOnAssets`, `grossMargins`, `operatingMargins` |
| バリュエーション | `Ticker.info` | `trailingPE`, `forwardPE`, `pegRatio`, `priceToSalesTrailing12Months`, `enterpriseToEbitda` |
| 財務健全性 | `Ticker.balance_sheet`, `.info` | `debtToEquity`, `currentRatio`, `totalCash`, `totalDebt` |
| キャッシュフロー | `Ticker.cashflow` | `Free Cash Flow`(推移、黒字転換の検出) |
| 株主構成 | `Ticker.info` | `heldPercentInstitutions`, `heldPercentInsiders` |
| モメンタム | `Ticker.history` | 終値、出来高(52週高値/安値、移動平均、出来高急増検知) |
| アナリスト | `Ticker.recommendations`, `.info` | `recommendationMean`, `targetMeanPrice`, `numberOfAnalystOpinions` |

### 5.2 取得頻度
- 財務系(info, financials, balance_sheet, cashflow):日次1回
- 価格・出来高系(history):日次(前日終値確定後)
- フルユニバース再取得(新規上場・上場廃止反映):週次

### 5.3 データ保持期間
- スコア履歴を最低8四半期分保持(トレンド判定・後日の有効性検証用)
  - ⚠ yfinanceは**過去分を遡って提供しない**ため、8四半期分の財務時系列は「自前でスナップショットを積み上げる」以外に入手手段がない(13.1)。運用開始から2年間はこの前提が満たせない
- 生データ(financials等)はDBにスナップショット保存(yfinance側の仕様変更・欠損に備える)

---

## 6. 機能要件

### 6.1 データ収集バッチ
- 対象ユニバースの全銘柄についてyfinanceからデータを取得しDBに保存する
- 個別銘柄の取得失敗時はスキップし、処理全体は継続する
- 取得ログ(成功/失敗/欠損フィールド)を記録する

### 6.2 フィルタリング(足切り)
スコアリング前に以下で母集団を絞る。閾値は設定ファイルでパラメータ化する。

| 条件 | 理由 |
|---|---|
| 時価総額が上限未満 | 小型株バイアスの担保 |
| 直近4四半期の決算データが揃っている | YoY計算不能な銘柄を除外 |
| `marketCap`, `sector`, 直近終値がnullでない | 最低限の計算可能性担保 |
| セクターが金融・ETF・REITでない | 指標の非対称性回避 |

### 6.3 スコアリングエンジン
5〜6軸のサブスコアを算出し加重合計で総合スコアを出す(詳細は7章)。

### 6.4 Web UI
- **ランキング一覧画面**:総合スコア降順、フィルタ(市場・セクター・時価総額帯)、ソート機能
- **銘柄詳細画面**:サブスコア内訳、生データ、スコア推移チャート
- **順位変動画面**:前回実行との比較(新規ランクイン・急上昇銘柄のハイライト)

### 6.5 API
- `GET /api/candidates`:ランキング一覧取得(フィルタ・ページネーション対応)
- `GET /api/candidates/{ticker}`:個別銘柄の詳細・スコア履歴取得
- `GET /api/universe/status`:直近のデータ収集ジョブの実行状況・成功率確認用

---

## 7. スコアリング詳細仕様

### 7.1 スコア構成要素と重み(初期案・要調整)

| サブスコア | 重み | 主な入力 |
|---|---|---|
| 成長加速スコア | 30% | 直近4四半期の売上YoY成長率の推移(加速していれば加点)<br>⚠ **実装不能**(13.1参照)。15章の改訂案で代替すること |
| 収益性改善スコア | 20% | 粗利率・営業利益率の四半期推移、FCFの黒字転換 |
| バリュエーションスコア | 20% | PEGレシオ、PSR(業種内相対) |
| 財務健全性スコア | 15% | debtToEquity、現金/負債比率 |
| 発掘度スコア | 10% | 機関投資家保有率の低さ、アナリストカバレッジ数の少なさ |
| モメンタムスコア | 5% | 52週高値からの位置、出来高トレンド(過剰反映を避けるため低ウェイト) |

重みはハードコードせず、設定として外出しし将来的にバックテストで調整可能にする。

### 7.2 正規化方法
- 各生データはセクター内でz-score化(平均0、標準偏差1)してからスコアに変換する
- シグモイド関数等で0〜100にスケーリングし、外れ値の影響を抑える
- 欠損値は「そのサブスコアを算出不能」として除外し、他のサブスコアのみで重み再配分する(欠損=0点にはしない)

### 7.3 セクター相対評価
- GICSセクター(`info`の`sector`)単位でグルーピング
- 初期実装はセクター単位、将来的にサブセクター(`industry`)対応を検討

---

## 8. 非機能要件

### 8.1 パフォーマンス
- ユニバース全銘柄(最大2,500銘柄程度)のフル更新が現実的な時間(目安:数十分以内)で完了すること
- yfinanceは個別銘柄ごとのリクエストが基本のため並列化(スレッドプール等)を前提に設計する

### 8.2 エラーハンドリング・欠損値処理
- 個別銘柄取得失敗時は該当銘柄をスキップし、処理全体は継続する
- `info`が空dict or 主要フィールドがNoneの場合はユニバースから除外し、除外ログを残す
- 決算期(fiscal quarter end)が銘柄ごとに異なるため、「直近4四半期」は暦月ベースではなく各銘柄の決算日基準で揃える
- 上記は方針レベルの要件であり、失敗種別ごとの具体的な扱い(一時的/恒久的/未知の分類、隔離リスト等)は **18.1** で商用グレードの詳細仕様として定義する

### 8.3 レート制限対策
- yfinanceは非公式スクレイピングベースであり、過度なリクエストでブロックされるリスクがある
- リクエスト間隔調整、キャッシュ層、失敗時のリトライ・バックオフを実装する
- 将来的な有償API(Alpha Vantage、Financial Modeling Prep等)への切替を見込み、データソースをインターフェースで抽象化しておく
- サーキットブレーカー・縮退運用の具体仕様は **18.4** 参照

### 8.4 デプロイ・運用
- バックエンド・フロントエンド・バッチジョブはそれぞれ独立してデプロイ可能な構成とする
- 初期は個人利用規模を想定し、軽量な構成(単一サーバー or ローカル実行 + 定期cron)で十分とする

---

## 9. データモデル(概略)

| テーブル | 主な内容 |
|---|---|
| `tickers` | 銘柄マスタ(ティッカー、市場、セクター、上場日等) |
| `raw_snapshots` | yfinanceから取得した生データのスナップショット(日次) |
| `scores` | 算出済みの総合スコア・サブスコア(日次、履歴として蓄積) |
| `collection_logs` | データ収集ジョブの実行ログ(成功/失敗/欠損フィールド) |

---

## 10. 除外条件・エッジケース

| ケース | 対応方針 |
|---|---|
| IPO直後(4四半期未満) | ユニバースから除外(将来的に別枠「新規上場ウォッチリスト」として分離検討) |
| 決算期変更があった銘柄 | YoY比較が歪むため、当該四半期は成長スコア計算から除外 |
| 特別損益による利益率の異常値 | 初期実装では対応しない(定性判断が必要な領域としてスコープ外に明記) |
| 会計基準の違い(GAAP/日本基準/IFRS) | 市場をまたいだ直接比較はしない(市場内相対評価に限定) |
| 通貨 | 日本株は円ベース、米国株はドルベースのまま扱い、市場間の統合ランキングは作らない |

---

## 11. 制約事項・前提条件

- yfinanceは非公式ライブラリであり、Yahoo Finance側の仕様変更で突然フィールドが取得できなくなるリスクを常に持つ(本番運用のSLAを保証できない)
- `info`辞書の項目網羅性は銘柄・市場によってばらつきが大きく、特に日本株は米国株よりも欠損率が高い傾向がある(実装時に実データで検証が必要)
- 本システムはあくまで一次スクリーニングであり、投資助言ではない(UI/ドキュメント上での免責明記が必要)

### 11.1 「商用利用」の解釈の明確化(v1.2)

「商用利用可能なレベルのクオリティ」という要求に対し、本アプリは以下の **解釈A** を採用する。

> **解釈A**:商用「利用」(データの再配布・課金・一般公開)ではなく、商用「品質」(実装の堅牢性・例外処理・運用性がプロダクションレベルであること)を指す。利用者は個人(非公開・非課金)であり、Yahoo Financeの利用規約上の「商用利用」には該当しない前提とする。

この前提のもとでは、14.12で指摘した規約違反リスク・14.12/15.6で触れた金融規制(投資助言業登録等)の懸念は**当面該当しない**。ただし以下は今後も有効な制約として明記する。

- **将来、他者への公開・課金・データ配布(解釈B/Cへの移行)を検討する場合は、その時点で以下を再検討すること**:(1) データソースをライセンス済みAPI(Alpha Vantage、FMP等、8.3参照)へ切替、(2) 日本の金融商品取引法上の投資助言業登録要否、米国のInvestment Advisers Actにおけるpublisher's exclusion適用可否について弁護士等専門家によるレビュー
- 本要件定義書における「商用グレード」「堅牢性」等の記述(18章)は、いずれも**解釈Aの範囲内での実装品質**を指し、データライセンス・法規制の問題を解決するものではない

---

## 12. 今後の拡張(スコープ外・検討事項)

- LLMによるIR資料・決算説明会のテキスト分析(モート・経営者評価の定性スコア化)
- EDGAR Form 4連携によるインサイダー取引スコアの追加
- バックテスト機能との統合による重みの自動最適化
- プッシュ通知・アラート機能
- 他アプリとのデータ連携(必要になった時点で別途検討)

---
---

# 【v1.1 追記】

以下は yfinance 1.4.1 を用いた実データ検証(2026-08-23実施)にもとづく追記である。

---

## 13. データ制約の実測結果(要件の前提修正)

v1.0は「yfinanceから四半期の時系列が取れる」ことを暗黙の前提にしているが、**実測ではこれが成立しない**。設計に着手する前に本章の制約を反映すること。

### 13.1 四半期財務は最大5期しか取得できない

| 銘柄 | 市場・規模 | `quarterly_income_stmt` の取得期数 |
|---|---|---|
| AAPL | 米・大型 | 5期 |
| AXON | 米・大型グロース | 5期 |
| IONQ | 米・中型グロース | 6期 |
| CELH | 米・中型グロース | 7期 |
| 4180.T(Appier) | 東証プライム小型 | **3期** |
| 9552.T(M&A総研) | 東証プライム小型 | **2期** |
| 3696.T(セレス) | 東証小型 | **2期** |
| 6027.T(弁護士COM) | 東証グロース | **1期** |
| 4485.T(JTOWER) | 東証グロース | **0期(空)** |
| 7342.T(ウェルスナビ) | 東証グロース | **0期(空)** |

**帰結**:
- 7.1の「直近4四半期の売上YoY成長率の**推移**」には最低8四半期が必要だが、米国株でも5〜7期、**日本の小型グロース株では0〜3期**しか取れない。この仕様のままでは成長加速スコア(重み30%)が主要ターゲット層で算出不能になる。
- 年次(`income_stmt`)は5期取得できたため、**年次を主軸、四半期を補助**とする構成に切り替えるのが現実的(15.3)。
- 「日本株は欠損率が高い傾向がある」(11章)という記述は控えめすぎる。**東証グロースの一部銘柄は `info` 自体が実質空**(`marketCap`, `currency`, `revenueGrowth` すべてNone、`quoteSummary` が404)であり、**本アプリが最も狙うべき層が最も取得できない**という構造的な逆相関がある。

### 13.2 日本株ユニバースの実現可能性の再評価

上記より、日本株を米国株と同じスコアリング機構に載せるのは初期リリースでは非現実的。以下いずれかを選択し要件に明記すること。

| 案 | 内容 | 評価 |
|---|---|---|
| A | 初期リリースは**米国株のみ**。日本株はPhase 2 | 推奨。検証サイクルが速く、データ品質が担保できる |
| B | 日本株は別スコア体系(年次のみ・項目数を削減) | 市場間で評価軸が変わり、UI上の説明責任が増す |
| C | 日本株は EDINET/TDnet 等の別ソースを併用 | データ品質は最良だがスコープが大幅増。要件外 |

### 13.3 v1.0で未検討だった有用エンドポイント(実在確認済み)

| エンドポイント | 返却内容(実測) | 用途 |
|---|---|---|
| `Ticker.earnings_dates` | 過去約8回分の `EPS Estimate` / `Reported EPS` / `Surprise(%)` + **次回決算予定日** | サプライズ分析(15.4)、**決算起点のイベント駆動更新**(14.9) |
| `Ticker.eps_revisions` | `upLast7days` / `upLast30days` / `downLast30days` / `downLast7Days`(期別) | アナリスト予想修正の方向性。学術的に最も頑健な単独シグナルの一つ |
| `Ticker.growth_estimates` | `stockTrend` / `indexTrend`(0q/+1q/0y/+1y/**LTG**) | 長期成長率予想。PEG自前計算の分母 |
| `Ticker.get_shares_full(start=)` | 発行済株式数の**時系列**(CELHで466点) | **希薄化の直接計測**(15.4)。v1.0に完全に欠落 |
| `Ticker.earnings_estimate` / `revenue_estimate` | コンセンサス予想の水準・レンジ | 予想分散(不確実性)の代理変数 |
| `Ticker.insider_transactions` | `Shares`/`Value`/`Insider`/`Position`/`Transaction`/`Start Date` | **インサイダー売買が取得可能**。2.2で「EDGAR連携が必要」として対象外にしていたが、簡易版はyfinance単体で実装できる |
| `Ticker.upgrades_downgrades` | 格付変更履歴 | カバレッジ開始検知(発掘度スコアの動的版) |
| `Ticker.get_isin()` | ISINコード | **銘柄同一性の管理**(14.5) |
| `Ticker.ttm_income_stmt` | TTMベースの損益 | 決算期ズレを吸収した横断比較 |

### 13.4 `get_shares_full` は分割調整されていない(実装上の罠)

CELHの実測値:

```
2022-12-31:  76,225,200      ← 2023-11-15 に 3:1 分割
2023-12-31: 231,675,008      ← 見かけ上 +204%
2026-08-23: 253,035,047
```

分割を考慮せずに希薄化率を計算すると **+204%の希薄化**という誤判定になる。実際の2023→2026の希薄化は **+9.2%**(良好)。
→ `Ticker.splits` の累積分割係数で正規化してから差分を取ること。**同じ罠は `sharesOutstanding` を用いる全指標(EPS、1株売上、時価総額の過去比較)に共通する。**

### 13.5 その他の実測上の注意

| 事象 | 内容 | 対応 |
|---|---|---|
| `pegRatio` の欠損 | 4485.T=None、IONQ=None。赤字・高成長銘柄ほど落ちる | `info` の値に依存せず、EV/Sales ÷ 予想成長率 で自前計算 |
| `trailingPE` の無効化 | 赤字企業ではNone。**10バガー候補は赤字が多い** | PER/PEG系を主軸にしない(15.3) |
| `financialCurrency` ≠ `currency` | 報告通貨と株価通貨が異なる銘柄が存在 | 財務比率算出前に必ず両者を突合。不一致銘柄は換算するか除外 |
| `grossMargins` = 0.0 | 4881.T で観測。0とNoneが区別できないケース | 0はNone扱いにする閾値ルールを設ける(売上高が非0であることを併せて検証) |
| 404応答 | 7379.T で `quoteSummary` が404 | 例外ではなく**通常の分岐**として扱う(上場廃止・ティッカー変更の検知トリガ) |

---

## 14. 考慮が不足している設計要件(追記)

### 14.1 「10バガー」の定義が未定義(最優先)

v1.0はアプリ名にある「10バガー」を一度も定義していない。**評価期間を決めなければ、スコアの正しさを検証する手段が定義できない。**

| 項目 | 定義(案) |
|---|---|
| 達成条件 | 配当再投資込みトータルリターンで **10.0倍以上** |
| 評価ホライズン | **7年**(必要CAGR **38.9%**)。5年なら58.5%、10年なら25.9% |
| 基準価格 | スコア確定日の翌営業日始値(実行可能性を担保) |
| 部分達成の扱い | 3x / 5x を中間指標として併記(10xはサンプル数が枯渇するため) |

### 14.2 成功指標(KPI)の欠如

「一次スクリーニングツール」として何をもって成功とするかが無い。以下を要件化する。

| KPI | 定義 | 目標(案) |
|---|---|---|
| デシル単調性 | スコア上位10%〜下位10%の各層の**将来リターン中央値が単調**か | 単調であること(絶対値より重要) |
| リフト倍率 | 上位デシルの3x達成率 ÷ ユニバース全体の3x達成率 | ≧ 2.0 |
| 上位デシル超過リターン | 対 Russell 2000 Growth(米)の年率超過 | > 0 |
| 破綻回避率 | 上位デシルの上場廃止・90%下落率 < ユニバース平均 | 下回ること |
| ランキング安定性 | 月次のTop50入替率 | 30%以下(14.6) |

**期待値の明記**:小型株が10年で10倍になる基準率は1%未満。上位デシルでも**大半は外れる**前提をUI上にも明示すること。

### 14.3 検証・バックテスト設計の欠如(構造的な問題)

v1.0の12章は「バックテスト機能との統合」を将来課題としているが、**yfinanceからはポイントインタイム(as-of)の財務データが取得できない**ため、後付けのバックテストは原理的に不可能に近い。以下は最初から作り込む必要がある。

| 問題 | 内容 | 対応 |
|---|---|---|
| ポイントインタイム不在 | 現在時点の値しか返らない。「2020年時点でこの銘柄のスコアは何点だったか」は再現できない | **Day 1から `scores` を毎日不変で積む**。スコア履歴そのものが唯一の検証資産 |
| 修正再表示(restatement)バイアス | 過去の財務は修正後の値で返る。当時の投資家が見ていた数字ではない | 疑似バックテストの結果は**参考値**と明記。断定的な最適化に使わない |
| 生存バイアス | 現在のRussell 2000構成銘柄で過去を検証すると、**倒産・上場廃止銘柄が消えており成績が過大評価される** | `universe_snapshots` テーブルを新設し、**その日の対象銘柄集合を毎日保存**。廃止銘柄もマスタから削除せず `delisted_at` でフラグ管理 |
| 先読みバイアス | 決算日より前の日付に決算数値を紐付けてしまう | 財務行には必ず `available_from`(yfinanceで観測できた最初の日)を持たせ、その日以降でのみ使用 |

**前方検証ジョブ(必須機能として追加)**:`scores` の各行に対し、+1M / +3M / +6M / +1Y / +3Y / +5Y の実現リターンを後追いで付与するバッチ。これが無いと、アプリは永久に「当たっているかどうか分からないランキング」を出し続けることになる。

### 14.4 破綻・希薄化リスクの評価軸が無い(最大の実害リスク)

v1.0のスコアは**上振れ要因のみ**で構成されており、小型グロース株の主要な失敗モードが評価されていない。10バガー探索において、100銘柄中1銘柄の10xより **20銘柄の-90%** のほうが影響が大きい。

| 未考慮リスク | 指標 | 扱い |
|---|---|---|
| 資金枯渇 | キャッシュランウェイ = `totalCash` ÷ 直近4四半期のFCFバーン | **6四半期未満は除外ゲート**(15.2) |
| 株式希薄化 | 分割調整後の発行済株式数 3年CAGR(13.4) | 年率+15%超はペナルティ、+25%超は除外 |
| 財務破綻 | Altman Z''-Score(非製造業版) | 1.1未満は除外 |
| 会計操作 | Beneish M-Score(取得可能変数での近似)、Sloanアクルーアル | 高スコアはペナルティ |
| 流動性 | 日次売買代金の中央値、株価水準 | 米:$1M / 日:1億円 未満、株価$1未満は除外 |

### 14.5 銘柄同一性(identity)の管理

- ティッカーは変更される(社名変更・合併)。**ティッカーを主キーにすると履歴が断絶する**。
- 廃止されたティッカー文字列が**別会社に再利用される**ことがあり、過去スコアが誤った銘柄に紐付く。
- 対応:`tickers` の主キーは内部連番とし、`Ticker.get_isin()` で得たISIN(取得可能な場合)+ ティッカー履歴テーブルで同一性を管理する。

### 14.6 スコアの安定性・バージョニング(運用要件)

| 項目 | 内容 |
|---|---|
| スコアバージョン | `scores` に `scoring_version` 列を必須追加。ロジック変更時に過去スコアを**再計算で上書きしない**(検証資産が壊れるため)。新バージョンは別系列として併走させる |
| ランキングのちらつき | 日次再スコアリングは順位が毎日揺れ、「急上昇銘柄」が実質ノイズになる。総合スコアに**5営業日EMA**を掛け、順位変動画面は**週次比較**を既定にする |
| ヒステリシス | Top50の出入りに閾値差(入:45位以内 / 出:60位超)を設け、境界銘柄の往復を防ぐ |
| パラメータ管理 | 設定ファイルをGit管理し、`scores` に設定ハッシュを記録。「どの設定で出たスコアか」を後から特定可能にする |

### 14.7 セクター相対評価の統計的頑健性(7.2の補強)

| 問題 | 対応 |
|---|---|
| セクター内サンプル数不足 | z-score算出に**最低30銘柄**を要求。下回る場合は上位分類または全ユニバースへフォールバックし、その旨をフラグで保持 |
| 財務比率の裾の厚さ | 平均・標準偏差は外れ値に脆い。**中央値とMAD**によるロバストz-score、または**単純に percentile rank** を使う(後者を推奨:分布仮定が不要でシグモイド変換も不要になる) |
| 外れ値 | z化前に **1%/99% でウィンザライズ** |
| サブスコア間の多重共線性 | 収益性・財務健全性・バリュエーションは相関する。重み設定時に相関行列を確認し、実質的な重複計上を避ける |

### 14.8 欠損時の重み再配分の悪用(7.2の穴)

「欠損サブスコアを除外して重み再配分」というルールは、**データが最も少ない銘柄が最も高いスコアを得やすい**という逆選択を生む(例:モメンタムスコアだけ算出できた銘柄が、その1軸で100点なら総合100点になる)。

- **カバレッジ下限**:重み合計で **70%以上**のサブスコアが算出可能な銘柄のみランキング対象。
- **必須軸**:成長軸と財務健全性軸は算出可能であることを必須とする。
- **カバレッジ減点**:再配分後のスコアに `(カバレッジ率)^0.5` を乗じ、情報量の少なさをペナルティ化する。
- UI にデータカバレッジ率をバッジ表示する。

### 14.9 バッチ設計・レート制限の現実性(8.1/8.3の補強)

v1.0の「日次1回・財務系フル取得」は、2,500銘柄 × 主要5エンドポイント ≒ **12,500リクエスト/日**。Yahoo Finance の非公式APIはこの水準で高確率にスロットリングされ、かつ**財務は四半期に1回しか更新されない**ため大半が無駄な取得になる。

| 改善 | 内容 |
|---|---|
| イベント駆動更新 | `Ticker.earnings_dates` の次回決算予定日を保持し、**決算後7日間のみ財務系を再取得**。それ以外は取得しない |
| 価格の一括取得 | 個別 `Ticker.history` ではなく `yf.download(tickers=[...], group_by=...)` でバッチ取得(リクエスト数が桁で減る) |
| 反映ラグの許容 | yfinanceへの決算反映は数日〜数週間遅れることがある。決算日到来のみで確定扱いにせず、**前回と値が変わるまでポーリング**する設計にする |
| 並列度 | 並列化は必須だが、Yahoo側のブロック回避のため同時実行数は控えめ(4〜8)+ ジッタ付きバックオフ。速度より完走率を優先 |
| バージョン固定 | yfinance は破壊的変更が多い。バージョンをピン留めし、アップグレードは**スキーマ検証テスト通過後**に行う |

### 14.10 データ品質検証層(未考慮)

取得成功=正しい、ではない。スコアリング前に検証ステージを挟む。

- レンジ検証(粗利率が -1〜1 の外、負の売上、時価総額が株価×株数と桁違い 等)
- 前日比の急変検知(売上が10倍/0.1倍になった等 → 単位変更・yfinance側の不具合を疑う)
- クロス検証(`marketCap` ≒ 終値 × `sharesOutstanding`、`totalDebt` ≒ BSの負債合計)
- 検証NGは**スコアリングから除外**し `collection_logs` に理由コードで記録(黙って通さない)

### 14.11 ストレージ容量(9章の補強)

`raw_snapshots` を「日次・全銘柄・生データ」で保存すると、2,500銘柄 × 約50KB × 365日 ≒ **年45GB**。SQLiteでは非現実的。

- 財務系スナップショットは**内容ハッシュが変化したときのみ**新レコードを作る(実質、四半期に1回)
- 価格は正規化した OHLCV テーブルに分離(JSONで持たない)
- 生JSONは圧縮(JSONB/gzip)+ 保持期間ポリシー(生データ2年、スコア履歴は無期限)を定める
- 初期からPostgreSQLを選ぶほうが移行コストは安い

### 14.12 法務・規約(未考慮)

| 項目 | 内容 |
|---|---|
| Yahoo Finance 利用規約 | 個人利用目的のスクレイピングを超える**再配布・商用利用は禁止**されている。本アプリを**外部公開する場合は規約違反となる可能性が高い**。初期リリースは「個人利用・非公開(ローカル or 認証付き単一ユーザー)」に限定することを要件として明記する |
| 公開時の条件 | 公開するならライセンス済みデータソース(FMP、Alpha Vantage、EOD等)へ切替。8.3の抽象化レイヤはこの目的にとって必須要件 |
| 金融規制 | 個別銘柄の売買推奨を行う設計にしない(投資助言・代理業への該当回避)。免責は11章にあるが、**UIの各画面に常時表示**すること |
| データ再配布 | スコアやランキングのエクスポート/共有機能を設ける場合、原データではなく**自社算出のスコアのみ**とする |

### 14.13 認証・セキュリティ(未考慮)

- 個人利用でもインターネット公開するなら認証必須(単一ユーザーでも Basic 認証以上)
- APIのレート制限、`GET /api/candidates` のページネーション上限
- 依存パッケージの脆弱性スキャン(バッチが外部HTTPを叩くため)

### 14.14 テスト戦略(未考慮)

| 種別 | 内容 |
|---|---|
| ユニットテスト | 各サブスコア関数を**固定入力→期待値**で検証(欠損・ゼロ除算・負値・外れ値を網羅) |
| ゴールデンファイル | 実銘柄のyfinanceレスポンスをフィクスチャとして保存し、リグレッション検知に使う(ネットワーク非依存でCIが回る) |
| スキーマ検証テスト | yfinance の返却キー/型を検証するテストを毎日実行。**仕様変更の早期検知**が本アプリの生命線(11章のリスクに対する唯一の実効的な対策) |
| 不変条件テスト | 「スコアは0〜100」「重み合計=1」「カバレッジ<70%の銘柄はランキングに現れない」 |

### 14.15 運用監視(未考慮)

- ジョブ失敗・成功率低下(例:取得成功率90%未満)のアラート
- **データ鮮度監視**:最終成功日時からの経過。UIに「データ最終更新日時」を常時表示
- 静かな劣化の検知:「取得は成功しているが特定フィールドのnull率が急上昇」= yfinance仕様変更のシグナル。`collection_logs` のフィールド別null率を日次で監視

### 14.16 UI要件の補強(6.4)

- **スコアの説明可能性**:なぜこの順位なのかをサブスコア寄与度(ウォーターフォール等)で提示。ブラックボックスなランキングは使われない
- データカバレッジ率・最終更新日・スコアバージョンの表示
- 除外された銘柄とその理由を確認できる画面(デバッグと信頼性のために必須)
- 免責表示の常時掲示(14.12)

---

## 15. 精度向上のためのスコアリング戦略(改訂案)

### 15.1 10バガーの構造分解 —— 何を測るべきかの根拠

株価は恒等式として次に分解できる。

```
株価 = 売上 × 純利益率 × PER ÷ 発行済株式数
```

したがって7年で10倍(CAGR 38.9%)を達成する経路は、次の4因子の積に限られる。

| 因子 | 現実的な寄与 | yfinanceでの測定 |
|---|---|---|
| ① 売上成長 | 3〜5x(年率20〜25%を7年) | 年次売上CAGR、TTM売上YoY |
| ② 利益率拡大 | 1.5〜3x | 粗利率・営業利益率のトレンド |
| ③ マルチプル再評価 | 1〜2x(**低評価から入ることでのみ得られる**) | EV/Sales、EV/GrossProfit の業種内相対 |
| ④ 株数希薄化 | **0.6〜0.9x(常にマイナス寄与)** | 分割調整後 `get_shares_full` の3年CAGR |

**設計上の含意**:
- v1.0のスコアは①②③を扱い、**④を完全に欠いている**。小型グロース株では希薄化が年率10〜20%に達することがあり、①の成果を丸ごと打ち消す。**④の追加が単独で最も費用対効果の高い精度改善**。
- ③は「割安であること」ではなく「**成長に対して割安であること**」。絶対的な低PSRを評価すると、成長しない万年割安株を拾う。

### 15.2 加重和からゲート+加重幾何平均へ(構造の変更)

**最も重要な改善提案。** 10バガーの条件は加算的(どれか強ければ良い)ではなく**連言的**(すべて満たす必要がある)である。v1.0の加重和では「成長30点満点・財務健全性0点」の銘柄が高順位に入り得るが、そういう銘柄は倒産する。

```
Step 1: 除外ゲート(1つでも該当すれば圏外。スコア化しない)
Step 2: 総合スコア = 100 × Π (サブスコア_i / 100) ^ w_i      ← 加重幾何平均
```

加重幾何平均は、**いずれかの軸が0に近いと総合スコアも0に近づく**ため、弱点のある銘柄が上位に紛れ込まない。

**Step 1:除外ゲート(必須条件)**

| ゲート | 閾値(案) | 根拠 |
|---|---|---|
| キャッシュランウェイ | 6四半期未満は除外(黒字企業は対象外) | 資金枯渇=強制的な希薄化 or 破綻 |
| 希薄化 | 分割調整後 株式数3年CAGR > +25% は除外 | 1株価値の毀損が成長を上回る |
| 財務破綻 | Altman Z'' < 1.1 は除外 | ダウンサイドの遮断 |
| 流動性 | 日次売買代金 中央値 < 米$1M / 日1億円 は除外 | 実際に買えない銘柄を出さない |
| 株価水準 | < $1(日本株は < 100円) | 上場廃止基準・ペニー株回避 |
| データカバレッジ | 重み合計70%未満は除外(14.8) | 情報不足銘柄の逆選択防止 |
| 売上規模 | TTM売上 > $3B は除外 | **大きすぎる企業は10倍になれない**。4章の時価総額ゲートとAND条件で併用する(15.6) |

**Step 2:改訂サブスコアと重み**

| サブスコア | v1.0 | **改訂案** | 主な入力 |
|---|---|---|---|
| 成長の質・持続性 | 30% | **25%** | 年次売上CAGR(3年)、TTM売上YoY、**粗利の絶対額成長**、成長率の安定性 |
| **1株価値の保全(新設)** | — | **15%** | **希薄化率**、FCF/純利益、アクルーアル |
| 収益性・ユニットエコノミクス | 20% | **15%** | 粗利率トレンド、営業利益率トレンド、Rule of 40、ROIC |
| 財務健全性・生存力 | 15% | **10%** | Piotroski F-Score、ランウェイ、Z''(ゲート通過後の程度評価) |
| 成長調整バリュエーション | 20% | **15%** | EV/Sales ÷ 予想成長率、EV/GrossProfit(業種内 percentile) |
| **予想修正モメンタム(新設)** | — | **10%** | `eps_revisions` の上方修正比率、`Surprise(%)` の連続性 |
| モメンタム | 5% | **10%** | **12-1モメンタム**、52週高値近接度 |
| 発掘度・スキン・イン・ザ・ゲーム | 10% | **? %**(下記) | 機関保有率の低さ、カバレッジ数、**インサイダー保有率の高さ・買い越し** |

※ 上表は合計100%になるよう発掘度を調整すること(残り0%ではなく、発掘度を10%とし他を微調整する運用を想定)。**重みは設定ファイルで外出しし、14.3の前方検証データが蓄積されるまで固定する**(データ無しでの最適化は過学習にしかならない)。

### 15.3 v1.0の指標選択の問題と代替

| v1.0の指標 | 問題 | 代替 |
|---|---|---|
| 四半期売上YoYの推移(4期) | **データが存在しない**(13.1) | 年次売上CAGR(3年、`income_stmt`)を主軸 + TTM YoY 1点で足元を補正 |
| `pegRatio` | 欠損多発、算出根拠が不透明(13.5) | `EV/Sales ÷ growth_estimates['+1y']` を自前計算 |
| `trailingPE` / `forwardPE` | 赤字企業でNone。**候補群の多くが赤字** | EV/GrossProfit、EV/Sales |
| `returnOnEquity`(`info`) | 自社株買い・レバレッジで容易に嵩上げされる | **ROIC = NOPAT ÷ 投下資本** を財務諸表から自前算出 |
| 総売上の成長 | 増資・買収による見かけの成長を拾う | **1株あたり売上**の成長(希薄化を自動的に織り込む) |
| 52週高値からの位置(5%) | モメンタムを過小評価。学術的には最も頑健なシグナルの一つ | **12-1モメンタム**(過去12ヶ月リターン − 直近1ヶ月)を追加し、合計10%へ |
| 純利益ベースの収益性 | 小型グロースでは赤字が常態、一時損益で暴れる | **粗利(Gross Profit)の絶対額成長**を主軸に(SaaS/成長企業の実質的な売上指標) |

### 15.4 追加を推奨する具体的シグナル(すべてyfinance単体で計算可能)

| # | シグナル | 計算式 | 根拠・効果 |
|---|---|---|---|
| 1 | **希薄化率** | 分割調整後 `get_shares_full` の3年CAGR。**必ず `Ticker.splits` で正規化**(13.4) | 15.1④。単独で最大の改善余地 |
| 2 | **EPS予想修正** | `eps_revisions`:`(up30d − down30d) / (up30d + down30d)` | 予想修正の方向性は最も再現性の高いクロスセクション・アノマリーの一つ |
| 3 | **決算サプライズの連続性** | `earnings_dates` の `Surprise(%)` が直近4回中3回以上プラス | PEAD(決算後ドリフト)。CELHは5回中4回でプラス、うち2回が+50%超 |
| 4 | **Piotroski F-Score** | 9項目の二値判定(ROA>0、CFO>0、ΔROA>0、CFO>NI、Δ長期負債比率<0、Δ流動比率>0、増資なし、Δ粗利率>0、Δ資産回転率>0) | 全項目が `income_stmt`/`balance_sheet`/`cash_flow` の年次5期から算出可能。財務健全性軸をルールベースで頑健化 |
| 5 | **Altman Z''-Score** | `6.56·(WC/TA) + 3.26·(RE/TA) + 6.72·(EBIT/TA) + 1.05·(自己資本/総負債)` | 非製造業向け版。破綻ゲート(15.2) |
| 6 | **Sloanアクルーアル** | `(純利益 − 営業CF) / 平均総資産`。**低いほど良い** | 利益の質。1行で計算でき、会計操作の初期検知になる |
| 7 | **Beneish M-Score(近似)** | 取得可能な変数(DSRI、GMI、SGI、SGAI、LVGI、TATA)のみで算出。変数欠損時は不適用フラグ | 粉飾検知。小型株ユニバースの地雷除去 |
| 8 | **Rule of 40** | `売上YoY(%) + FCFマージン(%) ≧ 40` | 成長と収益性のトレードオフを1指標に統合。赤字でも高成長なら評価される |
| 9 | **キャッシュランウェイ** | `totalCash ÷ 直近4四半期の平均FCFバーン` | 生存ゲート(15.2) |
| 10 | **ROIC** | `NOPAT ÷ (有利子負債 + 自己資本 − 現金)` | 再投資効率。`成長率 ≒ 再投資率 × ROIC` の関係から、高ROIC企業のみが高成長を長期に持続できる |
| 11 | **12-1モメンタム** | 過去12ヶ月リターン − 直近1ヶ月リターン | 直近1ヶ月を除くことで短期反転を回避 |
| 12 | **インサイダーの姿勢** | `heldPercentInsiders` の高さ + `insider_transactions` の直近6ヶ月の買い越し | 創業者主導企業は10バガーの典型。2.2で対象外としたが**yfinanceで実装可能**(13.3) |
| 13 | **カバレッジ開始検知** | `upgrades_downgrades` に新規カバレッジが出現 | 「発掘度」の静的評価(カバレッジ数の少なさ)を**動的な転換点検知**に進化させる |
| 14 | **成長の安定性** | 過去の売上成長率の変動係数(CV)。低いほど加点 | 単発の急成長より持続的成長のほうが10x到達率が高い |

### 15.5 ランキング運用の改善

| 項目 | 内容 |
|---|---|
| 正規化 | z-score+シグモイドではなく、**セクター内 percentile rank** を推奨(分布仮定不要、外れ値に頑健、UIでも「業種内で上位◯%」と説明しやすい) |
| リバランス頻度 | 10バガー探索は数年ホールド前提。**日次でスコアを計算しても、ランキング確定は週次**にする(14.6) |
| ランクイン継続期間 | 「Top50に何日連続で入っているか」を表示。**一過性の上位より継続的な上位のほうがシグナルとして強い** |
| 二層構成 | Tier 1(全ゲート通過・カバレッジ高)と Tier 2(監視対象:ゲート1つ未達 or データ不足)を分離表示。IPO直後銘柄(10章)もTier 2に置けば、除外せずに追跡できる |

### 15.6 大型株を二重ゲートで除外する理由

4章(時価総額上限)と15.2(TTM売上高$3B超で除外)は同じ「大きすぎる企業の除外」を目的とするが、**片方だけでは不十分なため両方をAND条件で残す**。

| ケース | 時価総額 | 売上高 | 時価総額ゲートのみ | 売上高ゲートのみ | 両方(AND) |
|---|---|---|---|---|---|
| 赤字の大型バイオ・AI企業(時価総額は大きいが売上はまだ小さい) | 大 | 小 | 除外される(正しい判定) | **除外されない**(漏れ) | 除外される |
| 低採算・資産集約型の大企業(小売・商社等、売上は巨大だが時価総額は相対的に低い) | 小〜中 | 大 | **除外されない**(漏れ) | 除外される(正しい判定) | 除外される |

いずれのゲートも単独では取りこぼしが発生するため併用する。

**そもそも大型株を除外する理由(3点)**:

1. **必要な絶対成長額が非現実的になる**:7年で10倍(CAGR 38.9%、14.1)には、時価総額が大きいほど到達すべき絶対額が跳ね上がる(例:$500Mなら$5B到達で済むが、$200Bなら$2T到達が必要で、史上稀な構造変化でしか起きない)。企業の巧拙ではなく算数上の制約。
2. **発掘度スコア(15.2改訂表)が構造的に0点になる**:大型株は機関保有率60〜80%超・アナリストカバレッジ30〜50人規模が通常であり、「保有率の低さ」「カバレッジの少なさ」を測るこの軸で必ず低スコアになる。ゲートで先に弾かなければ、加重幾何平均の計算・API取得コストを無駄に消費するだけで結果はほぼ変わらない。
3. **効率的市場化が進んでいる**:大型株は数十人単位のアナリストが日次でカバーしており、yfinanceの公開財務指標だけで見つけられる非効率性はほぼ残っていない。本アプリの狙いはカバレッジの薄い銘柄の非効率性を拾うことにあり、大型株を含めることはこの前提と矛盾する。

パラダイムシフト型の大型株10バガー(過去のNvidia等)は例外的に存在するが、これは定量財務スクリーニングではなく定性判断(技術トレンド認識)に依存する事象であり、2.2で対象外としたLLMによる定性分析領域に属する。本アプリのスコープには含めない。

---

## 16. 改訂に伴うスコープ変更の要約

| 章 | 変更内容 |
|---|---|
| 2.2 対象外 | インサイダー取引スコアは **yfinance単体で実装可能**なため対象内へ移動(13.3、15.4-12) |
| 4 ユニバース | 初期リリースは**米国株のみ**を推奨(13.2)。時価総額上限をドル建てに修正し、売上規模ゲート(15.2)とAND併用に明記(15.6) |
| 5 データ要件 | 13.3のエンドポイントを追加。取得頻度をイベント駆動へ変更(14.9) |
| 6 機能要件 | **前方検証ジョブ**、データ品質検証層、除外銘柄確認画面を追加(14.3、14.10、14.16) |
| 7 スコアリング | 加重和 → **ゲート + 加重幾何平均**(15.2)。サブスコア構成と重みを改訂 |
| 8 非機能 | ストレージ設計、監視、テスト戦略、認証を追加(14.11、14.13〜14.15)。エラーハンドリング・レート制限対策の詳細を18章へ委譲 |
| 9 データモデル | `universe_snapshots`、`ticker_aliases`、`forward_returns` テーブルを追加。`scores` に `scoring_version`・設定ハッシュ列を追加 |
| 11 制約事項 | Yahoo Finance 利用規約による**公開範囲の制限**を追加(14.12)。「商用利用」の解釈(A:商用品質/個人利用)を明記(11.1) |
| 18(新設) | 商用グレード品質のための実装詳細(例外処理の分類、数値ロジックのエッジケース、並行性・べき等性、可用性、API/セキュリティ、監視閾値) |
| 15.2 除外ゲート | **Altman Z''ハードゲートを撤去**し、R&Dバイアスを受けない`negative_equity`(自己資本マイナス)ゲートに置き換え。Z''はフェーズ3スコアリングの入力に格下げ(19.8) |
| 19(追記) | フェーズ1・2の実データ検証結果(閾値の裏付け、セクター除外漏れ、データ単位の罠、Altman Zバイアスの発見と是正、並列度調整の効果測定) |
| 7 スコアリング(実装) | 予想修正モメンタムサブスコアを v1 スコープ外とし、残り7サブスコアで重み再配分(20.1)。実装フィールドマッピングを20.2に記録 |
| 8.4 デプロイ・運用 | 日付処理はローカルタイムゾーンに依存せずUTCに統一する原則を追加(20.3、タイムゾーン不整合バグの実例つき) |
| 20(追記) | フェーズ3(スコアリングエンジン)のv1スコープ、実装指標のフィールドマッピング、タイムゾーンバグの発見・修正、全銘柄(5,312銘柄)での実データ検証結果 |
| 3 システム構成 | バッチスケジューラをWindows Task Schedulerで実装(21.1) |
| 6.5 API | 6.5の3エンドポイントに加え、14.16向けの除外銘柄検索エンドポイントを追加実装(21.2) |
| 9 データモデル | `scores`に`subscore_fallback`(JSONB)列を追加(21.2、14.7のフォールバック透明化) |
| 21(追記) | 日次自動実行(Windows Task Scheduler)の実装とバッチファイルのエンコーディングバグ、FastAPI実装の設計判断と実データ検証結果 |
| 9 データモデル | (追加)前方検証ジョブが`forward_returns`への書き込みを開始(22.1) |
| 18.4/18.6/18.7 | バックアップ・DBユーザー分離・監視閾値のログ実装。依存パッケージスキャン・APIレート制限は意図的に先送り(22.2) |
| 22(追記) | 前方検証ジョブの実装、運用移行チェックリストの実装状況と先送り項目 |
| 3/6.4 フロントエンド | Vite + React + TypeScriptで実装。`GET /candidates`に`date`パラメータ、`GET /scores/dates`を追加(23.1) |
| 23(追記) | フロントエンド実装内容、Playwrightによる実データ検証結果、実装上の判断 |

---

## 17. 実装順序の推奨

1. ✅ **データ収集 + 品質検証層 + 生存バイアス対策(`universe_snapshots`)** —— 検証資産は初日から積まないと永久に取り戻せない(14.3)。この段階で **18.1(例外分類)・18.3(べき等性)** を最初から組み込む(後付けは全面書き換えになりやすい)。**実装・全銘柄実データ検証完了(19章・20.4)**
2. ✅ **除外ゲートの実装**(15.2 Step 1) —— スコアリングより先。地雷を外すほうがリターンへの寄与が大きい。**18.2(数値エッジケース)** をゲート・サブスコア双方の実装時点で適用する。**実装・全銘柄実データ検証完了(19.8・20.4、768銘柄が通過)**
3. ✅ **スコアリングエンジン**(percentile rank + 加重幾何平均)。**v1スコープ(予想修正モメンタムを除く7サブスコア)で実装・全銘柄実データ検証完了(20章、754銘柄をスコアリング)**
4. ✅ **日次自動実行**(Windows Task Scheduler)。**実装・動作確認完了(21.1)**。スコア履歴はここから毎日蓄積される
5. ✅ **API**(6.5)。**4エンドポイント実装・実データ検証完了(21.2・21.3)**。フロントエンド(React、3章)は未着手
6. ✅ **前方検証ジョブ**(14.3)。**実装完了、日次パイプラインに組み込み済み(22.1)**。スコア履歴が最短ホライズン(1M)分蓄積されるまでは`computed: 0`が正常
7. ✅ **運用移行チェックリスト**(18.4・18.6・18.7)。**バックアップ・DBユーザー分離・監視閾値のログ実装が完了(22.2)**。依存パッケージスキャン・APIレート制限は11.1解釈Aの範囲では意図的に先送り
8. ✅ **React フロントエンド**(3章・6.4)。**3画面実装・Playwrightによる実データ検証完了(23章)**
9. ✅ **二層構成(Tier 1 / Tier 2)**(15.5)。**実装完了(26章)**。加重幾何平均が高成長株を構造的に沈めることを実データで確認し、監視リストとして分離表示した
10. 前方検証データが12ヶ月以上蓄積された後に、初めて重みの調整を検討する
11. (追加)予想修正モメンタム・12-1モメンタム・インサイダー売買の収集を追加し、スコアリングengineをv2へ拡張(20.1)

---

# 【v1.2 追記】商用グレード品質のための実装詳細

以下は「商用利用可能なレベルのクオリティ」という要求に対し、**解釈A(11.1:商用品質・個人利用)**の範囲で、ロジック・例外処理を実運用に耐える水準まで具体化したものである。8章(非機能要件)を補完する詳細仕様として扱う。

## 18. 例外処理・堅牢性の詳細仕様

### 18.1 データ取得層:失敗を1種類の`except`で握りつぶさない

8.2は「取得失敗はスキップ」とだけ規定しているが、商用品質では**失敗の種類ごとに扱いを分ける**必要がある。単一の`try/except Exception: continue`は、11章で指摘したyfinanceの仕様変更リスクを静かに握りつぶし、原因不明のスコア劣化として後から表面化する。

| 失敗の種類 | 判定方法 | 扱い |
|---|---|---|
| 恒久的失敗(上場廃止・ティッカー変更) | HTTP 404、`quoteSummary`エラー | `tickers.delisted_at` を更新し、以後は再試行しない(14.5のticker来歴管理と連動) |
| 一時的失敗(レート制限) | HTTP 429、タイムアウト | 指数バックオフで**当日中に再試行**。恒久的失敗と混同しない |
| 観測できたが空(`info`が空dict) | 正常応答だが主要フィールドが全てNone | 「一時欠損」として記録。**3日連続で空なら**恒久的失敗に格上げし調査キューへ |
| 部分欠損(主要キーの一部のみNone) | フィールド単位の判定 | `collection_logs`にフィールド別null率を記録。閾値超過は14.15のアラート対象 |
| パース例外(未知のキー構造・型変更) | 型/スキーマ検証エラー | ticker単位で例外境界を区切り、**他銘柄の処理を継続**。同時に仕様変更の疑いとして即時ログ出力(14.14のスキーマ検証テストと連動) |
| 恒久的失敗の累積 | 同一銘柄が連続N回(既定:5回)失敗 | **隔離リスト**に移し、日次の通常対象から除外。週次で再挑戦し、復旧すれば自動復帰(無限リトライ・無限スキップの両方を回避) |

### 18.2 数値計算ロジックのエッジケース

15.4で追加した指標群は、ゼロ割・符号反転・未定義値に直面する。「エラーで落とす」のではなく、**該当サブスコアのみ欠損として扱い、7.2/14.8のカバレッジルールに委ねる**ことを共通原則とする。

| 指標 | 壊れるケース | 対応 |
|---|---|---|
| YoY成長率 | 前期がゼロ以下(黒字転換・赤字転落) | 算出不能として欠損扱い。黒字転換自体は既存の「FCF黒字転換」加点ロジックと同様に、売上・利益についても別途の転換フラグとして加点対象にする |
| PSR / EV/Sales | 売上ゼロ(プレレベニュー企業) | 算出せず欠損扱い |
| ROIC | 投下資本がゼロ近傍・マイナス | 分母の絶対値が閾値未満の場合は算出不能として除外(異常値を生まない) |
| PEGの自前計算(15.3) | 予想成長率がゼロ以下 | 除外(負の成長率で除算すると符号が反転し「割安」に誤判定される) |
| Altman Z'' / Piotroski / Beneish系(15.4) | 分母(総資産・総負債等)が異常値 | 14.10のレンジ検証を通過した値のみ使用。未通過は算出不能扱い |
| percentile rank | セクター内の値が同一(小サンプルで発生しうる) | タイは平均ランク法で扱う。分母は`n`で正規化しゼロ除算を避ける |
| 分割調整後の希薄化率(13.4) | `Ticker.splits`の取得自体が失敗 | 誤った調整係数で計算するより**算出不能として欠損扱い**にする方が安全 |

### 18.3 並行性・べき等性・トランザクション整合性

| 問題 | 内容 | 対応 |
|---|---|---|
| バッチの多重実行 | 前回実行(数十分〜数時間)が終わる前に次のcronが起動する | DBのアドバイザリロック等でジョブ単位の排他制御を行う |
| 途中クラッシュからの再開 | 大量銘柄の処理中にプロセスが異常終了する | 銘柄単位でチェックポイントを保持し、**再実行時は未処理分のみ処理**する(全件やり直しにしない) |
| 部分書き込みの可視化 | 当日分のスコア計算中にAPIが「計算途中の不完全な結果」を返してしまう | `scores`はステージングテーブルに書き込み、**全銘柄の計算完了後に原子的にスワップ**する。計算中はAPIが前日分を返し続ける |
| 決定性(再現性) | 同一入力から常に同一のスコア・順位を再現できるか | 同点順位のタイブレークキー(例:ティッカーの辞書順)を明示的に固定し、UIの表示順が実行のたびにチラつかないようにする |

### 18.4 可用性・縮退運用

| 項目 | 内容 |
|---|---|
| サーキットブレーカー | バッチ実行中にyfinanceの失敗率が閾値(既定:30%)を超えたら**即座に中断**し、ユニバース全体を無駄にリトライしない。翌日のブロック解除を待って再試行する |
| 縮退運用 | 当日バッチが全滅した場合、APIは500エラーではなく**前回成功分をそのまま返す**。14.15の「データ最終更新日時」バッジで鮮度を明示する |
| バックアップ・復旧 | 単一サーバー構成(8.4)は単一障害点。少なくとも日次のDBバックアップと復元手順を明文化する。`scores`履歴は14.3の通り検証資産そのものであり再生成不可能なため、他のテーブルより優先度を上げてバックアップする |
| スキーマ移行 | `ALTER TABLE`をその場しのぎで行わず、バージョン管理されたマイグレーションツール(Alembic等)を用いて9章のテーブル追加(`universe_snapshots`等)を管理する |

### 18.5 API/データ契約の商用品質

| 項目 | 内容 |
|---|---|
| APIバージョニング | `/api/v1/candidates` のようにバージョンを切り、将来の破壊的変更でクライアントを壊さない |
| 入力検証 | ページネーションの`limit`に上限を設ける(過大な値による負荷を防止)。ティッカー文字列のフォーマットを検証する |
| 存在しないリソースの扱い | `GET /api/candidates/{ticker}`で存在しないティッカーを指定された場合は404を明確に返す(6.5に未定義だった挙動) |
| 自アプリのレート制限 | 将来の利用者増加に備え、自アプリのAPI自体にもレート制限を設ける |

### 18.6 セキュリティ・シークレット管理

- DB認証情報、将来の有償API(FMP等、8.3)のAPIキーは環境変数/シークレットマネージャで管理し、リポジトリに含めない
- バッチ層(書き込み権限)とAPI層(読み取り専用)でDBユーザーを分離し、最小権限の原則を適用する
- 依存パッケージの脆弱性スキャンをCIに組み込む(14.14のテスト戦略に統合)

### 18.7 運用アラートの具体的閾値

14.15は「アラートを出す」とだけ規定していたため、商用品質として具体的な数値を定義する。

| 監視項目 | 警告 | 緊急 |
|---|---|---|
| データ取得成功率 | 95%未満 | 90%未満 |
| フィールド別null率の前回比急増 | +20ポイント以上 | +40ポイント以上(仕様変更を強く疑う) |
| バッチ実行時間 | 想定時間(8.1)の2倍超 | 想定時間の4倍超、またはタイムアウト |
| 隔離リスト(18.1)入り銘柄数 | ユニバースの5%超 | ユニバースの10%超(データソース側の広範な障害を疑う) |

---

# 【v1.3 追記】実データによる閾値検証

フェーズ1(データ収集基盤)実装後、NASDAQ/NYSE候補5,312銘柄から無作為抽出した370銘柄(`random.seed(42)`、2026-08-23実行)の実データを用いて、4章・15.2で設定した閾値の妥当性を検証した。

## 19. フェーズ1実データ検証で判明した知見

### 19.1 検証方法

- 母集団:universe_source.py で取得したNASDAQ/NYSE候補5,312銘柄
- サンプル:無作為抽出800銘柄を収集対象としたが、収集中にサーキットブレーカーが作動(19.4参照)し、実際に財務データを取得できたのは370銘柄
- 370銘柄は時価総額$1.3M〜$4.5Tまで分布しており、小型株からメガキャップまでを含む(意図的に時価総額で層化していない無作為抽出のため、閾値が母集団の**銘柄数ベースの分布**のどこに位置するかを検証するのに適している)

### 19.2 時価総額・売上高ゲートの検証(4章・15.2・15.6)

| 閾値 | 実データでの該当率 | 判定 |
|---|---|---|
| 時価総額 < $3.5B | 370銘柄中68.1%(252銘柄) | 維持 |
| 売上高 < $3B(時価総額$3.5B未満の母集団内) | 71.8%(28.2%が超過) | 維持。**この28.2%が15.6のAND条件の実効性を裏付ける**(低採算・資産集約型企業が理論上の懸念どおり実在した) |
| 全ゲート(時価総額・売上高・株価・流動性・非金融)を同時通過 | 18.6%(69/370) | 5,312銘柄の母集団に外挿すると**約988銘柄**。4章の目標レンジ(米国800〜1,500銘柄)に整合することを確認 |

15.6で「時価総額ゲート単独では取りこぼす」と理論的に指摘した低採算・資産集約型企業のケースが、実データで28.2%という無視できない規模で存在することを確認した。AND条件の設計判断は妥当だったと言える。

### 19.3 株価下限・流動性ゲートの検証(15.2)

| 閾値 | 実データでの該当率 | 判定 |
|---|---|---|
| 株価 < $1 | 6.8%(25銘柄) | 維持。NYSE/NASDAQの継続上場基準(最低気配値$1)自体に根拠があるため、該当率に関わらず妥当 |
| 日次売買代金 < $1M | 30.5%(単日の出来高×終値による代理指標) | **暫定維持、要再検証** |

**流動性ゲートについての重要な留保**:15.2は本来「日次売買代金の**中央値**」を要求しているが、今回の検証時点では`price_snapshots`に各銘柄1日分のデータしかなく、中央値ではなく収集日(2026-08-21)単日の出来高×終値を代理指標として使わざるを得なかった。単日データは出来高の日次変動(材料の有無等)によるノイズを含むため、この30.5%という数字を最終閾値の根拠として過度に信頼すべきではない。**price_snapshotsが20〜60営業日分蓄積された時点で、真の中央値ベースの再検証を必須のフォローアップタスクとする**(config/universe.yamlにも同旨のコメントを記載済み)。

### 19.4 収集バッチの並列度に関する実測(8.3・14.9・18.4)

800銘柄を並列8ワーカー・ジッタ0.5〜2.0秒で収集したところ、開始から約5分・478銘柄処理した時点でtransient_failure(レート制限・タイムアウト)が急増し、失敗率30.1%(144/478)でサーキットブレーカーが作動した。

これは18.4で設計したサーキットブレーカーが意図通り機能した事例であると同時に、**この並列度・ジッタ設定では実運用規模(5,000銘柄超のユニバース)で恒常的にレート制限に抵触する**ことを示す実測結果でもある。`collection.yaml`の並列度を8→5、ジッタを0.5〜2.0秒→1.0〜3.0秒に調整した。ユニバース全体(約5,000銘柄)を1回のバッチで完走させるにはさらなる調整(並列度の追加削減、複数回に分けた収集、時間帯分散等)が必要になる可能性が高く、フェーズ2以降で実際に全銘柄収集を行う際に再検証すること。

### 19.5 データ項目の単位に関する新たな罠:`debtToEquity`

13.4(`get_shares_full`の分割未調整)と同種の実装上の罠が新たに見つかった。

`Ticker.info`の`debtToEquity`フィールドは、実データでは中央値35.3・90パーセンタイル238.7・最大3854.4という分布を示した。これは**比率(例:1.0倍)ではなく百分率(例:35.3%)で返っている**ことを意味する。仮に15.4-4(Piotroski F-Score)や15.4-5(Altman Z''-Score)の実装時にこのフィールドを比率だと誤って扱うと、財務健全性の判定が完全に破綻する。**フェーズ3(スコアリングエンジン)でこのフィールドを使う際は、必ず100で除算するか、他の生データ(`totalDebt`/`stockholdersEquity`)から自前で比率を再計算すること。**

### 19.6 セクター除外漏れの発見と修正:REIT

4章は「除外対象:ETF、REIT、ADR、金融セクター」と明記していたが、実装(`config/universe.yaml`の`excluded_sectors`)には`Financial Services`/`Financial`のみが登録されており、**REITが分類される`Real Estate`セクターが除外リストから漏れていた**。実データでは370銘柄中13銘柄(3.5%)が`Real Estate`セクターであり、うちABR・AAT・GNL等、時価総額$3.5B未満で他の全ゲートを通過し得るREITが実際に候補プールへ混入していた。`excluded_sectors`に`Real Estate`を追加して修正済み。

また、金融セクター(`Financial Services`)は370銘柄中100銘柄(27.0%)を占め、セクター除外が候補プールの規模に与える影響が想定以上に大きいことも確認した。

### 19.7 財務健全性ゲートの実装可能性に関する予備知見

15.4-4/5(Piotroski F-Score、Altman Z''-Score)の実装に向けた予備調査として、時価総額$3.5B未満・非金融セクターの166銘柄で以下を確認した。

| 項目 | カバレッジ | 備考 |
|---|---|---|
| `currentRatio` | 100% | 中央値2.09だが最大638.7と裾が極端に厚い。14.7で決定した「percentile rank採用・raw z-score不採用」の判断が正しかったことを裏付ける |
| `totalCash`/`totalDebt` | 99.4% | ネットキャッシュがマイナス(負債>現金)の銘柄が52.7%を占めた |
| `debtToEquity` | 83.1% | 19.5の単位の罠に注意 |
| `totalRevenue` | 83.7% | 14.8のカバレッジ下限(70%)を上回るが、100%ではない点は要考慮 |

**52.7%がネットキャッシュ・マイナス**という結果は、15.2で「単純な純キャッシュ・プラス条件ではなく、ランウェイ(現金÷バーンレート)ベースのゲートにする」という設計判断が正しかったことを裏付けている。ネットキャッシュを直接のゲート条件にしていたら、小型株候補の半数以上を機械的に落としてしまうところだった。

### 19.8 Altman Z''ハードゲートの撤回と自己資本ゲートへの置き換え(フェーズ2実装時に判明)

フェーズ2(除外ゲート)を実装し15.2のAltman Z''ハードゲート(Z''<1.1で除外)を実データに適用したところ、**370銘柄中126銘柄(34%)が除外**され、時価総額上限(117銘柄)を上回る最大の除外要因になった。除外銘柄を個別確認すると、大半は臨床段階バイオテック等、長年のR&D投資により利益剰余金が大幅な赤字になっている「正常な赤字成長企業」だった(例:ABEO — 総資産2.2億ドルに対し利益剰余金-7.4億ドル)。Altman Z''-Scoreは製造業向けに設計された指標であり、利益剰余金/総資産の項がR&D投資を続ける企業を構造的に不利に扱う。**このアプリが本来狙う10バガー候補(赤字グロース株)を、財務健全性ゲートが系統的に締め出していた。**

代替として「EBIT ÷ 支払利息 < 1」のインタレストカバレッジ・ゲートも検討したが、実データで採用を見送った。EBITが分子である以上Altman Z''と同じバイアスを再現し、**Axsome Therapeutics(AXSM)**——臨床段階から製品承認まで漕ぎ着けた実際の成功例——を「利払い不能」として誤って除外することを確認したためである(赤字期のEBITに起因)。

最終的に、Altman Z''はハードゲートから撤去し、フェーズ3のスコアリング(財務健全性サブスコアの程度評価)専用に格下げした。代わりに、R&Dバイアスを受けない**自己資本マイナス(債務超過)ゲート**を新設した。負債が増資による資金調達を上回って積み上がっている状態を直接見る指標であり、「戦略的な赤字」と「本当に危険な財務状態」を区別できる。

**個別検証**(現金水準を突き合わせて確認):

| 銘柄 | 現金 | 有利子負債 | 自己資本 | 所見 |
|---|---|---|---|---|
| BTMD | $11.2M | $128.6M | -$58.5M | 黒字企業だが債務がキャッシュの11倍 |
| BYSI | $6.5M | $0.17M | -$32.2M | 負債はほぼ無いが現金が極端に薄い |
| ESLA | $0.15M | $0 | -$10.4M | 現金がほぼ枯渇 |
| LESL | $45.9M | $1.02B | -$408.0M | PE主導の高レバレッジ小売企業 |

4銘柄ともAltman Zとは異なる「レバレッジ過多」「現金枯渇寸前」という、質の異なる本物の危険信号を正しく捉えていた。

**変更前後の比較**(同一の370銘柄サンプル、`quarterly_cash_flow`収集完了後):

| | 変更前(Altman Zゲート) | 変更後(自己資本+ランウェイゲート) |
|---|---|---|
| 合格銘柄数 | 34 | **48(+41%)** |
| 最大の除外理由 | `altman_z_floor`(126件、34%) | `market_cap_ceiling`(117件、本来あるべき最大要因に復帰) |
| 財務リスク系の内訳 | Z''のみ(126件、単一指標に依存) | `negative_equity`(33件)+ `cash_runway_floor`(58件、2種のリスクを分離) |
| ABEO・ABSI(赤字バイオテック) | 除外 | **通過** |
| AXSM(大型化した元バイオテック) | (未検証) | `market_cap_ceiling`で除外(**正しい理由**) |
| BTMD・ABEO等(実際に脆弱な銘柄) | Z''で除外 | `negative_equity`で除外(引き続き捕捉) |

**教訓**:財務健全性を単一の複合指標(Altman Z''のような)でゲートにすると、複合指標を構成する個々の要素のバイアスがそのままハードな除外判定に伝播する。**構成要素を分解し、より狭く直接的な意味を持つ指標(自己資本の符号、キャッシュランウェイ)に置き換える**ことで、同じ「財務的脆弱性の遮断」という目的を、対象外のバイアスなしに達成できた。この教訓は7章・15章の他のサブスコア設計(特に複合指標を使う箇所)にも適用可能。

### 19.9 並列度調整(collection.yaml)の効果検証

19.4で並列度を8→5、ジッタを0.5〜2.0秒→1.0〜3.0秒に調整した効果を、370銘柄の再収集で確認した。**transient_failure(レート制限)は0件**(旧設定では478銘柄処理時点で142件発生していた)。この設定変更が実際にYahoo側のレート制限を回避する効果を持つことを実データで確認できた。

---

# 【v1.5 追記】フェーズ3(スコアリングエンジン)実装と全銘柄実データ検証

## 20. スコアリングエンジンの実装スコープと全銘柄検証結果

### 20.1 v1スコープ:実装した7サブスコアと見送った3信号

7章改訂表・15.2の8サブスコアのうち、以下は新規のyfinanceエンドポイント収集が必要なため v1 では実装を見送った(既存の収集パイプライン(5章)に含まれていないため)。

| 見送った信号 | 必要なエンドポイント | 影響するサブスコア |
|---|---|---|
| 予想修正モメンタム(15.4-2) | `Ticker.eps_revisions` | 予想修正モメンタム(サブスコアごと見送り) |
| 決算サプライズ連続性(15.4-3) | `Ticker.earnings_dates` | 同上 |
| インサイダー売買(15.4-12の一部) | `Ticker.insider_transactions` | 発掘度(機関保有率・アナリスト数・インサイダー保有率のみで代替) |
| 12-1モメンタム(15.4-11) | 全銘柄分の3年価格ヒストリー(現状はごく一部のみバックフィル済み) | モメンタム(52週高値近接度のみで代替) |

「予想修正モメンタム」サブスコアを丸ごと除いた残り7サブスコアで重みを再配分し(`config/scoring.yaml`)、合計1.0になるようPydanticで検証している。モメンタム・発掘度は代替指標のみのため、他サブスコアよりやや低めの重み(6%・7%)にした。該当エンドポイントの収集を追加した時点で、14.6のバージョニング機構どおり`scoring_version`を上げて追加する設計にしてある。

### 20.2 実装した指標のyfinanceフィールドマッピング(実データで確定)

15.4で概念設計した指標を実装するにあたり、実際のyfinance払い出しフィールド名を実データで確認しながら確定した。特に以下は実装前の想定と異なっていたか、確認が必要だった点:

- **Altman Z''-Score**:`Working Capital`・`Retained Earnings`・`Stockholders Equity`・`Total Liabilities Net Minority Interest`(balance_sheet)、`EBIT`(income_stmt)がいずれも計算不要の完成フィールドとして直接提供されており、CA-CL等の手計算は不要だった
- **キャッシュランウェイ**:`Free Cash Flow`が`cash_flow`に直接提供されている。ただし**四半期粒度が必要**なため、既存の年次`cash_flow`だけでは代替できず、`quarterly_cash_flow`の収集を追加した(19.8で判明した経緯どおり)
- **ROIC**:`Invested Capital`(balance_sheet)・`Tax Rate For Calcs`(income_stmt)がいずれも直接提供されており、NOPAT算出に必要な実効税率を別途計算する必要がなかった
- **Piotroski F-Score**:「増資なし」の判定には`cash_flow`の`Common Stock Issuance`ではなく`balance_sheet`の`Ordinary Shares Number`(発行済株式数の期間比較)を採用した。前者は直近2期でNoneになるケースが実データで確認されたため、カバレッジの高い後者に切り替えた
- **「長期負債比率の低下」判定**:15.4の原案は`Long Term Debt`を想定していたが、実データでこのフィールドは多くの期でNoneだった。カバレッジの高い`Total Debt`に置き換えた

Piotroski F-Scoreは、9基準のうち算出できたものの充足率(0.0〜1.0)として実装した(9基準中6基準未満しか算出できない銘柄は判定不能扱い)。異なる欠損状況の銘柄間でも比較可能にするための設計判断。

### 20.3 タイムゾーン不整合バグの発見と修正

`apply_gates()`・`run_scoring()`等が使う「今日の日付」に`datetime.date.today()`(OSのローカルタイムゾーン依存)を使っていたが、本プロジェクトのPostgreSQLは`now()`/`current_date`がUTC基準である。開発機が日本時間(UTC+9)のため、**日本時間の深夜0時〜9時の間はローカル日付とUTC日付が1日ずれる**。

実際に、この時間帯に`apply-gates`を再実行して結果を確認しようとしたところ、実行のたびに集計結果が食い違うという現象が発生した。調査の結果、直前の実行がローカル日付(2026-08-24)のスナップショット行に書き込んでいたのに対し、確認用のSQLクエリは`current_date`(UTC、2026-08-23)を使っていたため、別の日付キーの新旧データを混同していたことが判明した。

`src/autoscreener/dates.py`に`utc_today()`を新設し、日付を扱う全箇所(6ファイル・9箇所)を置き換えて修正した。**8.4(デプロイ・運用)の観点でも重要な教訓**:デプロイ先のマシンのタイムゾーンは未確定であり、ローカルタイムゾーンへの暗黙の依存は、デプロイ地域によって「今日」の境界がずれるバグを生む。日付・時刻を扱うすべての箇所で、DBのUTC基準と一貫させることを今後の実装原則とする。

### 20.4 全銘柄(5,312銘柄)での実データ検証結果

タイムゾーン修正後、NASDAQ/NYSE候補5,312銘柄全件に対して収集→ゲート適用→スコアリングを実行した。

**収集結果**:success 3,745・invalid_data 1,033・empty_response 24・transient_failure 505(9.5%)・parse_failure 5。**サーキットブレーカーは作動せず**(19.9の並列度調整が本番規模でも有効であることを確認)。parse_failureの5銘柄(RPAY・ROMA・ROIV・ROK・ROG)は個別に再現テストしたところ再現せず、一時的なAPI不調と判断した(隔離リストには入らないため翌日の日次収集で自動再試行される)。

**ゲート適用結果**:768銘柄が全ゲートを通過。**4章の目標レンジ(米国800〜1,500銘柄)に収まることを確認**——19.2で370銘柄サンプルから外挿した約988銘柄という予測とは幾分差があるが、同じオーダーであり、外挿の方向性は妥当だったと言える。

**スコアリング結果**:754銘柄をスコアリング(14銘柄はカバレッジ70%未満で対象外)。セクター別のスコアリング対象数は Healthcare 186・Technology 177・Industrials 136 など主要セクターが14.7の最低サンプル数(30)を大きく上回る一方、**Utilities(8銘柄)は実際にセクター内サンプル不足のフォールバック(全ユニバースへの切り替え)が発動する状況を確認できた**。スコア上位はSEPN(82.5)・UROY(79.4)・MNTN(76.1)など、いずれもサブスコアのカバレッジ100%(7軸すべて算出可能)だった。

---

# 【v1.6 追記】日次自動実行とAPI実装

## 21. 日次自動実行(スケジューラ)とFastAPI実装

### 21.1 日次自動実行:Windows Task Schedulerでの実装

`batch/daily_pipeline.py` に、収集→ゲート適用→スコアリングを1回でまとめて実行するエントリポイントを実装した。ユニバース再取得(5.2:週次)は月曜日のみ実行する。Windows Task Schedulerに毎日09:00(JST)起動のタスク`AutoScreenerDailyPipeline`として登録した(`-WakeToRun`でスリープ中のマシンを起こす設定、実行タイムアウト3時間)。

**発見・修正したバグ**:スケジューラから呼び出すラッパーバッチファイル(`scripts/run_daily_pipeline.bat`)に日本語のコメントを含めたところ、cmd.exeのコードページ不整合でスクリプトが文字化けし、後続行がコマンドとして誤解釈されて即座に失敗した(`LastTaskResult: 1`)。コメントをすべて英語(ASCII)に書き換えて解決した。**バッチファイルは非ASCII文字に対して脆弱**という、Windows運用特有の実装上の罠であり、今後Windows向けスクリプトを追加する際はASCIIのみで書くことを原則とする。

また、ラッパーはパイプライン本体の実行前に `docker compose up -d --wait` を実行し、Docker Desktopの起動待ちを保証する設計にした(スケジューラ発火時にDocker Desktopがまだ起動中である可能性への対処)。

### 21.2 API実装:設計判断とその根拠

6.5の3エンドポイント(候補一覧・詳細・ユニバース状況)に加え、14.16(除外銘柄確認画面)を満たす検索エンドポイントを含めた計4エンドポイントを実装した。実装前に4つの設計判断を行った。

| 判断事項 | 採用案 | 根拠 |
|---|---|---|
| ゲート落ち銘柄を詳細取得した場合の応答 | **200 + `is_candidate: false` + `exclusion_reason`** | 404は「そもそも認識していない銘柄」専用に予約。「認識しているが候補から外れている」ことを正しく表現でき、14.16の要件に合致する |
| セクター内サンプル不足フォールバック(14.7)の透明化 | `scores.subscore_fallback`(JSONB)列を新設 | フォールバックは(指標, セクター)の組で決まり銘柄間で共有できるため、エンジン側の追加計算コストは小さい。実データでUtilitiesセクターがほぼ全サブスコアでフォールバックしていることを確認済み(20.4) |
| 除外銘柄の検索エンドポイント | `GET /api/v1/excluded?reason=...` を同時実装 | 既存の`universe_snapshots`への単純なフィルタ検索で新規ロジックが不要なため、後回しにするコストの方が高いと判断 |
| ページネーション | 既定50件・上限200件 | 現在の候補プール(754銘柄、4章の目標800〜1,500に近い規模)に対して妥当な既定値 |

`GET /api/v1/candidates/{ticker}` は404を「ティッカー文字列は正しい形式だが、そもそもuniverseに存在しない」場合のみに限定し、フォーマット自体が不正な場合(規定文字種・長さ超過)は422を返す(18.5)。この2つの応答コードの使い分けをテストで明示的に確認している。

### 21.3 実データ検証結果

FastAPI実装後、開発用サーバーを起動し、754銘柄の実データに対して4エンドポイントすべてを検証した。

- `GET /candidates?limit=5`:実際のスコア上位(SEPN・UROY・MNTN等)を正しい順位で返すことを確認
- `GET /candidates/ACGL`(19.6で修正した保険会社):`is_candidate: false`、`exclusion_reason: ["market_cap_ceiling", "revenue_ceiling", "excluded_sector"]` を正しく返すことを確認——19.6のバグ修正がAPI層でも正しく反映されている
- `GET /candidates/SEPN`(スコア最上位):`subscore_fallback`が全サブスコアで`false`(Healthcareセクターは186銘柄でサンプル充分)であることを確認
- `GET /universe/status`:直近の収集ジョブ統計・ゲート統計・スコアリング統計を正しく集計。ただし「直近の収集ジョブ」は`collection_logs.run_id`の最新のものを指すため、動作確認のための短時間テスト実行が本来の夜間全銘柄収集より後に行われた場合、そちらが「直近」として表示される(仕様通りの挙動だが、運用上は紛らわしくなりうる点として記録)
- `GET /excluded?reason=negative_equity`:463銘柄がヒットし、19.8で新設したゲートが除外理由として正しく記録されていることを確認

APIレイヤーのテスト(9件)は実際のDBに対してテスト専用ティッカー(`ZZTEST*`)を作成・削除する形で実装した(14.14のyfinance非依存方針とは異なり、DBには依存する。本プロジェクトの他の統合検証と同じ方針)。

---

# 【v1.7 追記】前方検証ジョブと運用移行チェックリスト

## 22. 前方検証ジョブ・DBバックアップ・API権限分離の実装

### 22.1 前方検証ジョブ(14.3)

`scoring/forward_validation.py` に、`scores`の各行に対し+1M/+3M/+6M/+1Y/+3Y/+5Y後の実現リターンを算出し`forward_returns`に記録するジョブを実装した。基準価格は14.1の定義どおり「スコア確定日の翌営業日始値」、終値は目標日以降で最初に観測できる値を使う。

- スコア確定から最短ホライズン(1M=30日)にも満たない行はSQLクエリの時点で除外し、無駄な走査を避ける
- 既に算出済み(`realized_return`が非NULL)の組み合わせは再計算しない(べき等)
- 日次パイプラインに組み込み、毎日自動実行されるようにした

**実データでの確認**:実装直後に実行したところ`computed: 0`(想定通り。スコア履歴が始まったばかりで、最短の1Mホライズンにすら遠く及ばないため)。今後、日次自動実行が蓄積することで自然に結果が出るようになる。

### 22.2 運用移行チェックリスト(18章)の実装状況

18.4・18.6・18.7で定義した項目のうち、以下を実装した。

| 項目 | 実装内容 |
|---|---|
| 18.4 バックアップ | `batch/backup.py`:`pg_dump`によるDB全体のバックアップを日次パイプラインの最後に実行し、gzip圧縮して`backups/`に保存(直近14日分をローテーション保持)。バックアップ失敗はパイプライン全体を失敗させない(当日のスコア計算を無駄にする理由にはならないため。ログには残す) |
| 18.6 DBユーザー分離 | `autoscreener_readonly`ロールを新設し、SELECTのみ付与(`scripts/create_readonly_role.sql`)。APIレイヤー(`api/dependencies.py`)はバッチ層と別のエンジン・セッションを持ち、`API_DATABASE_URL`(未設定時は`DATABASE_URL`にフォールバック)経由でこのロールに接続する。実際にINSERTが拒否されることを確認済み |
| 18.7 監視閾値 | `monitoring.py`:収集成功率(警告95%未満・緊急90%未満)、隔離リスト比率(警告5%超・緊急10%超)をログのWARNING/ERRORレベルで出力する仕組みを実装し、日次パイプラインに組み込んだ。個人利用規模(11.1解釈A)のため通知はログ出力に留め、メール・デスクトップ通知等への拡張は将来課題とする |

**今回実装しなかった項目(意図的な先送り)**:

| 項目 | 理由 |
|---|---|
| 依存パッケージの脆弱性スキャン(14.14) | CI/CD基盤(GitHub Actions等のリモートリポジトリ連携)が未整備のため、スキャンを自動実行する仕組み自体がまだ無い。個人利用規模での優先度は相対的に低い |
| APIレート制限(18.5) | 現状は個人利用の単一ユーザーのみが呼び出す前提のため、緊急性は低い。外部公開する段階(11.1解釈B/C)で必須の実装として再検討する |

これらは11.1解釈A(商用品質・個人利用)の範囲では許容できるリスクと判断したが、外部公開する場合は必須の対応として11.1の再検討事項に紐づけておく。

---

# 【v1.8 追記】Reactフロントエンド実装

## 23. フロントエンド実装と実データ検証

### 23.1 実装内容

3章・6.4の設計どおり、Vite + React + TypeScriptでフロントエンドを実装した(`frontend/`)。

| 画面 | 内容 |
|---|---|
| ランキング一覧 | セクター・時価総額帯でのフィルタ、ページネーション(50件区切り)、スコア降順表示 |
| 銘柄詳細 | サブスコア内訳(14.7のフォールバック発動時は「全ユニバース基準」バッジを表示)、スコア推移チャート(recharts)、ゲート除外時は理由を表示(21.2の200+exclusion_reason設計をそのままUIに反映) |
| 除外銘柄確認 | 14.16向け。除外理由でのフィルタ検索 |
| 順位変動 | 直近2つのスコア確定日を比較し、新規ランクイン・急上昇銘柄を表示。この機能のため`GET /candidates`に`date`パラメータ、`GET /scores/dates`エンドポイントを追加した |

各画面に14.12/14.16の免責文言(「投資助言ではない」)を常時表示している。

### 23.2 実データでの動作確認(Playwright)

`chromium-cli`が環境になかったため、Playwrightで簡易ドライバを書いてヘッドレスChromiumから実際に操作した(スクリーンショット取得・コンソールエラー確認込み)。

- **ランキング一覧**:実データどおりSEPN(スコア82.5)が1位、754件中50件表示、ページネーション表示も正しい(「1〜50/754件」)
- **銘柄詳細**:SEPNのサブスコア7件すべて表示、スコア推移チャートが2026-08-23〜08-24の実データで描画されることを確認
- **除外銘柄確認**:`reason=negative_equity`でフィルタし、表示された全行が実際に`negative_equity`を含むことを確認(誤フィルタなし)
- **順位変動**:偶然にも本日中に複数回パイプラインを実行していたため、2026-08-23→08-24の実際の順位変動(SGML:483位→472位など)が表示された。まだ日次自動実行が1日分しか経過していない段階でも、本機能自体は正しく動作することを確認できた
- コンソールエラーは0件

### 23.3 実装上の判断

- **CORS**:個人利用(11.1解釈A)を前提に、ローカル開発サーバー(`localhost:5173` / `127.0.0.1:5173`)からの呼び出しのみを許可する設定にした。当初は GET のみ許可していたが、LLM接続プロファイル管理(`/api/v1/llm/connections*`)とブラウザからのレポート生成(`POST /api/v1/llm/report/generate`)を追加した際、書き込み系がプリフライトで `400 Disallowed CORS method` になったため `POST`/`PUT`/`DELETE`/`OPTIONS` も許可(`allow_methods`、`api/main.py`)。書き込み系は `Content-Type: application/json` 必須で単純フォーム CSRF はプリフライトで弾かれる。外部公開時は認証必須(現状は無認証)
- **セクターフィルタの実装方式**:専用の`/sectors`一覧エンドポイントを新設する代わりに自由入力のテキストフィルタとした。エンドポイント数を絞り、必要になった時点で追加する判断
- **順位変動の実装**:全候補(最大768件、`_MAX_LIMIT`=200を超える)を比較するため、フロントエンド側でページネーションをループして全件取得してから差分計算する設計にした。将来ユニバースが更に拡大した場合はサーバー側での差分計算エンドポイントへの切り出しを検討する

---

# 【v1.9 追記】スコアリングエンジンv2拡張

## 24. 予想修正モメンタム・12-1モメンタム・インサイダー買い越しの実装

### 24.1 実装内容

20.1で見送っていた3信号を実装し、`scoring_version`を`"v2"`に上げた(14.6のバージョニング機構どおり、v1のスコア履歴は上書きせず別系列として残る)。

| 信号 | 関数 | 使用サブスコア |
|---|---|---|
| 予想修正モメンタム(15.4-2) | `eps_revision_momentum` | revision_momentum(新設) |
| 決算サプライズ連続性(15.4-3) | `earnings_surprise_consistency` | revision_momentum(新設) |
| インサイダー買い越し(15.4-12) | `insider_net_buying_ratio` | discovery(既存に追加) |
| 12-1モメンタム(15.4-11) | `momentum_12_1` | momentum(既存に追加) |

重みは15.2改訂表の原案比率(25:15:15:10:15:10:10:10、合計110%)を1.0に正規化し直した(v1で行っていた「未実装分の割引」補正は不要になったため撤廃)。

### 24.2 実装時に実データで判明した2件

- **`earnings_dates`取得には`lxml`パッケージが必要**:yfinance内部がHTML解析に`pandas.read_html`を使っており、`lxml`が入っていないと`ModuleNotFoundError`で失敗する。`pyproject.toml`に追加した。この依存が漏れていたことに気づかず本番相当の収集を流していたら、`earnings_dates`だけ全銘柄で欠損するところだった
- **`insider_transactions`の`Transaction`列は常に空文字列**:15.4-12の設計時点では`Transaction`列で売買種別を判定する想定だったが、実データでは全行が空。実際の売買種別は`Text`列("Sale at price...", "Purchase at price...", "Stock Award(Grant) at...", "Stock Gift at..."等)の先頭語から判定する必要があった。増資性のイベント(Award/Gift/デリバティブ行使)は売買ではないため買い越し比率の計算対象から除外している

### 24.3 補助シグナルの障害分離

`eps_revisions`・`earnings_dates`・`insider_transactions`は、取得できなくても銘柄収集全体を失敗にしない設計にした(`_safe_fetch`ヘルパー)。財務諸表(`info`・`income_stmt`等)と異なり、これらは「無くても他のサブスコアで代替可能な補助シグナル」であるため、18.1の「個別失敗が全体を止めない」という原則をフィールドレベルにも適用した。

### 24.4 実データ検証(v2、部分データでのスモークテスト)

新フィールド収集前の時点(大半のraw_snapshotsが旧形式)でv2設定を適用したところ、クラッシュせず正しく縮退することを確認した。SEPN(v1では総合スコア82.5)のv2スコアは、`revision_momentum`が未収集のため`null`、カバレッジ0.910(=1.0−0.09の重み)、総合スコア75.40(カバレッジの平方根ペナルティが反映)——設計どおりの縮退。

全銘柄再収集(新フィールド収集)と全銘柄価格バックフィル(12-1モメンタム用)を実行中。完了後の全銘柄実データ結果は追って追記する。

### 24.5 全銘柄再収集の待機中に発見した2件の追加問題

再収集の実行中(待機時間を使った実装レビュー)に、実装と要件定義書の記述との間の乖離を2件発見し、修正した。

**(1) 14.8の必須軸チェックが未実装だった**:「成長軸と財務健全性軸は算出可能であることを必須とする」と14.8に明記していたが、`run_scoring`の実装は全体カバレッジ率(70%以上)のみをチェックしており、この2軸個別の必須チェックが漏れていた。理論上、この2軸が両方欠損していても他の軸だけで70%を満たせば通過してしまう抜け穴だった。`_passes_coverage_requirements`として実装し、ユニットテスト4件を追加して修正した。

**(2) HTTP 401「Invalid Crumb」エラーの分類漏れ**:本日中に複数回の全銘柄収集を実施した累積リクエスト量により、Yahoo側のセッション認証トークンが一時的に無効化される事象(`HTTP Error 401: Invalid Crumb`)を実データで新たに観測した。`classify_exception`(18.1)は401を想定しておらず、`TransientFailure`として自動リトライされない`ParseFailure`(またはその手前で発生する`TypeError`起因の分類不能例外)として扱われていた。429/5xxと同様に401をリトライ対象に追加した。この事象は**累積リクエスト量への一時的な負荷起因**であり、11章の「yfinance側の仕様変更リスク」とは性質が異なる新しいリスク要因として記録する——1日に複数回・大規模な収集を実行する運用(開発中の検証等)は、通常の日次1回運用より401に遭遇しやすい。

**検証したが実害がなかった懸念**:12-1モメンタム(`momentum_12_1`)が使う`price_snapshots.close`が13.4と同種の分割未調整の罠を持つのではないかと疑い、CELHの実データ(2023-11-15の3:1分割)で分割日をまたいだ価格を確認したが、**価格は既に分割調整済みで不連続はなかった**(`get_shares_full`の株式数とは異なり、`history()`の価格系列はyfinance側で分割調整済みとして返される)。修正不要と判断した。

### 24.6 重大な実装漏れの発見:「上場後最低4四半期」ゲートが未実装だった

再収集の待機中に行ったシステム全体のレビューで、4章・6.2で明記していた「上場後最低4四半期分の決算データがあること」という要件が、**実装のどこにも反映されていなかった**ことが判明した。

- `UniverseConfig.min_listed_quarters`(設定値)は読み込まれ・検証されるだけで、`evaluate_gates`から一度も参照されていなかった
- `Ticker.listed_date`列はスキーマに存在するが、収集コードのどこからも代入されておらず、常にNULLのままだった

`screening/exclusion_gates.py`に`count_available_quarters`(quarterly_income_stmtの"Total Revenue"行で非None期数を数える)を実装し、`min_listed_quarters`未満なら`insufficient_listing_history`として除外するゲートを追加した。上場日そのものではなく実際に取得できた決算データの期数を基準にしたのは、13.1で判明した「yfinance側の開示期数は上場期間に関わらずばらつく」という実態に、より忠実に対応するため。

**実データでの影響**:収集済み4,644銘柄に適用したところ、**927銘柄がこのゲートで除外された**(AEHL・BANL・LZM等、`Total Revenue`が0期しか取得できていない銘柄を含む)。これは決して稀なエッジケースではなく、実装漏れが放置されていれば相当数の「決算データが実質存在しない銘柄」が候補プールに紛れ込み続けていたことを意味する。ゲート適用後の合格銘柄数は768→786に(母集団拡大の影響も含む)、他のゲートとのAND条件により純減とはならなかったが、**個別の除外理由としては最大級の規模**だった。

### 24.7 追加レビューで発見したさらに2件の乖離

同じレビューを続け、設定値・状態フィールドが実装のどこからも参照されていないケースを体系的に洗い出したところ、さらに2件見つかった。

**(1) `min_workers`が死んだ設定値だった**:`CollectionConfig.min_workers`は読み込まれ・バリデーション(`max_workers >= min_workers`)には使われるが、実際の並列実行(`ThreadPoolExecutor(max_workers=...)`)には`max_workers`しか渡していなかった。「並列度3〜8で可変」という設定コメントの記述は誤りで、実際は常に`max_workers`固定(現在5)で動作していた。動的スケーリングを新たに実装するのではなく、**存在しない機能を示唆する設定項目を撤去する**方針を選び、`min_workers`をモデル・YAML・テストから削除した。

**(2) `delisted_at`が一度設定されると永久にクリアされなかった**:`Ticker.delisted_at`はPermanentFailure発生時に設定される一方、それを解除するコードがどこにも無かった。このため、(a) 誤ってPermanentFailureと分類された銘柄(一時的な事象の誤判定)や、(b) 14.5で懸念していた「廃止されたティッカーが別会社に再利用される」ケースで、その後正常にデータが取得できるようになっても、`apply_gates.py`の対象銘柄クエリ(`delisted_at.is_(None)`)から**永久に除外され続ける**バグがあった。`collect_one`の成功パスで`delisted_at`が設定されていた場合はクリアするよう修正し、ティッカー再利用の可能性を示す警告ログを残すようにした。

**未着手のまま残る関連課題**:14.5で設計した銘柄同一性管理(ISINベースでの同一性検証、`ticker_aliases`テーブルでの改名履歴管理)は、スキーマ(`Ticker.isin`列・`TickerAlias`テーブル)は存在するが、`Ticker.get_isin()`の呼び出しも`TickerAlias`行の作成も一切実装されていない。(2)の修正は「ティッカー再利用が起きても検知不能なまま古いデータに新データが上書きされ続ける」という最悪の事態は防ぐが、「新旧2社のデータが同一`ticker_id`に混在する」リスク自体は解消していない。現状、この機能が必要になるほどティッカー再利用が実際に発生した形跡は無い(実データ検証の範囲内)ため、フェーズ3以降の課題として明記するに留め、今回は実装しない。

---
---

# 【v2.0 追記】評価ロジック全面レビューと11件の修正

## 25. 計算ロジック・銘柄評価方法の妥当性検証(2026-08-24)

「実装した計算ロジック・銘柄の評価方法が妥当か」という観点でアプリ全体を通しレビューし、**評価結果を実際に歪めていた不具合を7件、整合性・堅牢性の問題を4件**修正した。すべて実データ(収集済み5,312銘柄)で再実行して確認済み。

### 25.1 評価を歪めていた指標ロジックの誤り(5件)

| # | 箇所 | 症状 | 修正 |
|---|---|---|---|
| 1 | `momentum_12_1` | 15.4-11の**12-1モメンタムになっていなかった**。「12ヶ月リターン − 直近1ヶ月リターン」を文字どおり引き算していたが、展開すると `(1 + 直近1ヶ月リターン) × 本来の12-1モメンタム` であり、除外したはずの直近1ヶ月が**係数として掛かる**。11ヶ月で+100%上げ直近1ヶ月で全戻しした銘柄は、正しくは+100%のところ**−50%**と評価されていた(短期反転の回避という設計意図と真逆) | 学術定義どおり「12ヶ月前→1ヶ月前」のリターンに変更 |
| 2 | `growth_stability_cv` | 平均成長率の**絶対値**で割っていたため、**毎年安定して減収している銘柄の変動係数がほぼ0**になり、成長軸で最上位のパーセンタイルを獲得していた(15.4-14の意図と正反対) | 平均成長率が0以下なら「安定成長」ではないので最下位(`inf`)扱い |
| 3 | `ev_to_sales_over_growth` | 成長率が0以下だと`None`(=判定不能)を返していたため、**非成長企業だけがこの指標の減点を免れる**(欠損は重み再配分で免除されるため)。15.3の「成長しない万年割安株を拾わない」と逆の挙動 | 減収・横ばいは最下位(`inf`)扱いに変更 |
| 4 | `ev_to_sales_over_growth` | `revenueGrowth`が基数効果で異常値になる銘柄(実データ:SEPN = **22,375%**)で、分母が巨大化して比率がゼロに潰れ、EV/Salesがどれだけ高くても**バリュエーション軸で自動的に最上位**になっていた | 分母の成長率に上限(+200%)を設定。超高成長銘柄同士は分母が揃い、順位がEV/Salesの実際の高低で決まる。SEPNのバリュエーションは97.1→74.2、総合1位→2位に是正 |
| 5 | `compute_dilution_3y_cagr` | 指数が`1/3`固定だった。実際に渡す観測窓は「price_snapshotsの最古〜最新」であり、**日次収集が続くほど窓は4年・5年と伸びる**。固定3年で年率換算すると希薄化率を過大評価し、`dilution_ceiling`で正常な銘柄を誤除外していく(時間とともに悪化する種類のバグ) | 実測の経過年数で年率換算する`compute_dilution_cagr`へ。あわせて、株式数が`None`の先頭行を基準にして指標が丸ごと欠損していた問題も`dilution_cagr_from_observations`で解消(非Noneの観測点だけを使う) |

### 25.2 データ整合性の欠陥(3件)

**(1) 日次経路にだけ残っていた13.4の分割未調整の罠**:バックフィル(`fetch_price_and_shares_history`)は取得時点で分割調整済みの系列を保存するが、日次収集で1日ずつ積み上げた`price_snapshots`の行は**その日時点の単位**のままDBに残る。分割が発生した瞬間、過去行だけが分割前の単位で取り残され、12-1モメンタムが「3:1分割で−67%の暴落」を、希薄化CAGRが「株式数3倍」を誤検知する。13.4は`get_shares_full`について同じ罠を指摘していたが、**日次で積み上げる価格・株式数系列そのものが同じ罠を持つ**ことは見落とされていた。

`history()`が返す"Stock Splits"列(追加リクエスト不要)で分割を検知し、分割日より前の蓄積済み行を遡って調整するようにした(価格は÷倍率、出来高・株式数は×倍率)。「分割日以降の行がまだ1件も無い」ことを条件にすることで、同日中の再実行でも二重適用されない(18.3)。あわせて`backfill_history`が既存日付を単にスキップしていたのを上書き更新に変え、再バックフィルが自己修復として機能するようにした。

**(2) 検証結果(`validation_errors`)が更新されず、古い判定が使われ続けていた**:`collect_one`は`content_hash`が変わらない限り新しい`raw_snapshots`行を作らない(14.11)。財務データは四半期に一度しか変わらないため、**検証ロジックを直しても古い判定が何ヶ月も残り続ける**。計算時のサニタイズ(`sanitize_info`)は保存済みエラーではなく常に現行ロジックで再導出するようにし、内容が同じ場合でも`is_valid`/`validation_errors`は毎回上書きするようにした。

**(3) `market_cap ≒ 株価 × 発行済株式数`の突合閾値が厳しすぎた**:コメントには「桁違いのみを異常とみなす」と書かれていたが、実装値は乖離率0.5(±50%)。yfinanceの`sharesOutstanding`は複数種類株(デュアルクラス)の1クラス分しか返さないことがある一方`marketCap`は全クラス合算のため、正常な銘柄でも2倍前後の乖離が普通に起きる。`sanitize_info`がこの検知で`marketCap`をNone化し、`evaluate_gates`が`missing_market_cap`として除外するため、**創業者主導のデュアルクラス小型株(10バガーの典型例)がユニバースから丸ごと消えていた**(実データで260銘柄が該当)。コメントどおり3倍以上の食い違いのみを異常とする基準に修正した結果、ゲート通過銘柄は695→711に増えた。

### 25.3 パイプライン・API間の不整合(3件)

**(1) `run_scoring`が「最新」のuniverse_snapshotsを見ていた**:スコアリング対象を`universe_snapshots`の**最新日**から取っていたため、当日の`apply_gates`が走っていない・失敗した場合に前日のゲート結果で当日付のScore行を書いてしまう。API(`GET /candidates`)は`snapshot_date == score_date`で突合するため、**書かれたスコアが1件も表示されない**(原因の分かりにくい空ランキング)という不整合になっていた。`score_date`当日のゲート結果に限定し、無ければ何も書かずにエラーログを出す。

**(2) 週次ユニバース更新のスタブ行が`included=True`だった**:`refresh_universe`は「ゲート適用前の広い候補プール」を`included=True`で記録していた。日次パイプラインは直後に`apply_gates`を走らせる前提だったが、それが失敗した日や`collect-universe`を単独実行した場合、**ゲート未評価の全候補5,312銘柄が「合格」として残り`run_scoring`の対象になる**。既定値を安全側(`included=False` / `pending_gate_evaluation`)に変更した。あわせて、`apply_gates`が`delisted_at`付きの銘柄をクエリから除外していたため同じスタブ行が上書きされずに残る抜け穴も塞いだ(明示的に`delisted`として`included=False`を書く)。

**(3) 陳腐化したScore行と`universe/status`の件数不一致**:同日中に`apply-gates`→`run-scoring`を複数回回すと、対象外になった銘柄の古いScore行が残り、`GET /universe/status`の`scored`(生のScore行数)がランキング件数と食い違っていた(実データで708 vs 703)。`run_scoring`が同一(日付, バージョン)の陳腐化行を削除するようにし、`universe/status`もランキングと同じ集合(その日included=True)を数えるようにした。`GET /scores/dates`も現行`scoring_version`で絞る(絞らないと順位変動画面が旧バージョンしかない日を比較対象に選び、全銘柄「前回順位なし」になる)。

### 25.4 堅牢性の小修正

- `revenue_cagr_years`:決算期末が2月29日の銘柄で`date.replace(year=...)`が`ValueError`になる/売上が負の異常値で分数乗が**複素数**を返し後続のパーセンタイル比較が`TypeError`で落ちる、の2つのエッジケースを塞いだ(18.2)
- `_sorted_points`:`"YYYY-MM-DD"`以外のキーが payload に紛れ込んだ場合、1銘柄のせいで`run_scoring`全体が落ちていたのをスキップに変更(18.1)
- `earnings_surprise_consistency`:yfinance側の並び順(新しい順)に暗黙依存していたのを、`date`で明示的に降順ソートしてから直近4件を取るように変更。並び順が変われば**最も古い4件**を見て指標が逆向きに壊れるが、それを検知できない実装だった(11章)
- `compute_cash_runway_quarters`:直近4期を切り出してからNoneを捨てていたため、最新4期がyfinance側の欠測というだけでランウェイ判定不能になっていた。「非Noneの直近4期」を取るように変更
- `run_forward_validation`:基準価格・決済価格の探索が無制限に後ろ倒しできたため、収集が途切れた銘柄で**まったく別の時期の価格**を採用して検証資産(14.3)を汚染しうる。エントリー7日・エグジット10日の上限を設けた

### 25.5 実データでの再検証結果(2026-08-24)

| 指標 | 修正前 | 修正後 |
|---|---|---|
| ゲート通過 | 695 | **711**(marketCap誤除外の是正分) |
| スコアリング対象 | 703 | 703(うちカバレッジ不足で8銘柄が対象外) |
| ランキング総数 vs `universe/status`の`scored` | 703 vs 708(不一致) | **703 vs 703** |
| 総合1位 | SEPN 75.9(バリュエーション97.1は基数効果由来) | **ACTG 73.7**(SEPNは73.1で2位) |

ユニットテストは154件→164件に増やした(修正した各ロジックに回帰テストを追加。分割の遡及調整はべき等性まで含めて実データ相当のDBで検証)。

### 25.6 今回は修正しなかった既知の論点

- **14.5の銘柄同一性管理**(24.7末尾の記載どおり)は引き続き未実装
- **サブスコア内の指標欠損を「最下位」に倒すか「縮小」で扱うかの方針**:今回は「欠損か、明確に悪いかが判別できる」ケース(25.1の#2・#3)だけを最下位扱いにし、それ以外は25.7の縮小推定に委ねた。どちらが前方検証上有効かは、14.3のデータが貯まってから再検討する

> 当初ここに「`revenueGrowth`の基数効果は成長軸には残るが、`growth_stability_cv`・粗利成長で相殺されるため手を入れない」と書いていたが、実データのSEPNではその相殺役の2指標がいずれも算出不能で、基数効果だけが残る構造になっていた。25.7で対処済み。

### 25.7 サブスコアの「指標カバレッジ」と基数効果(SEPNの実例からの追加修正)

25章の修正後、総合スコア上位に残っていたSEPN(Septerna、臨床段階バイオテック)のレビューで、**評価方法として妥当でない点がさらに2件**見つかった。いずれも実装ミスではなく**設計上の穴**であり、v1.9までのすべてのスコアに影響していた。

**発見のきっかけ**:SEPNの成長の質・持続性サブスコアは `99.0`(ほぼ最上位)だったが、その内訳を見ると4指標のうち3指標が算出不能で、**TTM売上成長率1本だけ**で付いたスコアだった。しかもその1本が `revenueGrowth = 223.756`(= **+22,375.6%**)という値だった。

#### (1) サブスコア内の指標カバレッジが総合スコアに一切反映されていなかった

14.8のカバレッジ再配分は「**どのサブスコアが**算出できたか」という粒度でしか働かず、「そのサブスコアの中で**何本の指標**が使えたか」は総合スコアにもUIにも現れなかった。結果として、

- 4指標すべてで裏付けられた成長スコア99
- 1指標だけで付いた成長スコア99

が同じ確からしさで扱われ、SEPNの `coverage_ratio` は満点の **1.00** と報告されていた。

さらに悪いことに、**欠損はランダムではない**。SEPNの場合、3年売上CAGRは3年前の売上が$0のため算出不能、成長安定性は使えるYoYが2期分しかなく算出不能、粗利成長は`Gross Profit`がyfinance側でnull。つまり**売上履歴が短く不安定な銘柄ほど、厳しい指標が落ちて緩い指標だけが残る**という選択バイアスが働く。

**修正**:サブスコアを「算出できた指標の割合の平方根」の分だけ中立値50へ縮小する(`_shrink_toward_neutral`)。総合スコアで既に使っている `× sqrt(カバレッジ率)` と同じ形を1段下の粒度にも適用したもので、情報が少ないほど推定を中立へ寄せる縮小推定(shrinkage)にあたる。高い側・低い側に対称に効く(1本だけの悪い数字も、1本だけの良い数字と同じく弱い証拠として扱う)。

あわせて `scores.subscore_metric_coverage`(JSONB)を追加し、銘柄詳細画面に「指標 n/m で算出」バッジを出すようにした(21.2で `subscore_fallback` を追加したのと同じ、透明化のための列)。

**修正前**の実データでの分布(ゲート通過711銘柄のうち、指標カバレッジが1.0未満だったもの):

| サブスコア | 1指標のみ | 一部欠損あり |
|---|---|---|
| 成長の質・持続性(4指標) | 7銘柄 | 72銘柄 |
| 1株価値の保全(3指標) | 47銘柄 | 336銘柄 |
| 予想修正モメンタム(2指標) | 99銘柄 | 99銘柄 |
| 発掘度(4指標) | 0銘柄 | 308銘柄 |

「1指標のみ」の7銘柄(GLUE・RNA・SEPN・TRDA・VTVT・BIOA・CDNL)はすべて臨床段階バイオテックであり、個別のエッジケースではなく**特定の銘柄種別に系統的に効く穴**だった。

#### (2) TTM売上成長率の基数効果

`revenueGrowth = 223.756` は「前年同期の224倍」を意味しており、値そのものは正しい。だが内訳は **前年同期TTM売上 $439,953 → 直近TTM $98,882,000** であり、これは事業の成長率ではなく、実質売上ゼロの会社に提携・マイルストン収入が一括計上されたことによる**基数効果**である(Septernaは製品売上を持たない臨床段階企業)。15.3が「増資・買収による見かけの成長を拾う」ことを問題にしたのと同じ種類の誤りが、比率の**分母**側から入り込んでいた。

実データ(ゲート通過711銘柄)の分布は次のとおりで、`revenueGrowth` の中央値は+8.5%、99パーセンタイルでも+378%。上端には明確な断絶がある:

| 銘柄 | 前年同期TTM | 直近TTM | 成長率 | 判定 |
|---|---|---|---|---|
| ARVN | $28.4M | $316.7M | +1,015% | 基数に実体あり |
| VIR | $1.5M | $303.2M | +19,583% | 基数効果 |
| SEPN | $0.44M | $98.9M | +22,376% | 基数効果 |
| FDMT | $0.37M | $92.0M | +25,107% | 基数効果 |

**修正**:前年同期売上が直近TTMの**1%未満**(= 成長率が+9,900%超、売上が100倍超)の場合、「成長率が高い」ではなく「**成長率を測れない**」として`None`を返す(`revenue_yoy_ttm`)。`growth_quality`・`rule_of_40`・`ev_to_sales_over_growth` の3箇所が同じ関数を経由する。

**閾値を金額ではなく倍率にした理由**:当初は「前年同期TTM売上 < $1M なら算出不能」という絶対額で実装したが、実データに **LWLG**(前年同期 $196K → 直近 $250K、**+28%**)のような「基数も現在も小さいが比率は正常」という銘柄が存在し、絶対額での足切りは**正常な数値まで捨てて**しまうことが分かった。基数効果かどうかを決めるのは基数の絶対額ではなく「基数が現在に対して無視できるか」であり、倍率判定はスケールフリーで、閾値を金額で決め打ちする必要もない。観測された分布でも、10倍前後(ARVN)と200倍前後(VIR/SEPN/FDMT)の間に1桁分の空白があり、100倍はその中間にあたる。

#### 実データでの結果(2026-08-24)

| 銘柄 | 修正前 | 修正後 |
|---|---|---|
| SEPN | 総合73.1(2位)、成長99.0(1指標のみ)、カバレッジ1.00 | **ランキング対象外**(成長軸4指標すべて算出不能 → 14.8の必須軸チェックで除外) |
| FDMT | — | 総合34.0、成長61.1(指標2/4で算出) |
| VIR | — | 総合26.2、成長18.7(指標3/4で算出) |
| ARVN | — | 総合52.0、+1,015%は成長率として維持(基数$28.4Mに実体あり) |
| LWLG | — | 総合39.1、+27.9%を維持(小さいが比率は正常) |
| ランキング総数 | 703 | 702(カバレッジ不足で対象外9銘柄) |
| 総合1位 | ACTG 72.9 | ACTG 72.9(変わらず) |

SEPNのように「ゲートは通過するがスコアを算出できない」銘柄は、15.5で構想している Tier 2(監視対象)に置くのが本来の扱いであり、その実装は引き続き未着手のまま残る。

### 25.8 上位ランクイン銘柄を起点にした指標の妥当性検証(追加3件の修正)

25.7の修正後、**上位10銘柄のサブスコア内訳を1指標ずつ実データで確認**したところ、さらに3件の不備が見つかった。いずれも「上位に来ている銘柄でこそ効いている」種類の問題で、ランキングの並びを実際に変えた。

#### (1) 希薄化指標が71銘柄で欠測しており、スコア5位の銘柄を含んでいた

15.1④が「単独で最大の改善余地」とした希薄化率が、ゲート通過711銘柄のうち**71銘柄で算出できていなかった**。原因は`get_shares_full`の観測が不定期であること——3年分の価格行に対して株式数が100件程度しか付かない銘柄があり、「非Noneの最古観測」が2年以内に収まって`MIN_DILUTION_WINDOW_YEARS`で弾かれていた。

**当時のスコア5位 MAKO(Mako Mining)がまさにこれで**、実際には発行済株式数が3年で 65,743,000 → 87,009,006(**年+9.8%の希薄化**)だったにもかかわらず、その減点を一切受けていなかった。上位10銘柄のうち3銘柄(RHLD・MAKO・MNTN)がこの欠測に該当していた。

**修正**:`balance_sheet`の`Ordinary Shares Number`(年次)へのフォールバックを追加した(`dilution_cagr`)。この系列は既に収集済みで、Piotroski基準7でも使っている。71銘柄中**64銘柄**を回復できる。

**分割調整の検証**:13.4の罠(`get_shares_full`は分割未調整)が再発しないよう、この系列が分割調整済みかを実データで確認した。**PRE**(2023-11-14に1:15併合)・**AEVA**(2024-03-19に1:5併合)いずれも、併合を挟んでOSN系列が不連続にならず滑らかに推移している=**Yahoo側で遡及修正済み**。13.4で必要だった手動の分割調整はこの系列には不要である。なお、併合後に大量発行した銘柄(AVX:29株→7,744,900株など)では比率が極端になるが、これは遡及修正の副作用ではなく「併合してから希薄化した」という経済実態そのものであり、除外は正しい。

両方の系列が算出できる640銘柄で突き合わせたところ、**差の中央値は0.58pt・90%点で4.91pt**と概ね一致した。20pt超の乖離は9銘柄で、その内訳は下記(3)の既知の限界に記載。

**影響**:`dilution_ceiling`による除外が645→788銘柄(+143)。ゲート通過711→704銘柄。

#### (2) アナリストカバレッジの欠損を「不明」として捨てていた

`numberOfAnalystOpinions` が欠損の場合、発掘度サブスコアからこの指標を落としていた。しかしyfinanceは**アナリスト予想が1件も存在しない銘柄でこのフィールドを返さない**ため、欠損の大半は「人数が不明」ではなく「**カバレッジがゼロ**」である。

ゼロは発掘度として最良のシグナルであり、それを欠損として捨てるのは「カバレッジの薄い銘柄の非効率性を拾う」(15.6)という本アプリの根本前提と正面から矛盾する。実データでは該当41銘柄のうち**38銘柄が他のアナリスト系フィールド(`targetMeanPrice`・`recommendationKey`等)も一切持たず**、しかも**上位10銘柄のうち5銘柄(RHLD・MAKO・ITRN・FLXS・PAYS)が該当**していた。

**修正**:他のアナリスト系フィールドも無い場合は `0.0`(カバレッジゼロ)として扱う。他のフィールドが存在する3銘柄(ZUMZ・SIGA・SRI)は「カバーされているが人数不明」なので従来どおり判定不能とする。RHLDの発掘度は80.1→87.7、MAKOは52.7→65.6になった。

#### (3) EV倍率の分母だけが最大12ヶ月古かった

`ev_to_sales_over_growth`・`ev_to_gross_profit` の分母に直近**年次**の売上・粗利を使っていたが、分子のEVは常に現在時点の値であり、**分母だけが最大12ヶ月古い**という不整合があった。売上規模ゲート(`apply_gates`)は既に`info.totalRevenue`(TTM)を使っており、そちらとも食い違っていた。

実データでは`info.totalRevenue`が直近年次の1.2倍超の銘柄が**61銘柄(8.6%)**、最大12.9倍(ABUS)。**成長が速い銘柄ほど分母が古くなりEV/Salesが過大=割高に見える**という、本アプリの狙いと逆向きのバイアスになっていた。

**修正**:TTM値(`info.totalRevenue`・`info.grossProfits`)を優先し、無ければ年次にフォールバックする(`_ttm_or_annual`)。`info.grossProfits`は直近年次粗利との比の中央値1.051(10-90%点 0.89-1.51)で整合を確認済み。

**採用しなかったもの**:同じ発想で`rule_of_40`のFCFマージンも`info.freeCashflow`(TTM)に揃えることを検討したが、直近年次FCFとの比が**中央値0.808・10-90%点 -0.76〜2.65**と定義が食い違っており、指標にノイズを持ち込むだけと判断した。FCFマージンは年次/年次のままとする(TTM成長率と年次マージンが混在する点は下記の限界に記載)。

#### 修正後のランキング(2026-08-24)

| 順位 | 修正前 | 修正後 | 備考 |
|---|---|---|---|
| 1 | ACTG 72.9 | **ACTG 71.2** | バリュエーション96.7→84.3(TTM粗利で再計算) |
| 2 | HRMY 70.6 | **HRMY 70.8** | |
| 3 | RHLD 68.1 | **RHLD 70.1** | 希薄化+0.1%を回復、カバレッジ0人を反映 |
| 4 | TCMD 67.8 | **MAKO 69.0** | 希薄化+9.8%が可視化され1株価値保全40.0へ(それでも他軸が強く上位維持) |
| 5 | MAKO 67.6 | **INNV 68.6** | |

ゲート通過704銘柄・スコアリング695銘柄。ユニットテストは181件。

#### 検証したが今回は修正しなかった既知の限界

以下は実データで確認した上で、「仕様どおりの挙動」あるいは「14.3の前方検証データ無しに触るべきでない」と判断したもの。17章の実装順序9(データが貯まるまで重みや定義を動かさない)に従う。

| 事象 | 実データでの規模 | 判断 |
|---|---|---|
| `heldPercentInstitutions` が100%超 | 192/711銘柄(27%) | Yahooが浮動株ベースで算出しているため。パーセンタイル順位の向き(低いほど発掘度が高い)は保たれるためスコアはそのまま。表示のみ「浮動株ベースで100%超」と明示するよう修正 |
| `insider_net_buying_ratio` が±1.0に飽和 | 算出できた425銘柄中338銘柄(80%)、うち63銘柄は売買イベント1件のみ | 株数比率という15.4-12の定義そのものに由来する。小口の売却1件でも−1.0になるノイズがあるが、金額加重や最低件数の導入は定義変更にあたる |
| `cash_runway_quarters` が無限大 | 697銘柄中506銘柄(73%) | 黒字企業は定義上すべて無限大でタイになり、実質「黒字か否か」の2値に近い。財務健全性軸は主にPiotroski・Altman Zで決まっている |
| サブスコア内の平均が極端値を打ち消す | INNV:Altman Z''=0.075(破綻域)でも財務健全性63.1 | 15.2の連言的評価はサブスコア間(加重幾何平均)には効くが、サブスコア内(単純平均)には効かない。19.8の判断(Z''をハードゲートにしない)との兼ね合いがあり、前方検証データを見てから再検討する |
| 希薄化の測定窓が出典で食い違う | 640銘柄中9銘柄で20pt超 | 価格系列は「バックフィル日から遡って3年」しかカバーしないため、それ以前の大型増資を取りこぼす(ACTG:2023年に43.5M→99.9M株、AIOT:2024年の合併で37.2M→133.3M株、NCMI:2023年の再建で12.8M→96.8M株)。どちらの窓が「3年」として正しいかは一意に決まらないため、price系を優先しOSNはフォールバックに留める |
| `rule_of_40` が成長率を二重計上している懸念 | 成長率との順位相関0.710、FCFマージンとの順位相関0.566 | 両者にバランスして効いており、15.4-8の「成長と収益性を1指標に統合」という定義どおり。純粋な成長プロキシにはなっていない |
| `heldPercentInsiders` がPEスポンサー保有を含む | INNV 84.3% | 「創業者のスキン・イン・ザ・ゲーム」(15.4-12)とPEファンドの保有を、yfinanceのデータでは区別できない |
| TTM成長率と年次FCFマージンの混在 | `rule_of_40` | 上記(3)のとおり、TTMのFCFを使うと定義の食い違いでノイズが増えるため現状維持 |

---
---

# 【v2.1 追記】評価手法そのものの妥当性検証と Tier 2 の実装

## 26. 加重幾何平均の構造的な性質と、二層構成(15.5)の実装

25章までは「実装が仕様どおりか」を検証してきた。26章は視点を変え、**仕様そのもの(銘柄評価の手法)が10バガー探索という目的に対して妥当か**を、実データのスコア分布から検証した記録である。

### 26.1 検証:加重幾何平均は「弱点のない銘柄」を選び、10バガーの入口を沈める

15.2は「10バガーの条件は連言的だから加重和ではなく加重幾何平均にする」とした。これは**破綻回避の観点では正しい**が、探索対象の発見という観点では副作用がある。ゲート通過704銘柄・スコアリング695銘柄(2026-08-24)で測定した。

**成長軸トップ20銘柄が総合で何位に入るか**:

| 銘柄 | 成長 | 割安 | 総合スコア | 総合順位 |
|---|---|---|---|---|
| SHAK(Shake Shack) | 95.4 | 41.9 | 52.2 | 191位 |
| SATL(Satellogic) | 94.5 | 31.9 | 39.5 | 495位 |
| KRUS(Kura Sushi) | 92.5 | 33.7 | 38.4 | 517位 |
| **SOUN(SoundHound AI)** | **91.6** | **23.2** | **28.2** | **654位** |
| AMSC(American Superconductor) | 91.5 | 33.1 | 33.9 | 597位 |
| ACTG(Acacia Research) | 91.1 | 84.3 | 71.2 | **1位** |

**成長軸トップ20の総合順位は中央値182位/546銘柄。総合トップ50に入るのは4銘柄だけ**だった。

原因は幾何平均の設計そのものにある。いずれかの軸が低いと総合が強く引き下げられるため、**上位に残るのは「何かが突出した銘柄」ではなく「弱点のない優等生」**になる。実測でも、上位30銘柄の最強軸の中央値は86.1で、90を超える軸を持つのは30銘柄中6銘柄しかない。

一方、15.1①は売上成長を10バガーの最大の寄与因子(3〜5x)としている。そして現実の10バガーは、**入口の時点では割高であることが多い**(トレーリング指標で見れば必ずそうなる。成長が先に来て利益と株価が後から追いつくため)。15.1③の「マルチプル再評価は低評価から入ることでのみ得られる」という前提は要件定義書に根拠が示されておらず、この仮定と幾何平均という強い構造が結びついた結果、**探しているはずの銘柄群を系統的に排除している**。

なお、サブスコア間の相関を測ったところ大半の組み合わせは |r| < 0.2 でほぼ独立しており(加重幾何平均が意味を持つ前提は満たされている)、成長と割安の相関も −0.14 と弱い。つまりこれは「成長株は必ず割高だから」という単純な話ではなく、**幾何平均が『どの軸であれ弱点があること』を強く罰する**ことの帰結である。

### 26.2 判断:幾何平均は変えず、Tier 2 で拾い直す

幾何平均を弱める(加重和に戻す・べき指数を下げる)ことは検討したが**採用しなかった**。

- 15.2の「成長30点満点・財務健全性0点の銘柄は倒産する」という指摘は正しく、幾何平均はその防波堤として実際に機能している
- 14.3の前方検証データが1件も無い(`forward_returns` 0行)現状で構造を変えるのは、根拠のないチューニングにしかならない(17章の実装順序9)

代わりに、15.5が既に設計していた**二層構成をここで実装する**。Tier 1(弱点のない銘柄)と Tier 2(突出しているが弱点もある銘柄)は目的の異なるリストであり、分離して両方見せるのが15.5の趣旨に沿う。

### 26.3 実装:Tier 2(監視リスト)

`screening/watchlist.py`(純粋関数)+ `GET /api/v1/watchlist` + `/watchlist` 画面。理由は4分類:

| 理由 | 内容 | 出典 | 実データ件数 |
|---|---|---|---|
| `high_growth_suppressed` | 成長軸が当日の上位5%なのに総合50位圏外 | **26.1(新規)** | 26 |
| `single_gate_miss` | 除外ゲートを1つだけ落とした | 15.5 | 434 |
| `recent_listing` | 決算実績の期数不足(IPO直後) | 10章・15.5 | 16 |
| `insufficient_data` | 全ゲート通過だがカバレッジ不足でスコア算出不能 | 15.5・14.8 | 9 |

**`single_gate_miss` に含めないゲート**:時価総額上限・売上高上限・セクター除外は、改善して復帰することを期待する類の条件ではない(15.6:大きすぎる企業は算数上10倍になれない。セクターは変わらない)。仮に基準内に縮小したとしても、それは監視の成果ではなく事業の悪化を意味する。`missing_*`・`no_raw_data` も「あと一歩」ではなく「判定に必要なデータが無く何も言えない」状態なので除く(既存の `GET /excluded` で確認できる)。結果、監視対象になるのは 流動性196・ランウェイ111・希薄化63・自己資本57・株価下限7 の5ゲート。

**閾値の決め方**:`HIGH_GROWTH_PERCENTILE` は固定値ではなく**当日の成長軸の分布**から決める(ユニバース構成が変わっても意味が保たれる)。実データで該当件数を確認して 0.95(上位5%)を採用した:

| 設定 | 閾値 | 50位圏外の該当数 |
|---|---|---|
| 上位20% | 70.7 | 110銘柄(監視リストとして多すぎる) |
| 上位10% | 79.1 | 52銘柄 |
| **上位5%** | **84.6** | **26銘柄** ← 採用 |
| 上位3% | 87.3 | 15銘柄 |

`TIER1_RANK_DEPTH = 50` は、15.5が「Top50に何日連続で入っているか」を継続性の指標に挙げていることに合わせた。

**並び順**:高成長組は**成長スコアの高い順**に並べる。総合順位順(=惜しい順)にすると、50位台の「ほぼTier 1」の銘柄が上に来て、SOUN(654位)のような**最も強く沈められた銘柄が最後尾に埋もれる**——この分類を作った目的と逆になるため。

UI上は理由ごとのタブに分け、それぞれ「なぜこのリストに載っているのか」を常時表示する。ランキング画面だけを見ると Tier 2 の存在に気づけないため、READMEにも「ランキングだけを見ないこと」を明記した。

### 26.4 この実装で解決していないこと

Tier 2 は**発見の窓口**であって、順位付けではない。26.1で指摘した「幾何平均が10バガーの入口を沈める」という性質そのものは残っている。前方検証データが12ヶ月以上蓄積された段階で、以下を再検討する:

- サブスコアの重み(現状はすべて推測値。`forward_returns` が0行の現在、予測妥当性の根拠は存在しない)
- 幾何平均のべき指数、あるいは Tier 2 に独立したスコアリング(成長・モメンタム重視)を与えるか
- 15.5の残項目「Top50に何日連続で入っているか」(スコア履歴が2日分しかなく現時点では実装しても意味がない)

また、評価手法の妥当性検証で判明した以下の限界は、Tier 2 では解決しない(いずれも17章9に従い前方検証待ち):

- **シグナルのホライズン不整合**:重みの18%(モメンタム9%+予想修正9%)は予測が効く期間が1〜12ヶ月のシグナルだが、目的は7年で10倍(14.1)。数年ホールド前提(15.5)ならこれらの情報は保有開始から数ヶ月で陳腐化し、3〜5年では長期リバーサルにより符号が逆転する可能性すらある
- **収益性と財務健全性の重複**:この2軸の相関は0.54で、他の組み合わせ(いずれも|r|<0.4)より突出して高い。合計23%の重みが実質的に「利益とキャッシュを生めているか」を二重評価している
- **発掘度軸の根拠の弱さ**:`discovery` は他の全軸と負の相関(−0.05〜−0.17)。発掘度が高い銘柄ほど成長・財務・利益の質が低いという関係にあり、この軸(9%)が「掘り出し物」ではなく「小さくて質の低い株」を拾っている可能性がある
- **上位のスコア解像度の低さ**:上位10銘柄のスコア幅は4.40pt、上位50銘柄でも10.65pt(中央値46.1・最高71.2・標準偏差10.59)。1位あたり約0.2ptしか離れておらず、入力側のノイズ(インサイダー比率の±1飽和など)を考えると**上位10位の順序に有意差があるとは言えない**。UI上でこれを明示することは今後の課題
- **セクター内相対評価と10バガーの偏りの矛盾**:10バガーは業種と時期に強く偏って発生するが、セクター内順位で正規化することはその最大の駆動因を意図的に中和している

---

# 【v2.0 追記】スコアリングモデルの全面置換

## 27. 実現時価総額倍率(implied MOIC)モデルへの移行

26章は「加重幾何平均が10バガーの入口を沈める」という性質を認めつつ、前方検証データが0行である以上、構造を変えるのは根拠のないチューニングにしかならないとして**幾何平均を維持し、Tier 2で拾い直す**という判断を下した。

27章はその判断を覆す。理由は2つある。

1. 26.1の測定は「高成長銘柄が沈む」という一部の症状しか捉えていなかった。**モデル出力とその目的の相関を直接測ると、符号そのものが逆だった**(27.1)
2. 「前方検証データが無い」という前提自体が誤りだった。**擬似バックテストは今日実行できる**(27.8)

### 27.1 検証:v2のスコアは目的関数として符号が逆だった

2026-08-24、v2でスコアリングできた695銘柄について、スコアと「10バガーの駆動因」の相関を直接測定した。

| 測定項目 | v2実測 | 意味 |
|---|---|---|
| corr(総合スコア, 売上成長率) | **−0.068** | 成長と**無関係**(わずかに負) |
| corr(総合スコア, ln時価総額) | **+0.227** | **大きい銘柄ほど高得点** |
| 時価総額 最小5分位の平均スコア | 41.2($21M–372M) | 10倍余地が最大の層が**最低評価** |
| 時価総額 最大5分位の平均スコア | 48.3($2.1B–3.5B) | 単調増加 |
| 成長率トップ20銘柄の総合順位(中央値) | 約190位 / 695 | 探索対象を系統的に排除 |

上位30銘柄の実体は Seneca Foods(缶詰)、Steve Madden($3.4Bの靴)、Flexsteel(家具)、RCM Capital(人材派遣)、Ituran——**「高品質・割安な小型バリュー株スクリーナー」の出力**であって、10バガー候補ではない。

原因は3つに分解できる。

- **目的関数のミスマッチ**:10バガーは右裾の稀事象であり、必要なのは `P(リターン >= 10倍)` の最大化である。加重幾何平均は分散を罰する関数なので、右裾を狙う目的関数として符号が逆向きになる
- **パーセンタイル化が基数情報を破壊**:`normalization.py` の旧docstringは「順位だけに依存するので外れ値に頑健」を利点として挙げていたが、10倍株探索では**外れ値こそが探索対象**である。実際、売上成長 +259% の銘柄と +124% の銘柄が成長軸 95 と 91 に圧縮されていた
- **縮小推定の二重適用によるサイズバイアス**:`_shrink_toward_neutral`(サブスコア内)と `coverage_ratio**0.5`(軸間)を両方かけていた。`corr(指標カバレッジ, ln時価総額) = +0.183` であり、**データが揃っている = 成熟企業 = 10倍になりにくい**。カバレッジペナルティは事実上「10倍になりにくさ」に正の重みを与えていた

### 27.2〜27.7 新モデルの構造

15.1の恒等式をパーセンタイルに潰さず、そのまま**積**として計算する。

```
期待倍率 = ① 売上の成長 × ② 利益率の変化 × ③ マルチプルの変化 × 有利子負債の影響 ÷ ④ 希薄化

log(MOIC) ~ N(μ, σ),   μ = ln(期待倍率) − σ²/2

P(10倍) = P(MOIC >= 10) × 生存確率 × 規模の事前分布
```

| 節 | 内容 |
|---|---|
| 27.2 | **成長の減衰**:`g_t = 3% + (g0 − 3%) × 0.75^t`。初期成長率は3年CAGRと直近年次YoYの**小さいほう**(27.13)、上限60% |
| 27.2 | **粗利率の外挿**:直近2期のトレンドを40%に減衰、7年の総変化を±15ptに制限 |
| 27.3 | **終端マルチプル**:EV/GrossProfitをセクター中央値へ対数空間で50%寄せる。**上方リレーティングは成長企業にのみ**(非対称)。変化幅は0.30〜3.00倍 |
| 27.4 | **生存確率**:財務健全性を加点軸ではなく `(1 − 6%×exp(−1.2×健全性))^7` として扱う。健全性0で7年生存65%、+1で88%、−1で21% |
| 27.5 | **不確実性**:成長ボラティリティをデルタ法で伝播、マルチプル/利益率/希薄化の推定誤差を二乗和で合成、レバレッジ倍率を乗算 |
| 27.6 | **規模の事前分布**:MOICの算術は規模中立だが現実の10倍達成率は規模とともに下がる。その差だけを担う較正対象パラメータ |
| 27.7 | **測れないものは順位を付けない**:必須入力欠損・超高レバレッジ・期待倍率1.0未満は Tier 2 へ回す |

**実測での効果**(2026-08-24、v3):

| 測定項目 | v2 | **v3** |
|---|---|---|
| corr(スコア, 売上成長率) | −0.068 | **+0.237** |
| corr(スコア, ln時価総額) | +0.227 | **−0.262** |
| 中央見通しが損失の銘柄の上位50入り | 5銘柄 | **0銘柄** |

### 27.8 擬似バックテスト——14.3の結論を覆す

14.3は「ポイントインタイム財務が取れない → バックテスト不可能 → 前方検証を待つしかない」と結論した。**この結論は厳しすぎた。**

- 年次の `income_stmt` / `balance_sheet` / `cash_flow` は**期末日をキーとする時系列**で返る。評価時点 T において「T までに開示済みだった期」(期末 + 90日)だけを残せば、近似的なポイントインタイムの財務諸表が構成できる
- 価格・発行済株式数は `price_snapshots` に日次で入っており、T 時点の値をそのまま引ける

したがって「T 時点のデータだけでスコアを付け、T 以降の実現リターンで検証する」擬似バックテストは**今日実行できる**。7年のフィードバックループを持つシステムは事実上検証されないシステムであり、限界つきでもフィードバックを今得るほうが桁違いに価値がある。

**この方式を成立させるための設計制約**:モデルの入力を**年次財務諸表と価格系列だけ**に限定した。`info` 由来のTTM値・アナリスト予想・機関保有率・インサイダー取引は過去に遡れないため、使えばモデルが永久に検証不能になる。この制約により**ライブとバックテストで完全に同一の関数が走る**ことが構造的に保証される。

**限界(いずれも解消不能)**:リステートメントの先読み、開示ラグの近似、年次5期という期数の制約、保有期間の重複による検出力の低さ。

### 27.9 希薄化の測定窓——1位銘柄の誤評価

ACTG(Acacia Research、v2で総合1位)の実データ:

```
発行済株式数:  2022-12: 43.5M → 2023-12: 99.9M  (+130%、1年で倍増)
価格ヒストリー:  2023-08-22 以降のみ
```

希薄化の測定窓が、株式数が倍増した**後**から始まっていた。結果 `value_preservation = 77`(高評価)が付いていた。実態は「株を倍発行して事業を買い、売上を増やした」持株会社である。

修正:日次観測と年次 `Ordinary Shares Number` の**両方から算出し、実測窓が長いほうを採る**。年次系列は最大5期=約4年あり、日次観測の窓(バックフィル3年)より長いことが多い。長い窓を優先すれば、増資イベントが観測区間の外に落ちる確率が構造的に下がる。

### 27.10 正規化の置換

セクター内パーセンタイルを廃止。新モデルはスコアを絶対量(確率)として計算するため、銘柄を相互に順位づける正規化そのものを必要としない。クロスセクション統計が要るのは**終端マルチプルの回帰先となるセクター中央値**の1箇所だけ。裾の厚い比率への対処は順位化ではなく**対数変換 + 中央値/MAD** で行う。

### 27.11 上場廃止銘柄が検証資産から消えていた(実装バグ)

```
上場廃止 → yfinance失敗が連続 → is_quarantined = True
        → daily_pipeline が収集対象から除外
        → price_snapshots が止まる
        → forward_validation は行を1件も書かない
```

**−100%(最悪の結果)だけが検証データから欠落していた。** 14.3が「生存バイアス対策のため廃止銘柄をマスタから削除しない」と明記しているのに、検証資産そのものに生存バイアスが入る経路が残っていた。デシル単調性もリフト倍率も、実態より必ず良く出る。

修正:上場廃止銘柄も**最終観測価格で決済**して行を書き、`settlement` 列に区分を残す。−100%を決め打ちしないのは、上場廃止の原因が破綻だけでなく買収(TOB)でもあるため——最終観測価格はどちらの結末も正しく反映する。

### 27.12 短いホライズンで「10倍」をどう測るか

価格ヒストリーは3年分しかなく、7年で10倍という最終判定は今日どう頑張っても測れない。代わりに「**その時点でのペース**」を見る。10倍/7年は年率38.9%なので、ホライズン h 年での等価閾値は `10^(h/7)` になる(1年なら1.389倍)。

オンペースは10倍の達成を意味しないが、**モデルの序列が正しいかどうか**を測るには十分であり、7年待つ理由にはならない。較正の比較のため、モデルの7年予測も `μ_f = μ×f, σ_f = σ×√f` でホライズンに引き直す。

### 27.13〜27.14 実データで踏んだ論理破綻の記録

新モデルの初版を実データに当てたところ、順位が壊れる形の欠陥を4つ踏んだ。いずれも例外を投げず静かに順位を壊すため、`tests/unit/test_moic.py` にリグレッションテストとして固定してある。

| # | 症状 | 原因 | 修正 |
|---|---|---|---|
| 1 | CRMT が P(10x)=75% で1位。時価総額$21M・ネットデット$725M(EVの2.8%) | 株式が実質的に負債へのコールオプションなのに、低レバレッジ銘柄と同じばらつきで評価していた | `min_equity_share_of_ev = 0.15` で対象外に。加えて `leverage_effect` を σ に乗算 |
| 2 | IMMR が P(10x)=75%。マルチプル10倍拡大 | 買収による連結で3年CAGRが+214%。加重平均が上限に張り付いた。かつ EV/GP=0.18 からの平均回帰が15倍になっていた | 初期成長率は CAGR と YoY の**小さいほう**。マルチプル変化を0.30〜3.00倍に制限 |
| 3 | 上位10銘柄のうち6銘柄が**減収企業**(ANGI −16%、NUS −14% 等) | (a) 平均回帰が成長と無関係に上方へ働いていた (b) 現在の倍率に `floor=1.0` を掛けていたため、回帰の重みが0でも `1.0/0.5 = 2倍` の**幻のリレーティング**が生まれていた | 上方回帰は成長企業のみ(非対称)。現在の倍率はクランプしない |
| 4 | DEC(中央値0.70倍=3割の損失見込み)が P(10x)=9.5% で8位 | 点推定を**中央値**として扱っていたため、分散を広げると平均が膨らみ、**ばらつきが大きいほど10倍しやすい**という反転が起きていた | 点推定は分布の**平均**。`μ = ln(E) − σ²/2` |

### 27.15 バックテストの母集団に上場廃止銘柄が存在しない

`delisted_settlement_rate` を計測したところ **0.00%** だった。これは「1銘柄も廃止にならなかった」ではなく、**廃止された銘柄が最初から標本に入っていない**ことを示す警告灯である。

`tickers` は NASDAQ Trader の**現在の**上場一覧から作られるため、2023〜2026年に廃止された銘柄はマスタに存在しない。`backfill-history` も現在の上場銘柄に対してのみ実行されている。**バックテストは源泉の時点で生存バイアスを持つ。**

yfinance と NASDAQ Trader の公開データだけでは解消できない。CLIとAPIの両方でこの事実を警告として出し、リターンが実態より良い方向へ偏っていることを明示する。

### 27.16 削除したもの

| 削除対象 | 理由 |
|---|---|
| `scoring/subscores.py`(8サブスコア定義) | 恒等式に現れない量は掛け算のどこにも入らない |
| `financial_metrics.py` の11指標(Rule of 40・ROIC・Sloanアクルーアル・EV/Sales÷成長率・52週高値近接度・アナリストカバレッジ・予想修正モメンタム・決算サプライズ連続性・インサイダー買い越し・12-1モメンタム・成長安定性CV) | 新モデルが1つも使わない。かつ大半が過去に遡れずモデルを検証不能にする |
| `eps_revisions`・`earnings_dates`・`insider_transactions` の収集 | 上記指標専用。銘柄あたり3回の追加APIコールを削減(8.3・14.9のレート制限にも効く) |
| Tier 2 の `high_growth_suppressed` 分類 | 幾何平均が高成長銘柄を沈めるというモデル側の欠陥への対症療法。原因が消えたため不要 |
| `scores` の `overall_score`/`subscores`/`subscore_fallback`/`subscore_metric_coverage`/`coverage_ratio` | 廃止済み構造に固有の列 |

26.4が挙げた未解決事項(シグナルのホライズン不整合・収益性と財務健全性の重複・発掘度軸の根拠の弱さ・上位のスコア解像度の低さ)は、いずれも**該当する軸そのものが無くなった**ことで解消した。

### 27.17 中心的な見通しが損失の銘柄は順位を付けない

対数正規分布は、期待値を固定したまま分散を広げると閾値超過確率を `σ = √(2·ln(target/E))` 付近まで**上げる**。これは数学的に正しく、経済的にも妥当である——同じ期待値ならボラティリティの高い資産のほうが10倍に届く確率は高い。

危険なのはこの性質が**期待値そのものが1を下回る銘柄**に適用される場合だけである。事業が縮小していく見通しの銘柄に当てはめると「モデルが外れることに賭ける」順位づけになる。実データでは上位50銘柄のうち5銘柄が期待倍率1.0未満だった。

`requirements.min_expected_moic = 1.0` で構造的に排除し、Tier 2 へ回す。

**この判断の帰結**:ゲート通過704銘柄のうちランキングに載るのは329銘柄に減った。残りは「測れない」か「見通しがマイナス」であり、どちらも低いスコアを付けずに監視対象とする。現状この2つは同じバケットに入る(既知の制約)。

### 27.18 検証結果——モデルはまだ目標KPIに届いていない

`run-backtest`(2024-02〜2025-08 の8評価日、保有1年、観測2,642件):

| KPI(14.2) | 実測 | 目標 | 判定 |
|---|---|---|---|
| デシル単調性(順位相関) | **+0.588** | +1.0に近いこと | 中程度 |
| リフト倍率 | **1.21** | >= 2.0 | **未達** |
| 破綻回避率 | 上位デシル 8.7% vs ユニバース 11.2% | 上位のほうが低い | **達成** |
| 較正誤差 | −4.80pt | 0に近い | やや弱気 |

デシル1の中央値リターンは +7.3%(ユニバース中央値を上回る)、デシル10は −8.2%。オンペース率も 30.2% → 20.1% と概ね単調に下がる。**下位の識別はできているが、上位デシルの超過リターンは目標に届いていない。**

**この結果に合わせてパラメータを調整することはしない。** 保有期間が重複しているため独立な観測期間は実質3つ程度しかなく、2,642件という観測数は見かけほどの検出力を持たない。この標本にフィットさせるのは過学習である。パラメータは事前値のまま据え置き、`forward_returns` の蓄積と価格ヒストリーの伸長を待つ。

**UI上での扱い**:この検証結果は `/validation` 画面に常時表示する。14.2は「上位デシルでも大半は外れる前提をUI上にも明示すること」を要件としており、KPIが目標に届いていない事実を隠してランキングだけを見せるのは、ツールとして誤った確信を与える。

### 27.19 残る既知の限界

- **バックテストの生存バイアス**(27.15)——データ源の制約であり解消不能
- **測れない銘柄と見通しマイナスの銘柄が同じバケット**(27.17)——区別するにはスコアリング側から理由を持ち回す必要がある
- **保有期間の重複による検出力不足**(27.18)——価格ヒストリーが伸びれば改善する
- **セクター内相対評価と10バガーの偏り**(26.4から継続)——終端マルチプルの回帰先にのみ残っている。10バガーは業種と時期に強く偏って発生するが、セクター中央値への回帰はその駆動因の一部を中和する
- **`size_prior` と `size_compression` の二重計上の可能性**(27.6)——別機構として実装しているが、バックテストで分離できるだけの検出力がまだ無い。`exponent: 0.0` で無効化できるようにしてある
- **売上前・粗利前の企業を評価できない**(27.21①)——実データで44銘柄、うち32銘柄がHealthcare(臨床段階バイオテック)。恒等式モデルの原理的な限界
- **加速中の企業を保守側に丸めている**(27.21②)——ランキング対象の15%が該当

### 27.20 「測れなかった」と「測った結果がこうだった」を分ける

27.17で `min_expected_moic = 1.0` を入れた直後、ランキング外の銘柄がすべて
「データ不足」という1つのバケットに入っていた。内訳を測ると:

| ランキングに載らない理由 | 件数 |
|---|---|
| **見通しがマイナス**(測れたが期待倍率 < 1.0) | **303** |
| 必須入力の欠損(測れない) | 66 |
| 超高レバレッジ(モデルが成立しない) | 7 |
| 粗利が無い | 6 |
| EVがマイナス | 2 |

**8割以上が「測った結果がこうだった」なのに「測れなかった」と表示していた。**
これは単なるラベルの粗さではなく誤情報である——期待倍率0.7倍という測定結果は、
データが無いことより遥かに有用な情報であり、両者を混ぜると利用者は
「このアプリはこの銘柄について何も知らない」と誤解する。

修正:見通しがマイナスの銘柄には**確率NULLの `scores` 行を書く**。順位は付けない
(`GET /candidates` は `probability IS NOT NULL` で絞る)が、期待倍率・中央値倍率・
5因子の内訳はすべて記録され、銘柄詳細画面で確認できる。監視リストも
`negative_outlook` と `insufficient_data` の別タブに分かれる。

`compute_moic(..., enforce_min_expected_moic=False)` で閾値判定だけを外して
呼べるようにし、スコアリングエンジンが2つを区別できるようにしている。

### 27.21 最終監査:モデルが構造的に見落とすもの

実装完了後、「10バガー探索ツールとして論理に破綻がないか」を投資家の視点で
監査した。順位づけのロジック自体に残る破綻は見つからなかったが、**モデルが
構造的に扱えない領域**が2つ定量化された。いずれも仕様上の限界であり、
バグではない。隠さず記録する。

#### ① 売上前・粗利前の企業を一切評価できない(最大の穴)

実現倍率モデルは `EV / GrossProfit` を評価の軸に置くため、粗利が測れない企業は
入口で落ちる。実データでは44銘柄が該当し、**うち32銘柄がHealthcare**——
つまり臨床段階バイオテックである。

これは10バガー探索として痛い。承認前バイオテックは、パイプラインの成否で
株価が10倍にも1/10にもなる典型的な右裾資産であり、10バガーの主要な供給源の
ひとつである。恒等式(15.1)は「売上 × 利益率 × マルチプル」で価値を組み立てる
以上、**売上がまだ無い企業の価値=オプション価値を原理的に表現できない**。

対処は2つしかない。

- 割り切る(現状):これらは `insufficient_data` として監視リストに載せ、
  定性判断に委ねる。少なくとも「見落とした」のではなく「このモデルの
  対象外だと分かっている」状態にはなっている
- 別モデルを併走させる:パイプラインの相数・現金・バーンレート・提携の有無から
  オプション価値を推定する、まったく別の評価軸を作る。**恒等式モデルに
  無理やり混ぜてはいけない**(スケールが違うものを同じ確率として並べると、
  どちらの数字も意味を失う)

#### ② 加速中の企業を保守側に丸めている

初期成長率に `min(3年CAGR, 直近年次YoY)` を採る(27.13)のは、買収による連結と
基数効果を同時に潰すための判断だった。その代償として、**成長が加速している
企業は必ず遅いほう(CAGR)に丸められる**。

実データでは、ランキング対象327銘柄のうち **49銘柄(15%)** が
`YoY > CAGR + 10pt` の加速局面にあり、その加速分は評価に入っていない。

10バガーが識別可能になるのは多くの場合まさにこの加速局面なので、これは
「安全側に倒した結果、探索対象の一部を見えなくしている」トレードオフである。
`min` を止めれば IMMR(買収連結で3年CAGR +214%)のような誤検出が戻るため、
より良い解は「買収由来の成長を直接検出して除く」ことだが、年次財務諸表から
オーガニック成長だけを取り出す確実な方法は無い(のれん・無形資産の増加を
代理指標にする案はあるが、実データでの検証が必要)。**バックテストの検出力が
上がるまでは保守側で据え置く。**

#### 監査で問題が無かった項目

- **ゲートとモデルの整合**:`model_market_cap / info.marketCap` は p10=p50=p90=1.000。
  売上も p50=0.966(p10-p90 で 0.851-1.038)。ゲートとスコアリングが別の数字を
  見ている、という不整合は無い
- **セクター集中**:上位50は Healthcare 9・Industrials 8 を筆頭に8セクターへ分散。
  単一セクターに偏っていない
- **規模**:上位50の時価総額は中央値 $878M(範囲 $70M〜$3.29B)。10倍で $8.8B
  という到達点は算術的に現実的な範囲に収まっている
- **恒等式の閉包**:5因子の積が `expected_moic` と厳密に一致することを
  テストで固定済み(UIの内訳表示が実際の計算と乖離しない)

### 27.22 エラー応答がCORSヘッダを失い、原因が見えなくなっていた

DBマイグレーション後、古いAPIプロセスが動いたままだったとき、**全画面が
`Failed to fetch` になった。** 実際に起きていたのは「スキーマ不一致による500」
だが、ブラウザにはそれが見えなかった。

原因はミドルウェアの構造にある。Starletteで未処理例外を拾う
`ServerErrorMiddleware` は常に最外殻にあり、**そこが返す素の500には
CORSヘッダが付かない**。ブラウザはヘッダの無いクロスオリジン応答を読む前に
ブロックするため、`fetch()` は `TypeError: Failed to fetch` で失敗する。
結果、利用者からは以下の3つが**まったく区別できない**:

- APIプロセスが起動していない
- APIは動いているが500を返している
- CORSの許可オリジン設定が違う

修正:CORSミドルウェアの**内側**に例外ハンドラを置き、すべての例外をJSONに
変換してから返す。これでCORSヘッダが付き、フロントエンドが実際の理由を表示できる。
最も起きやすい失敗であるスキーマ不一致(`ProgrammingError`)には、対処まで
書いた専用メッセージを出す。

あわせて2点:

- **`/ready` を追加**。`/health` はプロセスの生死だけを見るためDBに触らないので、
  今回のような「プロセスは生きているがスキーマが古い」状態では200を返し続け、
  切り分けに使えなかった。`/ready` は実際にクエリを投げて**コードとスキーマが
  噛み合っているか**まで確認する
- **フロントエンドの `apiFetch`** が `fetch()` の例外を捕まえ、確認すべき3項目を
  含むメッセージに置き換える。ブラウザは理由を教えてくれない(セキュリティ上
  わざと)ので、素の "Failed to fetch" のままでは何も分からない

**運用上の教訓**:`uvicorn` は `--reload` 無しで起動すると、コード変更後も古い
モジュールを保持し続ける。マイグレーションを伴う変更の後は必ず再起動すること。
READMEの起動コマンドを `--reload` 付きに変更した。

### 27.23 「7年」というホライズンの根拠

**正直に書く:根拠は無い。** 14.1の表は「定義(案)」と明記されており、7年という
数字に出典も実証もない。当時「評価期間を決めなければスコアの正しさを検証する
手段が定義できない」という理由で置かれた作業上の仮定である。

後付けで説明できる理屈はある。

| ホライズン | 必要CAGR | 評価 |
|---|---|---|
| 5年 | 58.5% | 5年間これを維持した企業は歴史的にごく僅か。厳しすぎて候補が枯れる |
| **7年** | **38.9%** | 現実に達成例のある水準 |
| 10年 | 25.9% | 達成しやすいが、**年次決算が最大5期しか取れない**(13.1)ため、3〜4年の実績から10年を外挿するのは無理がある |

つまり7年は「到達可能性」と「予測可能性」の折衷点に置かれている。ただしこれは
**事後的な正当化であって、この数字を選んだ理由ではない。**

#### 実測:ホライズンの選択は順位をほとんど変えない

より重要なのは「この仮定がどれだけ効くか」である。2026-08-25の実データで、
ホライズンだけを変えて再スコアリングした。

| H | ランク数 | P(10倍)中央値 | P最大 | 期待倍率中央値 | 7年との上位50一致 | 7年との順位相関 |
|---|---|---|---|---|---|---|
| 5年 | 329 | 0.037% | 16.33% | 1.34 | 44/50 | **+0.979** |
| **7年** | **349** | **0.068%** | **17.51%** | **1.43** | — | — |
| 10年 | 368 | 0.125% | 17.71% | 1.57 | 43/50 | **+0.971** |
| 15年 | 399 | 0.242% | 19.33% | 1.80 | 31/50 | +0.848 |

**5〜10年の範囲では、ホライズンを変えても順位はほとんど動かない**(順位相関
+0.97以上、上位50の8割以上が一致)。理由は、この範囲では全銘柄が同じ方向に
同じくらい動くため、相対順位が保たれるからである。

一方で**絶対水準は大きく動く**(中央値が5年 0.037% → 10年 0.125% と3倍以上)。

#### 含意

- **ランキングを見る用途では、7年という選択は結論をほとんど左右しない。**
  この仮定に神経質になる必要はない
- **確率の絶対値を額面どおり読んではいけない。** ホライズンの選択だけで3倍動く。
  そもそも較正誤差が −4.80pt(27.18)あり、この数字は序列を表すものであって
  「本当に17%の確率で10倍になる」という意味ではない
- 15年まで伸ばすと順位相関が +0.848 まで落ちる。**極端に長い設定は避けること**
  (年次5期しか無いデータから15年を外挿する時点で無理がある)

`config/scoring.yaml` の `horizon_years` で変更でき、`run-backtest` で影響を
確認できる。ただし14.1の定義を変えるとKPIの意味も変わる点に注意すること。

### 27.24 「何年で何倍」を自由に指定できるようにする

7年で10倍という目標は14.1が置いた仮定にすぎず(27.23)、利用者ごとに見たい
条件は違う。「3年で3倍」「5年で5倍」のように**年数と倍率を自由に指定して
確率を出せる**ようにした。

#### 設計判断①:近似で引き伸ばさず、厳密に再計算する

対数正規を時間方向に伸ばす近似(`μ_f = μ·f, σ_f = σ·√f`、バックテストの較正で
使っているもの)は安いが、**本モデルのドリフトは成長減衰により前倒しに効く**ため、
短いホライズンでは上昇を過小評価する。

そこで `scores.inputs` に `MoicInputs` とセクター中央値を保存し、リクエストごとに
`compute_moic` を**その年数で走らせ直す**。1銘柄あたり十数個の数値にすぎず、
`raw_snapshots` を読み直すのに比べて桁違いに軽い(349銘柄でも体感ゼロ)。

保存時の注意:黒字企業のキャッシュランウェイは `float("inf")` になるが JSON で
表現できないため文字列にエスケープしている。ここが壊れるとランキングが静かに
別物になるので、往復のテストを置いた。

#### 設計判断②:必要年率を必ず併記する

**年数と倍率を別々に眺めていると難易度を取り違える。**

| 目標 | 必要年率 |
|---|---|
| 3年で3倍 | **44.2%** |
| 5年で10倍 | 58.5% |
| **7年で10倍(既定)** | **38.9%** |
| 7年で5倍 | 25.8% |
| 10年で10倍 | 25.9% |

「3年で3倍」は一見穏当だが、**「7年で10倍」より厳しい**。この関係は年率に
直さないと見えない。APIは `required_cagr` を必ず返し、UIはプリセットの各ボタンと
選択中の目標の両方に年率を表示する。

#### 実データでの挙動(2026-08-25)

| 目標 | 必要年率 | 対象銘柄 | 1位のP | 上位5銘柄 |
|---|---|---|---|---|
| 7年で10倍 | 38.9% | 349 | 17.51% | GRSD, ACTG, BYRN, ODD, STNE |
| 3年で3倍 | 44.2% | 283 | 47.87% | GRSD, BYRN, ODD, KARO, IMMR |
| 7年で5倍 | 25.8% | 349 | 33.25% | GRSD, ODD, ACTG, BYRN, COLL |
| 5年で10倍 | 58.5% | 324 | 16.33% | GRSD, BYRN, STNE, ACTG, IMMR |
| 10年で10倍 | 25.9% | 342 | 17.71% | GRSD, ODD, ACTG, CLW, BYRN |

**年数を短くすると対象銘柄数が減る**(349 → 283)。効いているのは
マルチプルの下方修正である:成長は年数の複利で効くのに対し、平均回帰による
再評価は**年数に関わらず一度きり**効く。したがって割高な銘柄では、短い年数ほど
下方修正の重さを成長で取り返せず、期待倍率が1.0を割って順位対象から外れる
(27.17)。

#### 実装範囲

- `GET /candidates?horizon_years=&target_moic=` — 再計算して並べ替え、`target` を返す
- `GET /candidates/{ticker}?horizon_years=&target_moic=` — 5因子の内訳もその年数で計算し直す
- ランキング画面に目標セレクタ(プリセット4種+任意入力)。選択は**URLに載せる**ので
  「3年で3倍のランキング」をそのまま共有・ブックマークでき、戻るボタンが履歴として機能する
- 銘柄詳細へのリンクが目標を引き継ぐ(一覧と詳細で数字が食い違わないように)
- 範囲は1〜15年・1.5〜100倍に制限。1年未満は株価が決算よりセンチメントで動き
  モデルの前提が成り立たず、15年超は年次5期のデータからの外挿として無理がある(27.23)

#### 保存される確率は既定の目標のまま

`scores.probability` に入るのは常に既定(7年で10倍)の値である。前方検証
(`forward_returns`)とバックテストのKPIはこの系列に紐づくため、UIでの目標変更が
検証資産を汚染することはない。銘柄詳細の「P(10倍)の推移」グラフも既定の系列を出す。

---

# 【v2.2 追記】v4:因子の予測力を実測してモデルを組み直す

## 28. 実測にもとづくモデル改訂と、KPIそのものの読み替え

v3(27章)は理論的に整合したモデルだったが、擬似バックテストのリフト倍率は
1.21・デシル単調性は +0.588 に留まり、27.18は「この標本にフィットさせるのは
過学習だから据え置く」と結論して終わっていた。**この結論は半分正しく、半分
逃げていた。** パラメータを標本に合わせるのは確かに過学習だが、
**モデルが持っている項が順位を悪化させていないかどうかは、パラメータ調整とは
別の問題**である。項の符号が逆なら、それは較正の問題ではなく構造の誤りであり、
標本が小さくても直すべきものだ。

28章はまず「どの項が効いていて、どの項が害になっているか」を実測し、
その結果にもとづいて構造を組み替えた記録である。

### 28.1 診断:因子ごとの予測力を実測する

擬似バックテストと同じポイントインタイム条件で、9つの評価日
(2024-04〜2025-08、各700銘柄弱、計7,024観測)について、モデルが使っている
因子と、使っていない候補因子を同じ土俵に並べ、**評価日ごとの順位IC**
(因子の順位と1年後の実現リターンの順位相関)を測った。

| 因子 | 順位IC | t値 | 符号が正だった評価日 |
|---|---|---|---|
| EV/粗利(水準) | +0.079 | +9.9 | 9/9 |
| 粗利のYoY成長 | +0.074 | +2.8 | 6/9 |
| **12ヶ月モメンタム** | **+0.070** | **+5.0** | **9/9** |
| Rule of 40 | +0.069 | +4.0 | 9/9 |
| Piotroski F-Score | +0.049 | +2.9 | — |
| **v3のモデル確率** | **+0.048** | **+6.0** | **9/9** |
| 規模の事前分布 | −0.048 | −2.8 | 1/9 |
| **v3のマルチプル変化(平均回帰)** | **−0.023** | **−3.1** | **2/9** |

読み取れたことは3つ。

1. **v3のスコアは効いている(9/9で正)が、単独の因子より弱い。**
2. **マルチプルの平均回帰項は順位を悪化させている**(9日中7日で負)。
3. **v3が削除した12ヶ月モメンタムは、モデル確率に対して直交化してもなお
   IC +0.075(8/9で正)** と、完全に増分の情報を持っていた。

### 28.2 マルチプルの平均回帰を撤廃し、「成長の対価の二重取り」を止める

**これがv4で最も重要な変更である。**

v3は「現在のEV/粗利をセクター中央値へ対数空間で50%寄せる」としていた。
15.1③「マルチプル再評価は低評価から入ることでのみ得られる」を平均回帰として
実装したものだったが、これは**モデルが市場より正しい適正倍率を知っている**
という主張であり、その根拠は無かった。

機序ははっきりしている。**EV/粗利の分子(EV)は今日の値、分母(粗利)は最大
15ヶ月前の決算である。** 株価が下がった銘柄は、業績悪化がまだ決算に出て
いないだけで「割安」に見える。そこへ平均回帰を適用すると、**悪化が数字に
現れる前の銘柄を系統的に買い上げる**ことになる。15.3が警告した「成長しない
万年割安株」を、v2とは別の経路で再現していた。

**v4の立場:マルチプルの適正水準について、モデルは市場に対する優位を主張
しない。** 現在の倍率は、その企業の現在の成長率・質・リスクについて市場が
到達した集合的な評価であり、モデルの推定より情報量が多い。したがって
`E[M_H] = M_0` を出発点に置き、**そこから動かしてよいのは、モデル自身が別途
仮定している変化と整合させるためだけ**とする。

#### 何と整合させるのか——「成長は既に価格に入っている」

モデルは売上を7年間 `growth_path` に沿って外挿する。初期成長率40%の企業は
7年目には3%台まで減速している。**40%成長の企業に付く倍率と、3%成長の企業に
付く倍率は同じではない。** 現在の倍率 M₀ は「40%で成長している企業」への
値づけであり、その成長が終わったあとも同じ倍率が付く前提は、成長の対価を
**二重に受け取る**ことになる。

断面の値づけ構造から、この対価の大きさは直接測れる:

```
ln(EV/粗利) = 定数 + κ · g
```

`uv run python -m autoscreener.cli estimate-elasticity` で推定できる。
**実測(2026-08-26、価格ヒストリー全期間の20断面、各700〜800銘柄)**:

| 項目 | 値 |
|---|---|
| κ の範囲 | +0.681 〜 +1.098 |
| 平均 | **+0.863** |
| 断面間の標準偏差 | 0.117 |
| t値 | どの断面でも +3.5 〜 +6.3(符号は20断面すべて正) |
| 決定係数 | 0.02〜0.06 |

決定係数が低いのは想定どおりである——EV/粗利のばらつきの大半は成長率以外
(利益率構造・セクター・センチメント)で決まる。ここで必要なのは**傾きの
符号と大きさ**であって説明力ではない。

この κ を使えば、成長が g₀ から g_H へ減速したときの整合的なマルチプル変化は

```
ln(M_H / M_0) = κ · (g_H − g_0)
```

となる。**リターンには一切フィットさせていない構造パラメータ**である点が
決定的に重要で、これは較正ではなく測定である。

帰結:

- 高成長銘柄は売上倍率で大きく稼ぐが、マルチプル圧縮でその一部を失う
  (g₀=50%なら圧縮0.66倍)。成長の対価を二重取りしなくなる
- 低成長銘柄は圧縮をほとんど受けない(g₀=10%なら0.94倍)
- **「割安だから上がる」という無償のリレーティングは、どの銘柄にも発生しない**

**κ は時期によって動く。** 2024年前半は0.93前後、2026年前半は0.68前後と低下
傾向にある(市場が成長に払う対価が縮んでいる)。設定には全期間の平均を固定値
として置いている——7年先を外挿するモデルに直近の断面値を入れると、相場つきの
短期変動がそのまま7年の見通しに乗ってしまうため。

**ユニバースを変えたら測り直すこと。** 市場・時価総額レンジ・除外セクターを
変えれば値づけの構造そのものが変わる。

### 28.3 決算の陳腐化に対する価格ナウキャスト

**モデルは構造的に古い数字を見ている。** 評価時点で使える年次決算は、期末から
開示ラグ90日を経てはじめて可視になる。つまり最悪の場合、モデルは**15ヶ月前の
事業実態**から7年後を外挿している。その15ヶ月のあいだに市場は決算・ガイダンス・
受注動向を織り込んで株価を動かしており、その情報をモデルは一切見ていなかった。

28.1でモメンタムが完全に増分の情報を持っていたのは、**モデルが構造的に見て
いない期間の情報**をそれが代理していたからだと解釈できる。

**どう使うか——リターン予測ではなく、成長率推定の修正として。**

κ を逆に読むと、ある銘柄の株価が市場全体に対して Δln(P) だけ動いたとき、
その一部が成長期待の改定によるものだと解釈すれば、含意される成長率の改定は
Δln(P)/κ になる。実際には株価の相対的な動きは成長期待の改定だけでは決まらない
(リスクプレミアム・流動性・センチメント)ので、全部は信じず

```
補正量 = clamp( nowcast_weight × (12ヶ月年率対数リターン − 断面中央値) ÷ κ , ±nowcast_cap )
```

とする(既定 weight=0.25、cap=±15pt)。

**市場全体からの超過分を使う**のが要点である。絶対リターンを使うと、強気相場
では全銘柄が一律に上方修正され、順位に何の情報も加わらないまま確率の水準だけ
が上がる。

**これはモメンタム戦略ではない。** 12ヶ月モメンタムを順位に足しているのでは
なく、成長率という**モデルの入力**を修正している。したがってナウキャストが
効いた銘柄でも、粗利率・希薄化・レバレッジが悪ければ順位は上がらない。

これは 27.21② が挙げていた「加速中の企業を必ず遅いほう(CAGR)に丸めており、
ランキング対象の15%がその取りこぼしにあたる」という既知の限界への直接の対処
でもある。株価は加速を先に織り込むので、`min(CAGR, YoY)` の保守性を保ったまま、
加速分を別経路で拾えるようになった。

実測(9評価日 / 4評価日):順位ICが 1年 +0.063→+0.082、2年 +0.080→+0.103、
デシル単調性が2年 +0.47→+0.59 に改善した。

**この項がライブとバックテストの同一性を壊さないこと**は設計上の要件である。
12ヶ月モメンタムは `price_snapshots` から過去時点でもそのまま引けるので、
27.8の「ライブとバックテストで完全に同一の関数が走る」保証は維持されている。
`annualized_log_momentum` が**実測の経過年数で年率換算する**のは、ライブ(日次
全行)とバックテスト(メモリのために月次サンプル)で値がぶれないようにするため。

### 28.4 σ の縮小推定

σ の4項のうち銘柄ごとに実際に変わるのは(a)過去の年次成長率のばらつきと
(b)レバレッジ倍率だけで、残り(マルチプル・利益率・希薄化の推定誤差)は
全銘柄共通の定数である。しかも(a)は年次が4期以上ある銘柄でしか算出できず、
**実データでの充足率は33%**——残り3分の2は既定値0.30が入る。つまり σ の断面の
ばらつきは、**大半が「測れたか測れなかったか」というデータ充足の差**でしかない。

対数正規の閾値超過確率は σ に極めて敏感なので、この誤差をそのまま確率に通すと
**順位が σ の推定ノイズに支配される**。誤差の大きい推定量を母集団の中心へ寄せる
のは縮小推定(James–Stein)の標準的な扱いであり、実測でも縮小を強めるほど
KPIが改善した。

| 縮小率 | 1年 IC | 1年 上位デシル破綻率(ユニバース比) | 2年 IC | 2年 破綻率 |
|---|---|---|---|---|
| 0.00 | +0.063 | 1.556 | +0.078 | 1.320 |
| 0.50 | +0.070 | 1.378 | +0.081 | 1.222 |
| 0.85 | +0.078 | 1.241 | +0.095 | 0.999 |
| 1.00 | +0.083 | 1.196 | +0.102 | 0.926 |

既定は 0.85。**帰結として v4 の順位は実質的に「生存確率で調整した期待倍率」の
順序に近い**(σ の断面差が15%しか残らない)。これは隠すべき事実ではなく、
**現在のデータで σ の銘柄差を主張できるだけの根拠が無い**という正直な状態表明
である。年次期数が伸びて(a)の充足率が上がれば、この値を下げて銘柄差を復活
させればよい。

### 28.5 `CrossSection`:銘柄間で共有する統計の置き換え

v3は「セクター別のEV/粗利中央値」を銘柄間の共有統計として持ち、終端マルチプル
の回帰先にしていた。v4はその平均回帰そのものを撤廃したため、セクター中央値を
必要とする箇所がモデルから消えた。代わりに必要になったのは、
**ユニバース全体で1組**の値である。

- `median_log_momentum` —— ナウキャストの基準線
- `median_log_sigma` —— σ の縮小の中心

σ の縮小中心は σ 自身の断面統計なので、`build_cross_section` は2段階になる
(基準線でモデルを1周回し、返ってきた素の σ の幾何中央値を中心にする)。
`scores.inputs` に一緒に保存され、APIが任意のホライズンで再計算するときに
復元される(27.24)。

これに伴い `scoring/normalization.py`(セクター中央値とフォールバック、
頑健zスコア)は丸ごと削除した。

### 28.6 財務脆弱性を σ へも伝播させる

健全性デシル別に実績を測ると、**脆弱な企業は上下どちらの裾も厚い**ことが
分かった。

| 健全性デシル | 「1年で2倍」達成率 | 「−50%以下」の割合 |
|---|---|---|
| 最下位 | 15.1% | 15.9% |
| 最上位 | 5.8% | 4.2% |

v3は健全性を生存確率(下向き)にしか反映していなかったため、脆弱な企業の
**上向きの裾を系統的に過小評価**していた。σ に `exp(−sensitivity × health)`
を掛けることで両側の厚さを表す(既定 sensitivity=0.15。health=−1 で σ が16%
広がり、+1 で14%縮む)。

生存確率そのものは維持する。7年というホライズンで**上場廃止したら10倍は実現
しようがない**からであり、実測でも生存項を入れたほうが上位デシルの破綻率が
下がる(1年 1.30→1.09、2年 1.01→0.89、いずれもユニバース比)。
**裾の厚さのうち上向きの分は σ が、下向きの分は生存確率が拾う**という役割分担
にしている。

### 28.7 規模の事前分布を既定で無効にした

v3は「MOICの算術は規模に対して中立だが、現実の10倍達成率は規模とともに低下
する」という仮説で `size_prior` を置いていたが、**実データはこれを支持しなかった**。

| ホライズン | size_prior なし | あり(exponent 0.15) |
|---|---|---|
| 1年 lift | 1.245 | 1.208 |
| 1年 IC | +0.082 | +0.078 |
| 2年 lift | 1.141 | 1.076 |

時価総額デシル別のオンペース率もほぼ平坦(0.22〜0.29)で、**最小デシルは
むしろ破綻率が最悪**(25.4% vs 全体10%)だった。

**帰結:v4は規模に対して中立である。** 恒等式は本質的にスケール不変であり、
ネットデットが0なら現在のマルチプルは完全に約分されて時価総額は確率に影響
しない。旧v2の `corr(スコア, ln時価総額) = +0.227`(大型ほど高得点)という
**逆向きの傾き**が問題だったのであって、傾きが無いこと自体は問題ではない。
ユニバース自体が時価総額$3.5B以下に絞られているため、その中でさらに規模へ
傾けるだけの根拠は現時点の実データに無い。

実測での確認:`corr(スコア, ln時価総額) = −0.022`(v2 +0.227、v3 −0.262)、
`corr(スコア, 売上成長率) = +0.139`(v2 −0.068、v3 +0.237)。成長との相関が
v3より下がっているのは、κ による圧縮が「市場が既に払っている成長の対価」を
差し引いているためであり、意図した動作である。

### 28.8 確率の較正層

**解こうとしている問題。** モデルが返す確率は、log-MOIC が正規分布だと**仮定**
したときの閾値超過確率である。仮定が外れていれば、その数字は「モデルの内部で
整合している値」ではあっても「実際に起きる頻度」ではない。v3では予測平均と
実測頻度が4.8ptずれていた。利用者は「P(10倍) = 3%」という表示を頻度として
読むので、これは誤情報になる。

**7年後の10倍そのものは較正できない。** 価格ヒストリーは3年しかなく、7年後の
実測は原理的に今日存在しない。較正できるのは、擬似バックテストが実際に観測
している事象——**「ホライズン h の期間で10倍/7年ペース(年率38.9%)に乗ったか」**
——だけである。

したがって作るのは「較正済みの P(7年で10倍)」ではなく、
**「較正済みの P(h年でオンペース)」という別の数字**である。これは実測に
裏打ちされており、ホライズンが短いので利用者が自分で答え合わせできる。
**7年後の10倍という検証不能な数字とは別の量として提示する**——較正した数字と
そうでない数字を同じ見た目で並べてはいけない。

手法は Pool Adjacent Violators(等調回帰)。単調性以外に何も仮定せず、
観測の外側では端点で頭打ちになる。9評価日・3,000観測という標本サイズで、
これ以上の仮定を置く根拠はない。**単調写像なので順位は変わらない**——
変わるのは水準だけである。

実測の較正曲線(v4、3,003観測):

| 予測確率の帯 | n | 平均予測 | 実測頻度 |
|---|---|---|---|
| 14.0〜15.3% | 301 | 14.7% | 19.6% |
| 17.0〜17.9% | 300 | 17.5% | 17.3% |
| 20.0〜21.3% | 300 | 20.7% | 28.0% |
| 26.3〜60.8% | 300 | 30.9% | 32.0% |

モデルは低い確率帯で系統的に弱気(14.7%と言って19.6%起きている)、
高い帯ではほぼ正確だった。

**較正は観測範囲の外へ外挿しない。** 最上位の帯より高い確率を出した銘柄には
その帯の実測頻度がそのまま当てられるため、上位数十銘柄の「1年オンペース率」は
ほぼ同じ値(32%)に張り付く。これは**そこから先を区別できるだけの観測が無い**
という正直な表明であり、無理に外挿すれば裏付けの無い数字を出すことになる。
順位そのものは P(10倍) 側で保たれている。

**較正写像は `config_hash` の完全一致を要求する。** パラメータを1つでも変えれば
対応は崩れる。一致する実行が無ければスコアは未較正のまま保存され、UIが
「設定変更後にバックテストが未実行」として状態を明示する。近い設定の写像で
代用すると、較正済みというラベルだけが残って中身が保証されない——それは
較正しないより悪い。

### 28.9 KPIの拡充:平均だけを見ない

保有期間が重複しているため、独立な観測期間の数は評価日数よりはるかに少ない
(27.18)。平均だけを見るとその弱さが隠れるので、次を追加した。

- **評価日ごとの内訳** —— 「9日中何日でリフトが1を超えたか」が読める
- **最悪の評価日のリフト** —— 平均が良くても効かない時期があれば実運用では耐えられない
- **順位IC とその t値** —— 上位デシルだけでなく断面全体の序列を見る。
  t値は評価日を独立とみなした**上限値**であり、有意性の主張として読んではいけない
- **較正曲線** —— どの確率帯で外しているか
- **右裾リフト**(28.11)

### 28.10 事業の質で成長の減衰率を変える

モデル確率の上位デシルの中に限って因子ICを測り直したところ、
**Piotroski F-Score が +0.138(9評価日中8日で正)** と、モデルが取りこぼして
いる情報として最大だった。同じ部分集合で生存確率のICは +0.035 しかなく、
「健全性 → ハザード率」という経路ではこの情報を拾いきれていない。

**F-Scoreの正しい居場所は生存確率ではなく、成長の持続性である。** 9項目は
収益性(ROA・営業CF・その改善・アクルーアルの少なさ)、レバレッジと流動性、
営業効率(粗利率と資産回転率の改善)からなり、**直近の決算が改善基調で実現
しているのか、一時的な山なのか**を測っている。前者なら外挿の出発点は信頼でき、
後者なら成長は早く消える。

v3は `fade` を全銘柄共通の定数(0.75)にしていた。だがそれは「どんな企業でも
超過成長は同じ速さで消える」という主張であり、**成長株投資の中核にある持続性の
差を最初から無いことにしている**。

```
fade = 0.75 + 0.06 × 品質(−1〜+1)
```

F-Score満点で0.81、0点で0.69。7年後に残る初期超過成長は 21% と 7.4% の差になる。

効果:上位デシルの破綻率がユニバース比 1.21 → 1.13、最悪評価日のリフトが
0.79 → 0.94。リフト平均と単調性はほぼ不変。**上振れを削らずに下振れを減らす**
方向に働くため採用した。

### 28.11 「リフト倍率 >= 2.0」という目標は、読み替えの時点で誤っていた

**これはモデルの改良ではなく、KPIの誤りの訂正である。**

14.2は成功指標として「リフト倍率 >= 2.0」を挙げているが、その分子・分母は
本来**「10バガー達成率」——極端に稀な事象**だった。価格ヒストリーが3年しか
ないため、27.12はこれを「1年で1.389倍(=10倍/7年と同じ年率)」に読み替えた。

**読み替えは妥当だが、目標値をそのまま持ち越したのが誤りだった。**
その事象の基準率は**約25%**であり、右裾ではまったくない。基準率25%の事象で
リフト2.0を出すというのは「上位10%の50%が達成する」ことを要求しており、
ほとんど達成不可能な基準である。

事象そのものを右裾へずらして測り直す。閾値は**各評価日の断面のリターン分位**
で決めるので、強気相場でも弱気相場でも基準率は定義上 `quantile` に固定される
——レジームの影響を受けずに「モデルはその日の勝ち組上位◯%をどれだけ多く
捕まえたか」だけを測れる。

**実測(v4、3,003観測、8評価日)**:

| 当たりの定義 | 閾値リターン(中央値) | モデル上位10%の的中率 | リフト | 最悪の評価日 |
|---|---|---|---|---|
| 断面上位25% | +39.4% | 35.6% | 1.43 | 0.82 |
| 断面上位10% | +90.2% | 17.6% | 1.76 | 0.77 |
| **断面上位5%** | **+131.9%** | **10.0%** | **2.01** | **1.15** |
| **断面上位2%** | **+224.9%** | **5.9%** | **2.94** | 0.96 |

**リフトは事象を右へずらすほど単調に上がる。** モデルは平均的な勝ちを見つける
道具ではなく、**裾を見つける道具**である。10バガー探索ツールとしての性能は
下2行のほうが実態に近い。

留保:上位2%の事象は基準率が2%なので、上位デシル(約30銘柄)での的中は
評価日あたり1〜2件しかない。この行の数字はノイズが大きい。また上位25%・10%
では最悪の評価日が1を割っており、**常に効くわけではない**。

### 28.12 ポートフォリオ確率と資産相関

銘柄ごとの確率を並べただけでは、利用者が実際に知りたい「上位20銘柄を買ったら、
そのうち1つでも10倍になる確率は」に答えられない。そして利用者はこれを自分で
計算しようとすると、ほぼ確実に `1 − Π(1 − p_i)` という**独立性を仮定した式**を
使う。これは楽観に偏る。

**10バガーの発生は独立ではない。** 擬似バックテストの評価日ごとのオンペース率は
16.0%〜35.2%と振れており、各銘柄が独立にコインを投げているなら起こりえない
散らばりである(観測数によるノイズをはるかに超える)。

1ファクターのガウシアン・コピュラ(Vasicek)でモデル化する。銘柄 i は
`√ρ·M + √(1−ρ)·ε_i > Φ⁻¹(1 − p_i)` のとき当たる。ρ は資産相関で、
**評価日ごとの的中率の散らばりから推定する**(二項ノイズ分は差し引く)。
実測では **ρ = 0.057**。

正の相関は「少なくとも1つ当たる」確率を**下げる**——当たる年は多くの銘柄が
当たり、外れる年はどれも外れるからである。UIは相関を織り込んだ値と独立を
仮定した値を**並べて**表示する。**N銘柄買ってもN回の独立な試行にはならない**
という事実こそが伝えるべき情報である。

限界:評価日は8個しかなく保有期間も重なっているため、この ρ は桁の目安であって
精密な推定ではない。また ρ はオンペース事象から推定した値を10倍事象に流用して
いる。

### 28.13 検証結果:v3 → v4

同一条件(保有1年、四半期リバランス)での比較。

| KPI | v3 | **v4** | 判定 |
|---|---|---|---|
| 観測数 | 2,642 | 3,003 | — |
| **デシル単調性**(14.2の主指標) | +0.588 | **+0.830** | 大きく改善 |
| **右裾リフト(上位5%の事象)** | — | **2.01** | **目標2.0を達成** |
| 右裾リフト(上位2%の事象) | — | **2.94** | — |
| 順位IC | — | **+0.148**(t +3.2) | — |
| オンペース・リフト(参考) | 1.21 | 1.27 | 基準率25%の事象なので参考値 |
| 最悪の評価日のリフト | — | 0.90 | 8日中1日で1を下回る |
| 破綻回避率 | 8.7% vs 11.2% | 10.6% vs 10.9% | 達成(v3より僅差) |
| 較正誤差 | −4.80pt | −5.02pt | 較正層で吸収する |
| 資産相関 | — | 0.057 | — |

**素直に読むべきこと。**

- 14.2が主指標とするデシル単調性は +0.588 → +0.830 と大きく改善した
- **右裾で測ればリフトは2.0を超える**。ただしこれはKPIの定義を直した結果で
  あって、v3を同じ定義で測り直せば v3 も上がる可能性はある(v3の実行では
  この指標を記録していないため直接比較できない)
- **破綻回避のマージンは v3 より薄くなった**。v3のマルチプル平均回帰は
  「高倍率=高ボラ銘柄を沈める」というリスクブレーキとして働いており、
  それを外した代償である。生存確率と品質フェード(28.10)で埋め戻したが、
  完全には戻っていない
- 較正誤差は改善していない。これは較正層(28.8)が別の数字として引き受ける

### 28.14 削除したもの

| 削除対象 | 理由 |
|---|---|
| `scoring/normalization.py`(セクター中央値・頑健zスコア) | v4はセクター中央値を必要としない(28.2・28.5) |
| `multiple.mean_reversion_weight` / `upward_rerating_growth_spread` / `min_sector_sample` / `floor` / `ceiling` | 平均回帰そのものを撤廃したため、指す対象が消えた |
| `multiple.size_compression_*` | κ による成長フェード圧縮が同じ機構を引き受けた。二重計上を避ける |
| `size_prior`(既定で無効化) | 実データが支持しなかった(28.7)。設定は残す |

### 28.15 検証して**採用しなかった**もの

改善案を試して棄却した記録は、採用した案と同じだけ重要である。同じ道を
二度通らないために残す。

#### ① バリュエーション残差(市場の値づけ規則からの乖離)

「セクター中央値への回帰は誤りだが、**市場自身の値づけ規則 ln M = a + κ·g からの
残差**なら情報を持つのではないか」という仮説。上位デシル内で EV/売上のICが
−0.127(9日中0日で正)だったことから着想した。

**棄却。** 残差そのもののICを測ると **+0.078(9/9で正)**——つまり「市場ルールに
対して割高な銘柄ほど報われた」という、平均回帰とは逆向きの結果だった。
これは2024〜2025年のグロース優位レジームの反映であって構造ではない。
どちらの符号でもこの標本にフィットさせることになるため、**マルチプルの水準に
関する項は一切持たない**という28.2の立場を維持する。

#### ② 実現ボラティリティを σ のアンカーにする

「σ は年次財務からの推測値ばかりだが、実現ボラティリティは σ そのものの直接
測定ではないか」という仮説。ボラティリティの持続性は実証的に最も頑健な事実の
ひとつなので、筋は通っている。

**棄却。** リフトはやや上がるが、**上位デシルの破綻率がユニバース比1.43倍まで
悪化した**。実質的に「高ボラ銘柄を上位に集める」戦略になり、10バガー探索としては
右裾と左裾を同時に買うことになる。加えて、価格由来の量を σ に入れると
`min_expected_moic` の防波堤が効きにくくなる。

#### ③ κ をリターンで較正する

κ=1.5 にするとリフトは上がる。**採らない。** κ は断面の値づけ構造から測れる
**観測可能な量**であり、リターンにフィットさせた瞬間に「その時期にたまたま
高成長株が報われたかどうか」を7年の見通しに固定することになる。
測れるものを推定するのに、測らずに当てにいく理由はない。

### 28.16 残る限界

27.19 から引き継がれるもの、および v4 で新たに分かったもの。

- **バックテストの生存バイアス**(27.15)——データ源の制約であり解消不能。
  上場廃止決済の割合が0.00%のままなのがその警告灯
- **保有期間の重複による検出力不足**(27.18)——価格ヒストリーが伸びれば改善する。
  独立な観測期間は実質3つ程度しかない
- **売上前・粗利前の企業を評価できない**(27.21①)——恒等式モデルの原理的な限界。
  実データで44銘柄、うち32銘柄が臨床段階バイオテック。10バガーの主要な供給源の
  ひとつを構造的に見落としている
- **最悪の評価日ではリフトが1を割る**(28.11・28.13)——2024-05-21起点の1年では
  上位デシルがユニバースを下回った。常に効くモデルではない
- **破綻回避のマージンが薄い**(28.13)——v3のリスクブレーキを外した代償
- **較正が上位で飽和する**(28.8)——観測範囲の外へ外挿しないため、上位数十銘柄の
  較正済み確率は同じ値に張り付く
- **資産相関の推定が粗い**(28.12)——評価日8個、しかも重複。桁の目安でしかない
- **κ が時期とともに低下している**(28.2)——2024年前半0.93 → 2026年前半0.68。
  固定値で運用してよいかは、より長い観測期間が要る
- **ナウキャストの重み・σ の縮小率は弱くしか特定されていない**——独立な観測期間が
  実質3つ程度の標本で選んだ値である。`config/scoring.yaml` のコメントに
  「断面から測った構造パラメータ」「公表された基準率からの事前値」
  「擬似バックテストで選んだ値」の3分類を明記してある

### 28.17 設定スキーマ不一致が「設定ファイルが壊れている」ように見えていた

v4移行の直後、UIに次のエラーが出た。

```
サーバー内部エラー: 8 validation errors for ScoringConfig
multiple.mean_reversion_weight  Field required
multiple.upward_rerating_growth_spread  Field required
multiple.size_compression_exponent  Field required
...
```

**列挙されているのは、v4で削除したフィールドである。** 実際に起きていたのは
「**APIプロセスだけが古く**、v3のスキーマで新しい `scoring.yaml` を検証していた」
という状態だった。27.22のDBスキーマ不一致と**まったく同じ形の障害**である
——ディスク上のファイルは新しく、メモリ上のコードだけが古い。

しかしメッセージは正反対に読める。「必須フィールドが無い」は**設定ファイルを
直せ**と言っているように見え、実際に直すべきなのは**プロセスの再起動**である。
設定を書き戻すと、今度は新しいコードが起動しなくなる。

対処を3つ入れた。

1. **`ValidationError` 専用のエラー応答**(`api/main.py`)——「まずAPIプロセスを
   再起動してください」を先頭に置き、不一致だったフィールド名を `fields` として
   別に返す。27.22で入れた `error_envelope` の枠組みをそのまま使う
2. **`/ready` が設定ファイルも検証する**——従来はDBしか見ていなかったため、
   この障害の最中も `/ready` は200を返し続けていた。**「readyと言っているのに
   何も動かない」は切り分けに最も使えない状態**である。`/ready` が200を返すなら
   データも返せる、を満たすべき性質として固定した(`scoring_version` も返す)
3. **リグレッションテスト**(`tests/unit/test_config_mismatch.py`)——
   `/ready` が503を返すこと、`/health` は200のままであること(切り分けには両方が
   要る)、データエンドポイントのメッセージが再起動を案内することを固定した

#### 付随して踏んだWindows特有の罠:同一ポートの多重LISTEN

再起動したのに古い応答が返り続ける、という現象も同時に起きた。原因は
**2つのuvicornプロセスが 127.0.0.1:8000 を同時にLISTENしていた**ことである。
Windowsでは `SO_EXCLUSIVEADDRUSE` を指定しない限り複数プロセスが同一ポートに
bindでき、**エラーも出ないまま**リクエストがどちらに振られるか不定になる。
「新しいプロセスが正常に起動したのに、応答は古い」という、原因の見えない状態
になる。

さらに `--reload` 付きで起動している場合、停止すべきプロセスは**リローダー親と
ワーカー子の2つ**ある。親だけを停止しても子がソケットを保持し続けるため、
`netstat` には元の親PIDでLISTENが残り続ける。README のトラブルシューティングに
確認手順を記載した。

### 28.18 用語が分からないまま順位だけを見せていた

利用者から「マルチプル、希薄化、デシル、CAGR、YoY など専門用語が多すぎて分から
ない」という指摘を受けた。**これは表示の不親切さではなく、このアプリの目的に
対する欠陥である。**

14.2 は「上位デシルでも大半は外れる前提をUI上にも明示すること」を要件として
おり、27.18・28.13 では未達のKPIを隠さず出すようにしてきた。いずれも
**利用者がこのモデルを信じてよいかを自分で判断できる状態を作る**ためだった。
ところが、その判断材料である「期待倍率」「較正誤差」「右裾リフト」といった
言葉の意味が分からなければ、**判断材料を出していないのと同じである。**
留保事項を丁寧に書けば書くほど、読めない人にとっては情報がゼロに近づく。

対処を3層で入れた。

#### ① 用語集(`frontend/src/glossary.ts`)—— 説明の唯一の出典

52語を9つのカテゴリに分けて収録した。書き方の方針を3つ決めている。

- **`short`(ツールチップの1行)には別の専門用語を入れない** ——
  「マルチプルとはEV/粗利のことです」では何も説明していない
- **定義より「なぜそれを見るのか」を優先する** —— 定義だけなら辞書で足りる。
  このアプリが何のためにその数字を見ているかのほうが、判断には要る
- **具体的な数字の例を必ず入れる** —— 「1000円で仕入れた服を2500円で売れば
  粗利は1500円」のような例が無いと、抽象的な定義は頭に残らない

説明を**1箇所にしか書かない**ことが構造上の要点である。ツールチップと用語集
ページで別々に文章を持つと、必ず片方だけが古くなり、しかもズレたことに誰も
気づけない。用語の説明は、モデルのパラメータと同じくらい「静かに間違う」種類の
情報である。

#### ② ツールチップ(`components/Term.tsx`)—— 読んでいる場所で引ける

用語集ページを作っただけでは、**そこへ行こうと思った人しか読まない**。
表のヘッダや説明文の中で、その言葉を読んだ瞬間に引けることが要る。

**hover だけに頼らないこと**を実装上の要件にした。タッチ端末とキーボード操作
には hover が存在せず、それだけに頼ると説明が完全に消える。`<button>` にして
クリック・Enter・フォーカスでも開くようにし、Escape と外側クリックで閉じる。

#### ③ 型による保証 —— 「説明が付いているつもり」を防ぐ

`<Term id="...">` に存在しないidを渡しても、実行時には何も起きない。説明が
付いているつもりの箇所が**無言で普通のテキストになる**だけであり、このアプリで
最も気づきにくい壊れ方のひとつになる。

`GLOSSARY` を `as const satisfies` で定義して id のリテラル型
(`GlossaryId`)を導き、`Term` の `id` プロパティをその型に縛った。
フロントエンドにはテストランナーが無いが、`npm run build` は `tsc -b` を
含むので、**ビルドの時点で必ず止まる**。実際にこの型を入れた直後、
`DIAGNOSTICS[].term` と `KpiRow.term` が `string` のままだった2箇所を
コンパイラが検出した。

#### 付随して見つかった表示上の不整合

この作業中に、`/reference` 画面が **κ の古い値(1.03)を表示したままだった**
ことが分かった。28.2 で全期間の20断面から 0.86 へ再推定した際、設定ファイルと
モデルのdocstringは更新したが、UIの文言と `api/metric_explain.py` の説明文が
漏れていた。**利用者に見える数字がモデルの実際の挙動とずれている**という、
用語の説明と同じ種類の「静かな誤り」である。あわせて修正した。

### 28.19 全体レビューで見つけた5件のバグ

v4への移行と用語集の追加を終えた時点で全体を通し、実データ(1,079銘柄)で
全エンドポイントを総当たりして検証した。**5件の不具合が見つかり、うち2件は
利用者に誤情報を出していた。**

#### ① 順位が付かない銘柄に、以前の較正済み確率が残っていた(データの誤り)

スコアは同一 (銘柄, 日付, バージョン) の行を `setattr` で上書きするため、
**書き込む値の辞書に無いカラムは前回実行の値がそのまま残る。** 例外も警告も
出ない。ランキング対象と対象外で別々にカラムを並べていたところ、対象外の側で
`calibrated_on_pace_probability` を書き忘れていた。

その結果、**一度ランキングに載ってから外れた14銘柄に、当時の「1年オンペース率」
が残り続けていた**。順位を付けないと判断した銘柄の詳細画面に較正済み確率が
表示される——27.20 が「測れなかった」と「測った結果がこうだった」を分けようと
した、まさにその境界を壊す誤情報である。

**辞書を直すだけでは再発する**ので、書き込みを `_score_values` 1箇所に集約し、
`_MUTABLE_SCORE_COLUMNS` との一致を実行時にアサートするようにした。加えて
`tests/unit/test_score_columns.py` が、この集合と**モデルの実際のカラム定義**を
突き合わせる。カラムを増やしたのに書き漏らせば、テストが落ちる。

#### ② 見通しマイナスの銘柄の詳細が、全件500になっていた(既存バグ)

`CandidateDetail.factors` の型が `dict[str, float]` だった。ところが 27.20 以降、
同じJSONに `unranked_reason`(**文字列**)が同居している。レスポンス検証が
失敗し、**見通しマイナスの256銘柄すべてで詳細取得が失敗していた**——監視リストの
「見通しがマイナス」タブからリンクを踏むと必ずエラーになる状態だった。

v4移行より前から存在していたバグで、今回の総当たり(1,079銘柄の詳細を全件
取得)で初めて表面化した。**個別に動作確認していただけでは見つからない類の
不具合**であり、全件を機械的に叩く価値がここにあった。

型を `dict[str, float | str]` に広げ、あわせて `unranked_reason` をAPIの契約
として独立した項目に出した(`factors` に埋もれていると型も意味も曖昧になる)。
UIにも「順位を付けていない理由」を明示するバナーを追加した。

#### ③ エラーハンドラが、無関係な障害に「再起動してください」と案内していた

28.17 で追加した `ValidationError` ハンドラが**広すぎた**。設定ファイルの
スキーマ不一致だけを想定していたが、②のレスポンスモデル検証失敗も同じ例外型で
飛んでくるため、そちらまで「APIプロセスを再起動してください」と案内していた。

**原因の違うものに同じ対処を案内するのは、何も案内しないより悪い。** 利用者は
再起動を試し、直らず、そこで手がかりを失う。設定の読み込みだけを
`ConfigSchemaError` に包み、ハンドラはその型だけを拾うようにした。
それ以外の検証エラーは本来の500(サーバー側の不具合)へ戻る。

#### ④ 優先株のシンボルがユニバースに混入していた

NASDAQ Trader のディレクトリでは優先株が `FITB$I` のように `$` を含む。
ユニバースの除外は**証券名**のパターン(`preferred` 等)で行っていたため、
名称にその語を含まない25銘柄がすり抜けていた。実害は3つ:

- 普通株ではないので10バガー探索の対象として意味がない
- 日次収集で毎回 404 になり、API呼び出しを無駄に消費している
  (実際に本日のログに `Quote not found for symbol: AHL$D` 等が並んでいた)
- **除外銘柄一覧に出ているのに、クリックすると 422 で開けない**

シンボルの形状は証券名より安定した判別材料なので、`$` を含むシンボルを
ユニバース段階で落とすようにした。既存の `tickers` 行は次のユニバース再取得
まで残るため、APIのティッカー検証パターンも `$` と `.` を許容するようにした
——**一覧に載っている銘柄はすべて詳細を開けなければならない**。

#### ⑤ 用語ツールチップの「くわしく →」が一度も押せなかった

28.18 で追加したツールチップで、開閉のハンドラをトリガーの `<button>` に
付けていた。ツールチップはボタンの**兄弟要素**なので:

- 中のリンクへマウスを動かすと、ボタンから出た時点で閉じる
- リンクを押そうとすると mousedown → ボタンの blur → アンマウント、の順で
  進み、mouseup が行き場を失って**クリックが成立しない**

用語集への導線が存在するのに一度も辿れない、という状態だった。ハンドラを
ラッパー(ボタンとツールチップの共通の親)へ移し、視覚的な6pxの隙間は透明な
疑似要素で埋めた。

#### 副産物:`watchlist` の未知の絞り込み値が「該当0件」に見えていた

`reason` パラメータを検証していなかったため、タイプミスが 200 + 空リストとして
返っていた。利用者には「そういう銘柄が無い」と読める——絞り込みの指定を
間違えたのか、本当に0件なのかが区別できない。`Literal` で候補を固定した。

#### 検証方法

`api_sweep.py` で、ランキング401・監視リスト792・除外200の**全銘柄について詳細を
取得**し、目標(何年で何倍)を6通り変えた再計算と、境界値9通りを確認した。
修正前は 1,078/1,079 が成功(1件が④の422)、②は監視リスト経由の銘柄で発生。
修正後は **1,079/1,079 成功、境界値もすべて期待どおり**。

---

# 【v2.3 追記】規模の上限を目標倍率の関数にする

## 29. ユニバースの上限が「7年で10倍」専用だった問題(2026-08-27)

### 29.1 発見:目標は可変になったのに、母集団は固定のままだった

27.24 で「何年で何倍」を自由に指定できるようにした。しかし `config/universe.yaml`
の規模の上限(時価総額 $3.5B・TTM売上高 $3B)は定数のまま据え置かれていた。

**この上限の根拠は10倍という目標に依存している。** 15.6 と 24章はこう書いている:

> 時価総額上限・売上高上限・セクター除外は、改善して復帰することを期待する類の
> 条件ではない(15.6:**大きすぎる企業は算数上10倍になれない**)

「10倍になれない」は「3倍にもなれない」を意味しない。$10B の企業が7年で10倍
($100B)になるのは考えにくいが、3年で3倍($30B)は普通に起きる。つまり
**目標を緩めたのに母集団は10バガー用のまま**という不整合が、27.24 の時点から
残っていた。UIには目標セレクタだけがあり、母集団が追随しないことはどこにも
表示されていなかった。

### 29.2 設計:出口の規模を不変量に置く

上限を目標倍率で割る形に変えた。割る対象は**目標達成時点(出口)の規模**である。

```
その目標での上限 = exit_ceiling ÷ max(目標倍率, min_supported_target_moic)
```

`exit_market_cap_ceiling_usd = $35B` は「7年後に $35B を超える会社にはならない
だろう」という、19章の $3.5B に暗黙裡に入っていた前提を明示しただけのものである
($3.5B × 10倍)。この定義には重要な性質がある——**既定の目標では上限が1ドルも
変わらない**。したがって v4 の較正(κ・σ・生存ハザード・較正写像)はそのまま
生き、変わるのは目標を緩めたときだけになる。

| 目標 | 必要年率 | 時価総額の上限 | TTM売上高の上限 |
|---|---|---|---|
| 3年で3倍 | 44.2% | $11.7B | $10B |
| 5年で5倍 | 38.0% | $7.0B | $6B |
| **7年で10倍(既定)** | **38.9%** | **$3.5B** | **$3B** |
| 10年で10倍 | 25.9% | $3.5B | $3B |

ホライズンには依存させていない。「10年なら市場全体も成長するぶん出口は大きくて
よい」という理屈は成り立つが(名目GDP 3% で7年→10年なら +9%)、効果が小さい
わりに自由なパラメータが1つ増えるため採らなかった。

#### バッチとAPIの役割分担

スコアは日次で1組しか作れないので、目標ごとに母集団を作り直すことはできない。
そこで **バッチは最も緩い目標(`min_supported_target_moic = 3.0`、上限 $11.7B)の
母集団を materialize し、目標ごとの絞り込みはAPIが行う**。1.5〜3.0倍を指定した
場合は上限がそこで頭打ちになり、レスポンスの `universe_ceiling_capped` が真になる
(UIが「母集団はここで頭打ちです」と明示する。黙って狭い母集団を返すと、利用者は
上限が目標に追随していると誤解したまま「該当が少ない」という結論を受け取る)。

#### 断面統計(`CrossSection`)も目標の関数になる

28.5 の断面統計——ナウキャストの基準線(市場全体の動き)と σ の縮小中心——は
**母集団の中央値**である。母集団が変われば変わる。バックテスト
(`backtest.runner._evaluate_one_date`)は昔から「検証している目標の母集団」から
断面を作っており、v4 のKPIはその構成で測ったものなので、ライブも同じ構成に揃えた。

- `run_scoring` が保存する断面 … **既定の目標の母集団**($3.5B 未満)から作る
- 目標を変えたリクエスト … APIが**その目標の母集団**から作り直す
  (`routes._cross_section_for_target`)

前者により、既定の目標のスコアは29章の前後で**ビット単位で同一**になる。

### 29.3 実測:広げても選別力は落ちない

上限だけを変えて同じモデルで擬似バックテストを回した(目標「3年で3倍」・保有1年・
評価日9点、2026-08-27)。

| 上限 | 観測数 | デシル単調性 | リフト(最悪日) | 順位IC | 上位デシル破綻率 |
|---|---|---|---|---|---|
| $3.5B(現行) | 2,947 | +0.794 | 1.38 (**0.86**) | +0.146 | 11.7% vs 10.8% |
| **$11.7B(採用)** | 5,116 | +0.879 | 1.55 (1.37) | +0.140 | 9.5% vs 8.1% |
| $24B | 5,976 | +0.952 | 1.64 (1.37) | +0.131 | 9.3% vs 7.4% |

**リフトの改善の大半は分母の希薄化である**(ユニバースのオンペース率が
22.8% → 20.3% へ下がる)。上位デシルの的中率そのものは3案ともほぼ30%で動かない。
意味があるのは次の2点:

1. **最悪評価日のリフトが 0.86 → 1.37。** 1を割る=その日は上位デシルがユニバース
   に負けた、という 28.16 由来の既知の弱点が、母集団を広げると解消する
2. **新しく入る帯がランキングを占拠しない。** $3.5B 以上はユニバースの 37.7% を
   占めるのに、上位デシルでは 31.1% にとどまる

②は事前の最大の懸念に対する答えである。モデルは規模に対して中立
(`size_prior.exponent = 0.0`)であり、MOICの算術は $10M の企業と $10B の企業を
同じに扱う。無条件の実測では規模と上振れ頻度に明確な差がある
(開始時点の時価総額で層別した3年リターン:$1B–3.5B の3倍達成率 9.0% に対し
$3.5B–12B は 5.4〜7.3%)。にもかかわらず**条件付き**では占拠が起きなかった——
売上成長率・粗利率・希薄化といったモデルの入力が、規模の効果を実質的に吸収して
いるためと解釈できる。28.7 が「規模の事前分布は実データに支持されなかった」と
した結論は、$11.7B まで広げても維持される。

$24B を採らない理由:単調性とリフトはさらに良く見えるが、それは①と同じく分母の
希薄化が効いている一方、**順位ICは3案の中で最も低い**(+0.131)。κ・σ・生存
ハザードはいずれも $3.5B 以下の母集団で測った値であり、外挿を7倍まで広げる根拠
としてこのKPI差は弱い。3.3倍($11.7B)に留める。

### 29.4 κ の再測定(28.2 の指示に従って)

`config/scoring.yaml` は「ユニバース(市場・時価総額レンジ・除外セクター)を
変えたら必ず測り直すこと」と指示している。17断面で再測定した。

| 母集団の上限 | κ(クランプ含む) | 断面間SD |
|---|---|---|
| $3.5B | +0.832 | 0.125 |
| $11.7B | +1.001 | 0.101 |
| $24B | +1.014 | 0.101 |

乖離 0.169 は断面間のばらつきを超えており、**κ は母集団のレンジに依存する**。
ただし κ = 1.00 を採ってもKPIは改善しなかった(3年3倍・$11.7B:単調性
0.867→0.806、最悪日リフト 1.39→1.32、順位IC +0.137→+0.140)。単調性と最悪日
リフトが揃って悪化し、改善するのは順位ICだけなので**値は変更しない**。断面回帰の
傾きが母集団の合成で動くこと自体は珍しくなく(大型株が低成長・低倍率の一点に
集まると傾きが立つ)、構造パラメータが変わった証拠とまでは言えない。

同時に E-4(defect_audit_2026-08-27.md)の宿題も片付いた:クランプ銘柄を含む κ
(+0.8318)と除いた κ(+0.8132)の乖離は 0.0186 で断面間SD 0.1252 の範囲内。
**クランプ済み成長率を説明変数に使う現在の測定方法は妥当**であり、
`raw_initial_growth` への切り替えは不要。

### 29.5 実装で気をつけた点

#### ランキングにも除外一覧にも出ない銘柄を作らない

$5B の銘柄は日次バッチのゲートを通る(materialize 範囲は $11.7B)が、既定の目標
では候補ではない。この状態は29章で初めて生じるもので、放置すると**ランキングにも
`GET /excluded` にも現れない銘柄**ができる。`GET /excluded` と銘柄詳細を目標依存に
し、バッチと同じ理由コード(`market_cap_ceiling` / `revenue_ceiling`)を合成して
返すようにした。フロントはヘッダーのリンクで目標(`h` / `m`)を引き継ぐ。

#### セクター絞り込みをSQLから外した

断面統計は母集団全体から作らなければならない。セクターの絞り込みをSQLで先に
掛けると、**セクターを選んだ瞬間に σ の縮小中心とナウキャスト基準線がその1
セクターのものに変わり、確率そのものが動く**。絞り込みは表示の操作であって
モデルの前提を変える操作ではないので、母集団を確定したあとに適用する。

#### `_latest_raw_snapshots_by_ticker` が全スナップショットを読んでいた

該当ティッカーの**全行**(1銘柄あたり平均3行 × 約13KB)を読んでPython側で先頭を
取っていた。母集団が約1.9倍になるぶんそのまま応答時間に乗るため、`DISTINCT ON`
で最新1行に絞った。詳細画面が断面を作り直すときは、payload 全体ではなく規模の
2値だけをSQLで射影する(`routes._scale_by_ticker`)。

### 29.6 設定の書き方

同じ量を2箇所に書くと必ず食い違うため、ドル建ての上限は**設定に書かない**。

```yaml
exit_market_cap_ceiling_usd: 35_000_000_000  # 出口の規模
exit_revenue_ceiling_usd: 30_000_000_000
min_supported_target_moic: 3.0               # materialize する最も緩い目標
```

`market_cap_ceiling_usd` / `revenue_ceiling_usd` は導出値(プロパティ)になった。
